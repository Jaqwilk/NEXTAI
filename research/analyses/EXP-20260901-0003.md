# OBSERVATION

EXP-20260901-0003 completed technically and scientifically under immutable plan
`research/plans/EXP-20260901-0003.json` (SHA-256
`2a97e6a42b0e42af7b4189e4db033a21cf7669d9143097a70913404f78c2b995`)
with runner-random seed `808114446`. All ten candidates completed all nine
K-by-exposure cells. Pre/post integrity passed with evaluator digest
`62a12fd340bfb536ad37c9cf02930cf839b88d75c20440078385dbf2d73b1db4`.

The shared compiler obtained accuracy, warm accuracy, near-equivalent accuracy,
minimum-combination accuracy, new-fact accuracy and retention `1.0`, with false
reuse `0.0`, in every cell. At each K=8/32/128 its warm query work fell from
`670.5` operations at exposure 1 to `666.0` at exposure 4 and `661.5` at
exposure 16. Thus the requested exactness, local-update retention, bounded-K and
declining-warm-work signatures were observed.

The full-cost decision was negative. The exact interpreter matched every quality
and stability value while using fit/meta-fit `0/0`, query/warm-query `734/660.5`,
update `1`, state/peak `64/64` bytes and R16 workload `51,814`. The compiler used
fit/meta-fit `166,494/166,494`, query/warm-query `749/666`, update `1,159`,
state/peak `2,472/274,416` bytes and R16 workload `125,794.33`. The valid
implementable Pareto frontier contains only `operator_interpreter`.

# INTERPRETATION

Counterexample-derived probes did learn a reusable exact fingerprint and its
probe prefix shortened with experience. That qualitative effect is real in this
cohort, but it does not create capability and saves only nine warm operations
relative to the first exposure. Acquisition, probe fitting, updates and state
more than erase the saving. Direct recursive interpretation therefore dominates
the exact proposed mechanism at matched capability and full cost.

This falsifies the exact HYP-0030 mechanism and cost claim, not every possible
form of learned operator discovery. Increasing probe count, changing cache
thresholds or making the tested operators more expensive would tune the task or
mechanism after observing the result and is not authorized.

# CONFIDENCE

Confidence is `0.92` that this exact counterexample-probe compiler should be
discarded. The comparison uses exact operation counts, all nine cells, complete
mandatory controls and a valid nonempty frontier. Only one runner-random seed
was used, so seed variance is unestimated; however, that uncertainty cannot
reverse the large fixed fit, update, state and workload gaps while both methods
are exactly correct. This visible synthetic scout makes no broader claim about
open-world learned representations.

# ALTERNATIVE EXPLANATIONS

Longer or intrinsically expensive operators might amortize probe fitting over a
larger interpreter cost, but that is a different preregistered question and
would require a new discriminator rather than post-result depth tuning. The
finite executable tables may also make the direct interpreter unusually strong;
conversely, they make the compiler's exactness easier than open-world operator
discovery. Neither possibility rescues the tested full-cost claim.

# DECISION

`discard`. Mark HYP-0030 `falsified` for this exact mechanism. Preserve the
positive qualitative signature as diagnostic history, but do not replicate,
promote or tune it: the compiler is Pareto-dominated by the exact interpreter.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, leave operator compilation and run one quick breadth scout of
a fundamentally different mechanism on the already frozen
`heldout_wt_changepoints_prequential_v1` cohort: a source-identical,
event-triggered sparse local-plasticity rule whose state and updates touch only
surprisal-selected coordinates. Preregister its fixed trigger/update law before
implementation, compare it unchanged against persistence, LMS, RLS, transition
bank and bounded replay, and require matched NRMSE plus a qualitative reduction
in update bytes and R16 workload across all three K scales. A valid negative
ends that exact rule without threshold tuning; one positive seed selects only a
later unchanged replication.
