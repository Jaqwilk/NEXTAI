# OBSERVATION

`EXP-20260830-0057` completed all six registered K×depth cells for all eight candidates under immutable plan `research/plans/EXP-20260830-0057.json` (SHA-256 `3f0dcfceba6f70eaeabad9c92a2061decd0db1386e1e74f8ea930229ab400c36`) and runner-realized seed `405583788`. Integrity passed before and after scoring with evaluator SHA-256 `c57762ee52133e5fa8cf732d8c5c844dd877bbdccabb58edb3cce4a0858ed553` and candidate-bundle SHA-256 `7373db27571a517c7405f74235df30e9ce315d9f4920326ba7cda12e3de54230`.

The preregistered semantic fixture passed before scoring: observable equations recovered two transitions absent from the direct composition path; the no-relations control failed; relabeling opaque states and permuting anonymous maps preserved the completed maps.

The shared operator-algebra learner scored `0.625` mean accuracy but `0.0` minimum-cell/minimum-combination accuracy. It therefore missed the preregistered `0.95` mean and `0.90` minimum gates by `0.325` and `0.90`. The cell split is sharp: at K=8, accuracies were `0.50`, `0.25`, and `0.0` for depths 1, 4, and 6; at K=32, accuracy was `1.0` at all three depths.

The no-relations ablation and exact-MDL control each scored `0.0416667`; the independent ablation scored `0.0`. Thus observable relation propagation improved mean accuracy by `0.5833333` over both no-relations and exact MDL on this seed, and by `0.625` over independent fitting. The classical unigram, order-5 Markov, and nearest-template controls scored `0.0`, `0.0`, and `0.0208333`. The privileged oracle scored `1.0` in every cell.

The shared learner paid meta-fit/search cost `135671` operations versus `5192` for exact MDL, a factor of about `26.13`; resident state was `26064` versus `16400` bytes, about `1.59×`. Mean query cost was equal at `7.333333` operations, and R16 workload excluding the separately reported meta-fit axis was equal at `3774.333333`. Acquisition (`1896`), fit (`1320`), update (`1`), peak bytes (`225696`), and mean bytes touched (`58.666667`) remain explicitly reported. No implementable candidate entered the capability-gated Pareto frontier because every implementable candidate failed the frozen quality floor.

There is one runner-random seed. The six cells are controlled conditions sharing that seed, not replications; uncertainty about seed-to-seed robustness remains high. The large threshold failures and the exact K-stratified pattern are nevertheless objective for this registered run.

# INTERPRETATION

The causal factor worked locally: global operator equations, rather than direct pair selection or independent fitting, explain the large improvement and exact K=32 transfer. This is stronger mechanistic evidence than EXP-0056. It is not a successful hypothesis test because low-support transfer collapses under rollout, the minimum gate is zero, no implementable candidate reaches the capability-gated frontier, and the full meta-fit cost is much higher than exact MDL.

The K=32 result must not be rescued post hoc by narrowing the benchmark or replicating only favorable cells. The immutable outcome policy says a valid null makes this exact learner dormant without changing support, thresholds, split, roles, or controls after observing scores. A K-only replication would be post-score selection.

The result applies to one supplied opaque finite-function interface. It does not demonstrate one representation across heterogeneous worlds, a general successor to language models, or a scaling law. The next direction should return to the project's multi-world objective rather than specialize this algorithm to CB recombination.

# CONFIDENCE

- `1.00` that EXP-0057 is complete, scientifically valid, and integrity-matched.
- `0.995` that the exact preregistered learner fails its mean and minimum quality gates.
- `0.98` that relation propagation, not direct pair selection, caused the observed K=32 capability on this seed.
- `0.90` that favorable K=32-only replication would be an invalid post-hoc narrowing rather than the registered next step.
- `0.75` that the broader observable-equation principle merits future consideration only inside a genuinely shared multi-world learner, not another recombination-specific patch.
- Reduce HYP-0022 confidence from `0.14` to `0.08` and mark the exact hypothesis `dormant`.

# ALTERNATIVE EXPLANATIONS

- Seed `405583788` may make equation discovery unusually easy at K=32 or unusually sparse at K=8; one seed cannot establish robustness.
- The supplied common state space may make relation closure effective without learning a representation that transfers across genuinely different interfaces.
- The sharp K threshold may reflect simple coverage percolation in a finite partial permutation, not a new learning principle.
- Meta-fit accounting is explicit, but the aggregate R16 workload does not fold `meta_fit_ops` into its scalar; Pareto interpretation must retain meta-fit as a separate cost axis.

# DECISION

`discard` this exact low-support operator-algebra learner and mark HYP-0022 `dormant`. Preserve the plan, seed, result, logs, candidates, fixture, negative cells, and this analysis. Do not tune the relation threshold, support size, target split, rollout, or pair selection against this visible seed, and do not replicate only K=32.

# NEXT DISCRIMINATING EXPERIMENT

No second experiment is started in this wake. In the next wake, perform one no-scoring design gate for a new HYP-0023: audit whether a single family-neutral observable-equation interface can be derived losslessly from at least three existing benchmark families without family labels, hand-written ontologies, native-type dispatch, or evaluator-supplied common state semantics. The gate must provide one manual positive fixture shared by all selected families and one negative routing/classification control. If and only if that interface is identifiable and the negative control fails, preregister a later EXP-0058 comparing one unchanged learner with source-identical independent learners and the strongest contextual Chow–Liu, empirical-joint, autoregressive, exact-search, and oracle controls under all full-cost axes. If the gate fails, reject HYP-0023 before implementation and choose a different principle rather than constructing another benchmark-specific algorithm.
