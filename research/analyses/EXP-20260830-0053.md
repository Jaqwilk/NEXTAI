# OBSERVATION

`EXP-20260830-0053` completed all 18 registered cells for each of ten candidates under immutable plan `research/plans/EXP-20260830-0053.json` (registered SHA-256 `5123ba282e9f2f7ffd88260757b2a29975ca83e953449d81e45d5a3b22cb8076`) and runner-realized seed `1163243315`. The evaluator SHA-256 remained `fab675150f7f4d5af8ff3433ab10e08743fe55a77c0444a42eff037b9506a3f0`; candidate bundle SHA-256 remained `32a251727a844ae433751aa0cf7aba34a3f4b2789b7fc2afa318496969547514`. Before seed realization, the registered PPM-D and CTW reference fixtures, deliberate first-order impostor rejection, cohort tests, full 233-test suite, candidate audit, eight baseline records and integrity gate passed.

The iterative learner scored `5.287130` mean and `6.257308` worst-span bits per byte, `0.186740` byte accuracy and `0.0` exact-span accuracy. The source-identical one-pass ablation scored `4.875813`, `5.423967`, `0.193034` and `0.0`, respectively. Thus iteration worsened aggregate loss by `0.411316` bits/byte, worsened worst-span loss by `0.833340`, reduced byte accuracy by `0.006293`, and produced no exact-span gain.

At D=4 the iterative and one-pass mean losses were `5.384` and `4.875813` bits/byte; at D=6 they were `5.603` and `4.875813`. The preregistered requirement was an iterative improvement of at least `0.05` in both groups. At the registered L=128, D=6 endpoint the iterative critical path was `50`, within the ceiling of `60`, but the corresponding one-pass path was `9` and iteration did not improve quality.

The iterative learner used `82,758,718.9` R16 workload operations versus `39,347,585.0` for one-pass (`2.103x`), `5,804,460.9` total workload operations versus `3,091,265.0`, and `1,324,373.3` mean bytes touched versus `573,440.0`. Both reported `526,848` resident/peak state bytes, `633,252` acquisition operations and `65,501` fit operations. One-pass was therefore no worse on any registered capability/cost axis and strictly better on loss, byte accuracy, critical path, total/R16 work and bytes touched; the runner excluded iterative from the implementable Pareto frontier.

Among implementable controls, empirical unigram had the lowest aggregate loss at `4.842257` bits/byte, one-pass had `4.875813`, parallel Markov BP `5.354621`, exact bidirectional Markov `5.357770`, faithful CTW `6.241680`, PPM-D `8.468859`, and dense autoregressive `16.002011`. PPM-D alone achieved nonzero implementable exact-span accuracy (`0.041667`) and the highest implementable byte accuracy (`0.202962`), but at high loss and work. The privileged oracle was exact with zero loss and is not implementable evidence.

There is one runner seed. Its 18 K/D/span cells share the same corpus, permutation and scoring seed and are not independent replications; seed dispersion is unavailable. EXP-0047 is scientifically invalid and is not used for comparison, confidence change, replication or falsification.

# INTERPRETATION

Bounded simultaneous execution itself worked and its declared critical path stayed below the registered ceiling, but repeated self-filled context did not extract useful additional structure. It amplified early errors: loss rose as the allowed rounds increased while the identical one-pass predictions remained better and cheaper. This valid corrected cohort falsifies the exact registered iterative masked learner mechanism under the preregistered null rule.

The result does not show that all parallel infilling or discrete diffusion mechanisms fail. It shows that this fixed local conditional model plus confidence-selected self-filling has no earned basis for further tuning on the visible corpus. Changing the reveal rule, representation, model order, corpus or training objective would be a new hypothesis.

# CONFIDENCE

- `0.99` that EXP-0053 is technically and scientifically valid under the frozen v2 protocol.
- `0.995` that the exact iterative mechanism fails every preregistered quality/ablation gate.
- `0.99` that one-pass implementably Pareto-dominates iterative on this cohort.
- `0.90` that more rounds systematically harm this implementation rather than reveal a hidden benefit; the single-seed visible screen prevents a broader claim about all parallel masked models.
- Reduce HYP-0017 confidence from `0.16` to `0.03` and mark it `dormant`.

# ALTERNATIVE EXPLANATIONS

- A learned corruption/reveal schedule or richer representation could make iterative refinement useful, but either changes the tested causal mechanism and would be post-result tuning here.
- The local visible corpus and single byte permutation may be unusually unfavorable. That uncertainty cannot rescue a result whose source-identical ablation is better in both registered multi-round groups and across all aggregate quality/cost gates.
- Exact-span accuracy is very stringent for 32/128-byte spans, but the primary probabilistic loss also worsened materially, so the null is not an artifact of exact-match alone.
- Faithful PPM-D and CTW are left-to-right controls whose performance depends on their frozen semantics; their weak aggregate scores do not benefit the iterative learner because the simpler one-pass and unigram controls already defeat it.

# DECISION

`discard` the exact iterative masked learner mechanism and make HYP-0017 `dormant`. Preserve the plan, seed, result, logs, candidates and this analysis. Do not tune rounds, reveal schedule, context, model order, corpus or state cap, and do not replicate EXP-0053. The result supports the skeptical HYP-0012 full-system accounting principle but cannot promote any architecture.

# NEXT DISCRIMINATING EXPERIMENT

The next wake may not reopen HYP-0017 by tuning. Use one design/service-only cycle with no scoring to specify and freeze a new cohort for a genuinely learned, family-blind latent-variable mechanism on existing worlds, with an analytic pre-scoring identifiability fixture that cannot be solved by unigram, finite-order Markov, nearest-template or supplied family labels. Only the following wake may preregister a one-seed quick comparing that one shared learner with its source-identical independent ablation, contextual Chow-Liu, empirical joint, autoregressive specialists and oracle under full R16/state/bytes accounting. If no such fixture can be made identifiable without a hand-written ontology, record that blockage and create no experiment.
