# EXP-20260905-0001 — interrupted PC-01 development, not a learning failure

Immutable plan: research/plans/EXP-20260905-0001.json.
Canonical plan SHA-256: 3286682aeb04cacc71922ca786d8578debe6ccd291d4dc12bd63345f827c4391.
Immutable result: research/results/EXP-20260905-0001.json.
Result file SHA-256: db815b440bf0bcc7b289547931178b5f1a7fa4c143367a93035348cd45a8a3dc.

## OBSERVATION

The single authorized dev attempt ran through the audited runner, seed 1103,
2026-09-05 13:09:02–13:10:07 UTC. It returned worker_crash / inconclusive,
exit code 1. The parent did not terminate for a budget/resource limit.
No retry, final-series freeze or final evaluation occurred.

The worker traceback identifies sample_device -> atomic_write_json -> os.replace:
PermissionError [WinError 5] while replacing device.json with a temporary JSON.
This occurs at the per-update telemetry write, after optimizer.step; no
fit-finished.json or final measurement envelope was produced. Exact last update
is not persisted: 750 updates are checkpoint-confirmed, and the next scheduled
1000-update checkpoint is absent. Do not label 750 as the exact crash iteration.

| Saved update | Dev bits/byte |
|---:|---:|
| 0 | 8.128573081664243 |
| 250 | 2.959368253657654 |
| 500 | 2.4720044282098574 |
| 750 | 2.336437023032063 |

These are retained intermediate development observations, not a complete valid
PC-01 measurement. Only one seed and one corpus were observed; no across-seed
uncertainty interval or final-test performance can be estimated. The immutable
result correctly has measurement=null, empty architecture candidates/frontier,
and architecture_promoted=false. The required 5000-update recipe, final controls,
FP32/BF16 comparison and end-to-end timing battery did not finish.

Supervisor measurements: 58.1819114 s fit, 64.8588800 s total worker;
peak process-tree RSS 1421934592 bytes; CUDA allocated 1768945152 and reserved
1893728256 bytes; PC-01 payload 44406874 bytes; disk free 110830465024 bytes.
As preregistered for an unfinished fit, the attempt is conservatively charged
1200 s, not just observed wall time. Current approval: 1/1 registrations used.
Design aggregate: 1200/7200 s charged, 6000 s remaining; this remainder does NOT
authorize another attempt. Resource peaks are sampled, not hard OS quotas.

Integrity passed before and after, 861 protected files. Evaluator remained
ad311a51ff3652e5bdf9ac283b9b4d24ea1a9734c7f2a43641426793183a0f77.
Candidate file SHA-256 remained
426bfa8b9da43fb7cd991e104e48dfd366cf037dd9e840330d671b2174706aaa.
806 preactivation tests passed with no errors/failures/skips. The earlier
report-freshness failure and pre-registration-only history-validator limitation
are retained in the activation analysis/addendum, not hidden by re-freezing.

## INTERPRETATION

The finite recipe was interrupted by telemetry persistence, not by a failed
learning threshold or exhausted training allowance. The intermediate curve is
consistent with learning, but cannot certify the positive control or support
architecture/economics/transfer claims. An incomplete implementation run does
not falsify a transformer family or change unrelated BELIEFS/HYP-0012.

## CONFIDENCE

High that the immediate failure is the recorded Windows file-replacement error
and that the attempt is incomplete. The precise holder/cause of access denial
is unknown. No independent seed/corpus replication supports a broader claim.

## ALTERNATIVE EXPLANATIONS

The supervisor reads device.json every 50 ms while the worker replaces it after
each update; monitoring also read telemetry during this run. A transient file
access conflict is plausible, but the traceback does not identify the responsible
reader. Antivirus/indexing or another access restriction cannot be excluded from
the recorded evidence. Do not assert a specific lock owner without reproduction.

The unit suite exercised short process fixtures and a meta model, not thousands
of high-frequency Windows read/replace interactions. Passing those tests did not
establish production telemetry reliability. No threshold/optimizer/model change
is justified by this failure.

## DECISION

Inconclusive. Preserve every result, plan, ledger entry, worker artifact and the
last development checkpoint. Do not restart this EXP, resume the checkpoint,
change evaluator/source bytes or open the final series. The one approved attempt
is consumed; laboratory progress must point to a user decision.

## NEXT DISCRIMINATING EXPERIMENT

First request a bounded no-training maintenance stage to reproduce Windows
telemetry writer/reader contention with synthetic JSON, including transient and
persistent denial, STOP/deadline behavior and resource-limit detection. Select
and test a bounded robust telemetry mechanism without disabling enforcement or
swallowing persistent failures. Correct the auxiliary history validator to use
prefix verification for append-only ledgers in that prospective revision.

Only after that stage passes, archive/freeze the changed harness under a new
comparison identity/certificate and obtain separate authorization for a newly
registered dev attempt from initialization. Keep the model/data/5000-update
recipe and 1200/1800 s caps unchanged; no rescue tuning from final data.
