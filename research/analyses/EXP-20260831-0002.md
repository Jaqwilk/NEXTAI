# OBSERVATION

`EXP-20260831-0002` is a valid one-seed quick on frozen `heldout_three_family_continuous_transfer_v2`. The immutable plan is `research/plans/EXP-20260831-0002.json`, plan SHA-256 is `90747c310272833aa1b294d215faf72b1262a8c469daa57c409927b37eaf9e98`, evaluator SHA-256 is `b035558e9ac636952c975f2e708032e55eedb1741de46c57cf96a754e93c2107`, candidate-bundle SHA-256 is `6d89cd44f610e40d1ec4d9b6a04b5751086520a3dcc4c094176b8db04a99a283`, and the runner-realized seed is `1731376362`. All eleven candidates completed; integrity passed before and after scoring. The result-file SHA-256 is `cf69ce822c50dbc4ed048389ba1530788a271d97f14fad7c49199999f6b7cdbe`.

The shared exchangeable residual learner reached transfer accuracy `0.619947`, minimum-family accuracy `0.415508`, normalized RMSE `0.832876`, stable-rollout rate `1.0`, and minimum shared-versus-independent gain `+0.355428`. Its shared causal gain was positive in every family and K cell. Its 24-byte learned prior and bounded prediction rule also passed the preregistered world-order, channel-permutation and 50-step stability fixture.

The second causal contrast failed decisively. Cross-family-only versus support-only gain was negative in both mechanical families at both K values: DS08a `-901571.05` and `-732526.69`, DronePropA `-5.81` and `-9.01`. It was positive only in continuous-event worlds (`+0.34`, `+0.35`). Thus the preregistered every-family transfer criterion failed four of six cells.

Persistence reached lower overall transfer accuracy (`0.525307`) and higher NRMSE (`0.920498`) than shared, but better minimum-family accuracy (`0.466142` versus `0.415508`). It also required less fit and adaptation work (`186624` and `884736` operations versus `486162` and `4606080`) and slightly less R16 workload (`9.6922440197e9` versus `9.709896342e9`). The shared learner therefore improved the family-balanced average but worsened the worst family and did not satisfy universal Pareto eligibility; both reported frontiers are empty because no complete candidate passed the frozen minimum screening-accuracy gate.

Uncertainty across runner seeds is unestimated because quick used exactly one seed. The newly requested minimum meaningful effect above development noise was not part of the already immutable plan, so no positive contrast from this run may be interpreted using a retrofitted threshold. This omission cannot rescue the result because the cross-family signs and worst-family criterion are already negative.

# INTERPRETATION

Channel exchangeability and bounded residual prediction repair the numerical instability of the coordinate-aligned spectral learner and produce a substantial aggregate gain when same-family training worlds are available. They do not yield a representation that transfers across all heterogeneous families: foreign-family data is actively harmful for DS08a and DronePropA, while only the synthetic continuous-event family benefits.

The likely explanation is that the global scalar residual prior acts as useful regularization when combined with related training worlds but lacks enough shared causal structure to transfer between mechanical families under the frozen shape-only recursion. This rejects the exact fixed ridge-0.001, prior-weight-0.25, residual-clip-0.25, state-clip-8 mechanism, not all permutation-equivariant learners.

# CONFIDENCE

Confidence that this exact learner merits replication falls from `0.10` to `0.02`. Confidence is `0.99` that the preregistered conjunctive positive is false on the realized seed: four cross-family cells are negative, worst-family accuracy is below persistence, and the learner is not on an implementable frontier. Confidence is `0.80` that same-family regularization rather than portable cross-family representation explains the aggregate improvement; one seed limits claims about variance.

# ALTERNATIVE EXPLANATIONS

- The realized seed may exaggerate foreign-family harm, but both K values have the same negative sign in two families and the magnitudes are not marginal.
- The mask-derived recursion may be a poor common observation model for the mechanical families; changing it after viewing this result would be outcome-informed and is not an admissible replication.
- A richer permutation-equivariant mechanism might model cross-channel interactions, but that would be a new hypothesis rather than a rescue of this scalar learner.
- The frozen global minimum-accuracy gate makes the capability frontier empty. This prevents a non-dominance claim but does not alter the negative causal and worst-family observations.

# DECISION

`discard` this exact learner and mark HYP-0025 `falsified`. Do not tune its ridge, prior weight or clipping constants and do not run the three-seed replication. Preserve the aggregate improvement as diagnostic evidence that stable same-family residual regularization is possible, not as evidence of cross-family transfer. No claim about an LLM successor, general intelligence or a scaling law is supported.

# NEXT DISCRIMINATING EXPERIMENT

The next wake should be one service-only cycle with no hypothesis, plan, seed or scoring: determine why plans record `git_commit` as `null`; if the repository truly has no base commit, create one local snapshot containing code, configuration and scientific history while excluding large data, secrets and temporary files. Do not push or publish. Also record that any future scientific plan must preregister a development-only minimum meaningful effect above noise before scoring. Do not create another benchmark or schema absent a real integrity defect.
