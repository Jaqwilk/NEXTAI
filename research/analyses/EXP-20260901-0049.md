# OBSERVATION

`EXP-20260901-0049` completed all eight registered roles and all nine K/depth cells at runner-random seed `1135723238`. The immutable plan is `research/plans/EXP-20260901-0049.json`, with scientific commitment SHA-256 `f6edede708347df8473cdf10f2aa9fb8ef2167a3d81959b30c15ca3c7bf7fe07` and file SHA-256 `fbefcecb78f22ee4e8427f8c5f212b9feac4df926f1f66625a0932656bada3f2`. Result SHA-256 is `45dbb91e2677bcfe09879d34825033aef720366d89e970ef273d36af56fd2818`. Every candidate completed without timeout, crash, missing metric or budget failure. Pre- and post-score integrity passed over 738 protected files with evaluator digest `af865df28ad29228ae185f24972b2320e1bd9db03973906a21e6e25455b99fa` and candidate bundle `9a98edc6c8f9b7197cd2610e51718a9ebb4d01f8c5a77fe7aea9d945f362867e`.

The aligned tree obtained aggregate normalized RMSE `0.166979`, accuracy `0.347222`, damaged-input accuracy `0.138889` and stable rollout `1.0`. The shuffled-gain source-identical role was better at RMSE `0.156621` and accuracy `0.472222`, with the same damaged-input accuracy and stability. The matched aligned-minus-shuffled RMSE difference across nine cells was `+0.010358 ± 0.018428` screening SE, range `[-0.071606,+0.090581]`. At D8 the aligned role was worse at every K: the differences were `+0.090581`, `+0.054875` and `+0.065234` for K 64, 256 and 1024. Although aligned was better in all three D16 cells, the preregistered every-K D8/D16 conjunction failed and the aggregate sign was adverse.

Partition growth itself had a real quality effect. The frozen one-leaf role and the ordinary affine ridge control produced identical predictions and aggregate RMSE `0.211280`; aligned improved on both by `0.044301 ± 0.013114` screening SE across cells. However, the frozen role paid `2.753M` fit operations for source-identical proposal evaluation versus only `87,061` for ordinary ridge. Aligned paid `18.146M` fit operations, `9,103` mean query operations, `7,024` state bytes and `19.464M` R16 workload. Ridge paid `8,363` mean query operations, `480` state bytes and `1.298M` R16 workload. Thus aligned was about `15.0x` ridge at R16 and did not even reduce mean query work.

The aligned three-scale K query slope was effectively zero (`0.000061 ± 0.002342`), so dormant ring size did not drive computation. Depth-work slope was `1.7875 ± 0.0293`, reflecting the expanding sparse causal cone. All outputs were finite. The stored implementable frontier is empty because every implementable role missed the cohort's generic `0.95` accuracy capability gate; this is not missing data and supplies no Pareto-success claim.

The nine cells share one scoring seed and are not independent replications. Their screening SEs quantify cell heterogeneity only. Seed-to-seed uncertainty remains high, but the registered causal direction already failed on the realized valid quick.

# INTERPRETATION

An observation-grown partition can represent the nonlinear local dynamics better than a single affine operator. The exact frozen control matching ordinary ridge confirms that this improvement came from routing among fitted leaves rather than a different base predictor.

The hypothesized error/novelty selection rule was not the reason for the useful partition. Reassigning exactly the same proposal-gain multiset to different candidate splits produced better aggregate quality and much better D8 quality. Training one-step residual reduction therefore did not identify partitions that transferred reliably to OOD amplitudes and recursive rollout. The D8/D16 sign reversal is consistent with chance geometry under the anonymous encoding rather than a stable reusable operator decomposition.

The full-cost conjunction also failed independently: exhaustive child-ridge evaluation made aligned discovery about two orders of magnitude more expensive than ridge fit, and its routed query was slightly more expensive than ridge. Zero K slope and local one-leaf updates are useful structural properties, but they cannot rescue inferior causal selection or full-cost standing.

Changing the gain target, threshold, median rule, capacity, leaf model, shuffle or split schedule would tune the exact mechanism after observing its failure. Those variants are not authorized. This result does not falsify adaptive partitioning or mixture-of-experts generally; it falsifies the registered eight-leaf median error/novelty rule.

# CONFIDENCE

Confidence is `1.00` in completion, the runner seed, immutable hashes and pre/post integrity. Confidence is above `0.999` that the preregistered success conjunction failed because the shuffled source-identical role had better aggregate RMSE and all three D8 cells reversed the required margin. Confidence is `0.995` that partitioning rather than a different leaf model caused the improvement over frozen/ridge because those controls shared the same affine predictor and frozen matched ridge numerically. Confidence is `0.98` in discarding this exact rule without tuning or replication. Confidence is below `0.20` for extending the negative to learned conditional computation generally because this is one visible synthetic family and one seed.

# ALTERNATIVE EXPLANATIONS

The runner seed may favor the shuffled partition accidentally. That uncertainty cannot authorize replication after a valid negative quick whose exact outcome policy required aligned superiority on this seed.

Axis-aligned medians may partition the random observation encoding rather than intrinsic dynamics. A learned oblique or representation-invariant partition might behave differently, but it changes the tested causal factor and would be post-result model selection.

One-step training SSE may be a poor proxy for multi-step rollout. Optimizing rollout loss could change the sign, but it would use a different objective and higher fit cost rather than repair the registered rule.

# DECISION

`discard`. Mark `HYP-0053` falsified for the exact eight-leaf median proposal set, ridge `0.001`, minimum 48/24 samples, novelty span `0.20`, relative gain threshold `0.05`, seed-fixed gain reassignment and leaf-local NLMS `0.05`. Preserve the positive partition-versus-root diagnostic and zero-K-slope signature, but do not tune, replicate or promote.

# NEXT DISCRIMINATING EXPERIMENT

Cycle 212 may perform exactly one service-only identifiability and redundancy gate on the existing frozen DronePropA v6 corpus for a fundamentally different shared-structure hypothesis: learn a sparse actuator-to-state Jacobian support graph across anonymous training flights while fitting all numerical edge weights only from each held-out flight's charged adaptation prefix. Compare the proposed shared support with source-identical per-flight, shuffled-edge and frozen-dense support roles and the existing persistence, ridge/RLS, empirical-joint, contextual Chow--Liu and privileged support controls.

Reject before migration if public actuator excitation cannot distinguish edge support, if the graph is observationally equivalent to the falsified rank-12 operator subspace or ordinary sparsified ARX, or if a role requires condition, trajectory, path or semantic channel metadata. If and only if the support intervention is identifiable and nonredundant, activate a role-only `heldout_dronepropa_factor_recombination_v7` without changing files, splits, normalization, targets, metrics, costs or controls. Create no hypothesis, experiment plan, candidate, seed or score in cycle 212. Cycle 213 must then preregister and score one quick; another audit-only wake is forbidden. A negative ends the exact sparse-support rule without tuning, while one positive seed remains scout evidence only.
