# OBSERVATION

`EXP-20260830-0048` completed all 24 cells for each of six preregistered candidates under immutable plan `research/plans/EXP-20260830-0048.json` (SHA-256 `36a54cecea68be36d901def74b5901952ac4aa92ded035cfdc9a3cda2d15902f`) and runner-realized seed `76609398`. Evaluator SHA-256 `2d5f8d7538f465a25f0772b0366df8217f8e94c94d93ca5241c31e359b713b32` and candidate bundle SHA-256 `9c9584fd8bc1512fdaadf89b084bc25507072ae33361abf75d8c657fa3c1a159` matched before and after scoring. Candidate source audit, four registered semantic-baseline checks, state limits and integrity passed.

The shared width-32 recurrent learner achieved `0.015625` overall unseen-world accuracy and `0.0` minimum-family accuracy. Family means were probabilistic `0.041667`, program `0.020833`, predictive-state `0.0` and local-dynamics `0.0`. Its source-identical independently fitted ablation achieved `0.072917` overall and `0.0` minimum-family accuracy, so sharing changed overall accuracy by `-0.057292`, opposite the preregistered `+0.05` requirement, and changed the minimum-family metric by `0.0`. Shared near-equivalent accuracy was `0.015625`, continual retention `0.041667` and minimum-cell accuracy `0.0`.

The shared candidate used `6,062,145` fit operations (`88,209` pooled meta-fit operations), `383,637` mean acquisition operations, `2,023.375` mean query operations, `5,948,606` R16 workload operations, `16,064` resident state bytes and `2,301,720` peak fit bytes. The independent ablation used `6,462,060` fit operations, `994.875` mean query operations, `5,291,223` R16 workload operations and `17,280` resident state bytes.

The actual contextual Chow-Liu specialist suite achieved `1.0` overall and minimum-family accuracy with `70,375,758` R16 operations and `5,800` state bytes. Empirical joint achieved `0.921875` overall / `0.6875` minimum family with `2,215,064` R16 operations and `88,679` state bytes. Autoregressive achieved `0.817708` / `0.270833` with `980,245` R16 operations and `72,336` state bytes. The privileged oracle was exact and remains a lower bound only. Contextual Chow-Liu was the sole implementable member of the runner's declared-axis Pareto frontier.

There is one scoring seed. The 24 matrix cells share that seed and are not independent replications. Knowledge and depth slopes are descriptive screening estimates only; they do not affect the decisive capability and sharing failures.

# INTERPRETATION

The fixed width-32 hashed recurrent state plus linear template readout did not discover a transferable predictive representation. Pooling made the result worse than fitting the identical rule separately, which is evidence against this concrete shared-state/readout mechanism rather than evidence for positive transfer. The zero scores on two distinct families show representational or readout collapse, not a marginal threshold miss.

This does not falsify all recurrent predictive-state learning. The transition was deliberately minimal and its supervised linear template readout may be the limiting component. However, changing the transition, objective and decoder together would define a new mechanism rather than justify tuning EXP-0048. The large gap to correctly implemented specialist controls makes immediate replication or width search low-information.

# CONFIDENCE

- `0.99` that this exact implementation fails every preregistered capability and sharing criterion.
- `0.97` that replication of the unchanged implementation is not justified: the margins are large, both shared and independent variants collapse at minimum-family accuracy `0`, and the shared effect has the wrong sign.
- `0.72` that the main failure is an inadequate generic state/readout interface rather than recurrence alone; this is interpretation because no representation-versus-readout ablation was scored.
- Confidence in HYP-0019 should fall from `0.12` to `0.04` and its status should become `dormant`. One local visible seed cannot support a broader claim about shared representation learning.

# ALTERNATIVE EXPLANATIONS

- The linear template classifier may be unable to decode pointer-valued and continuous targets even if the recurrent state contains useful information.
- The sparse tied recurrence may forget long support structures before a query, so failure may reflect memory construction rather than lack of cross-family regularity.
- Anonymous structural shapes can implicitly cluster worlds; nevertheless the independent variant, which can exploit matching among anonymous supports, remained weak and sharing still hurt.
- The local visible evaluator may not represent real open-world transfer. This limits external claims but does not rescue failure on the preregistered cohort.

# DECISION

`discard` this implementation and make HYP-0019 `dormant`. Do not replicate, promote, tune width, search seeds or alter the frozen evaluator. Preserve the candidate, plan, result, logs and analysis as negative scientific history. The result is scientifically valid: no registered invalidation condition occurred.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, preregister a distinct family-blind **predictive dictionary composition** mechanism rather than another recurrent hyperparameter variant. The candidate should learn a bounded shared dictionary of anonymous input-output relation fragments from pooled training worlds, compose only retrieved fragments through one generic rule, and be compared with a source-identical independent dictionary, the same registered native specialist suites and the oracle on `cross_family_shared_representation_v3`. The causal question is whether explicit reusable relation fragments solve the long-support/readout bottleneck that defeated a fixed recurrent state. Before implementation, the plan must declare transfer accuracy and minimum-family accuracy as maximize axes and acquisition, fit, query, update, state, bytes and R16 workload as minimize axes. A one-seed quick may only discard the mechanism or authorize a three-seed adversarial screen.
