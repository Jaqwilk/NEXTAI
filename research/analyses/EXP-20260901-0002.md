# OBSERVATION

EXP-20260901-0002 completed technically under immutable child plan `research/plans/EXP-20260901-0002.json` (registered SHA-256 `ba6ad48e4be1d1dd4f6e6a135b18df60f254047a2ea9f925efc3a407fd986f7d`) with runner-random seed `1275381712`. All ten candidates completed all nine K-by-exposure cells, and pre/post evaluator digest remained `1351dd8fb92e56082855a17e61c593610e6715554412f5afbf810ab41b9ae135`.

At trial level the shared compiler had warm, near-equivalent, new-fact and retention accuracy `1.0` and false-reuse rate `0.0` in every cell. Its mean warm query work was independent of K and fell from `670.5` operations at exposure 1 to `666.0` at exposure 4 and `661.5` at exposure 16. Both scope ablations remained exact but had no reuse and constant warm work `660.5`. The exact interpreter was also exact at `660.5` warm operations, with fit `0`, update `1`, state `64` bytes and mean R16 workload `51,814`, versus shared fit `166,494`, update `1,159`, state `2,472` bytes and mean R16 workload `125,794.33`.

The result is scientifically invalid. `continual_new_fact_accuracy` is present in every trial (`1.0` for every non-random candidate and `0.0` for random), but the shared `aggregate_trials` function omitted it from every candidate summary. Because this was a preregistered primary metric, every row failed the complete-axis filter and the runner wrote an empty Pareto frontier. The immutable result and logs remain diagnostic history; they are excluded from evidence, confidence, falsification, replication and promotion.

# INTERPRETATION

Diagnostic numbers suggest that the learned probe prefix creates the requested declining warm-cost curve, but the gain is only nine operations and does not overcome input scanning, fit or update cost. The exact interpreter is already cheaper at matched exact quality. This would be strong negative evidence against the exact compiler if produced by a valid complete aggregation, but the missing required summary metric prevents an official scientific decision.

# CONFIDENCE

Confidence that the aggregation defect invalidates this result is greater than `0.99`: the immutable trial rows contain the metric, every summary omits it, and the declared Pareto axes include it. Confidence in any scientific claim about HYP-0030 remains unchanged at `0.12`; one invalid seed estimates neither valid effect nor cross-seed uncertainty.

# ALTERNATIVE EXPLANATIONS

The behavioral probes may be useful only when recursive execution is much more expensive than the four-step operator used here. Conversely, the observed cost decline may be a finite-domain cache artifact rather than learned transferable compilation. Neither explanation can be adjudicated from a result whose required summary contract failed.

# DECISION

`inconclusive` due to `invalid_required_metric_aggregation`. Preserve EXP-20260901-0002 unchanged and exclude it from all evidence. Do not tune the compiler, probe count or thresholds.

# NEXT DISCRIMINATING EXPERIMENT

The next wake must be one protected service-only migration to `heldout_mechanism_recombination_v5`, with no hypothesis, plan, seed or scoring. Add `continual_new_fact_accuracy` to the common aggregate summary, add exact final-schema regression tests proving every v4 primary metric is non-null and the frontier is non-empty for complete rows, preserve v4 and EXP-0002 append-only, and activate v5 only after full pytest, preflight, integrity and doctor PASS. A later wake may preregister an unchanged one-seed rerun of this exact compiler; a valid repeat of the diagnostic dominance ends HYP-0030 without tuning.
