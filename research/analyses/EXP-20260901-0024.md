# OBSERVATION

EXP-20260901-0024 completed as a technically and scientifically valid one-seed quick on `heldout_wt_changepoints_prequential_v3` with runner-random seed `1566530300`. All thirteen candidates completed all eighteen whole-file K/H trial rows. The evaluator digest remained `1715c47db63bcc8fa4d6e591063c83ff5495babd89a0be6b6b52f201cd861f28`; ten semantic baseline gates, the transitive source audit, preflight, and both 624-file integrity checks passed.

The future-supervised role reached aggregate NRMSE `1.258247`, versus `1.457285` for frozen DCT and `1.362498` for history-only PCA. Its gains were therefore `0.199038` and `0.104251`; the second missed the frozen meaningful-effect threshold `0.132527`. At H32 its NRMSE `1.100887` improved on frozen DCT by `0.120554` and history PCA by `0.044544`, so both preregistered H32 margins failed. At H96 the corresponding value `1.604893` improved on the two ablations by `0.375358` and `0.240611`, showing a real future-supervision signal within the shared recursive implementation.

The strongest complete classical quality control was nevertheless much better. The exact control-level residual bank had aggregate NRMSE `1.007129`, H32 `1.010283`, H96 `1.042100`, and worst-file NRMSE `1.134350`, versus predictive-state `1.258247`, `1.100887`, `1.604893`, and `2.682275`. The pooled-mean control had the best worst-transition NRMSE `1.381764`, versus predictive-state `3.859068`. Thus the predictive role lost the aggregate baseline gate by `0.251118` in the wrong direction and regressed both worst-case measures.

The computational sub-gates did pass: stable rollout was `1.0`, query-work K slope was exactly `0.0`, state was `80,768` bytes under the 16 MiB cap, and the focused test proved slot-local post-artifact updates. These properties did not compensate for cost. Predictive-state fit/query/update/R16 work were respectively `1,569,520`, `14,933.33`, `9,600`, and `12,845,325` operations, versus control-level bank `17,280`, `1,920`, `0`, and `7,886,525`; peak state was `1,066,112` versus `400,982` bytes. The implementable Pareto frontier contained only persistence, pooled mean, and control-level bank; none of the three causal roles entered it.

Uncertainty is seed-limited: one runner seed provides no seed variance estimate, and the two locally visible test files are screening rather than blind evidence. The decisive negative does not depend on a small stochastic difference: multiple frozen capability margins have the wrong sign, the worst-file gap is `1.547925`, and a much simpler complete control is simultaneously better and cheaper.

# INTERPRETATION

Future-supervised temporal directions carry useful information that history reconstruction and a fixed DCT basis omit, especially when the shared recursion is extended from fit horizon 32 to H96. This isolates a representation-level signal rather than an implementation mismatch. It is not a successful predictive-state engine: the learned four-dimensional temporal state discards or destabilizes information already captured by an exact public-control residual bank, and recursive rollout amplifies the deficit on held-out files.

The failure is fundamental for the exact preregistered conjunction. Increasing rank, changing windows, ridge, clipping, coverage weighting, recurrence, or the local update after seeing this output would be tuning the same rule. The classical spectral PSR and CSSR controls also outperform or approach the simpler banks without establishing a learned advantage, so the result does not justify another WT predictive-state variant.

# CONFIDENCE

Confidence that this exact future-supervised rank-four factorization, recursive block rollout, and slot-local NLMS rule fails its quick claim is `0.995`. Confidence that all predictive-state approaches fail on other tasks remains low: this is one seed and one real local-visible cohort. The within-family H96 gain is credible as a component observation, but it cannot reverse the failed aggregate, H32, worst-case, Pareto, and full-cost gates.

# ALTERNATIVE EXPLANATIONS

Rank four may be too small, per-channel linear readouts may miss cross-channel dynamics, and recursively adding residuals may accumulate offsets. Each is plausible, but each changes a frozen causal factor and therefore defines a new hypothesis rather than a repair. The control-level bank may benefit from the finite set of public actuator levels; that is exactly the intended strong null, not leakage, because the control is candidate-visible, mechanically discovered from training only, and fully charged. A larger blind dataset could alter absolute NRMSE, but the current valid experiment still falsifies the declared rule on its registered cohort.

# DECISION

Discard EXP-20260901-0024's exact rule and mark HYP-0040 falsified at confidence `0.01`. Do not replicate, promote, or tune it. Preserve the isolated observation that future-supervised state improved source-identical H96 rollout while maintaining zero K slope and bounded local updates. Keep broader HYP-0008 dormant with slightly lower confidence; the useful sub-signal did not beat the classical action-conditioned controls or full-system boundary.

# NEXT DISCRIMINATING EXPERIMENT

Pivot away from predictive-state, residual, spectral, routing, cache, and local-cellular variants. In the next wake perform exactly one minimal protected service migration of the already frozen real `heldout_parallel_masked_infilling_v4` corpus to a v5 role-only cohort for a sparse imperfect learned energy factor graph: shared learned parallel relaxation, source-identical one-sweep relaxation, and source-identical frozen-factor relaxation. Preserve every file hash, split, mask, round, metric, cost, seed rule, and the eight existing controls; add no candidate, hypothesis, plan, seed, or score in that service wake. Freeze semantic fixtures requiring a nontrivial multi-factor completion, monotone energy under parallel updates, permutation equivariance, no target access, and explicit factor/iteration cost. The immediately following wake must preregister one HYP-0007 revival quick before implementation and score one runner-random seed. Success must beat fixed Markov belief propagation, exact bidirectional Markov inference, PPM, CTW, dense autoregression, and both source-identical ablations on conditional loss and exact-span accuracy, retain bounded critical depth and state over all registered spans, and remain implementably Pareto non-dominated; a valid negative ends this sparse-energy route without tuning.
