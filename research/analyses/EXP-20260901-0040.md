# OBSERVATION

EXP-20260901-0040 used runner-realized seed `1925487674` on frozen
`heldout_parallel_masked_infilling_v10`. The immutable plan SHA-256 is
`92e9380eba2992c2e872e904987c7ef84d922dedec2727c4bfdd88f0000dce26`
and the evaluator SHA-256 is
`655e8713fb8a7264f11dc99b80f34d644e71d930b812feda504c2787291c8ad0`.
Integrity passed before and after scoring with 711 protected files. Eleven of
twelve candidates completed; the result status is `complete_with_failures`.

The unchanged `learned_pushdown_masked_byte` again achieved accuracy and
exact-span accuracy `1.0` with bits per byte `0.0` in every K=`8/32`,
D=`3/4/5` cell. The source-identical depth-two role had exact-span accuracy
`0.0` in every cell and byte accuracy `0.6667/0.5/0.4` by depth; the frozen
role had accuracy and exact-span accuracy `0.0` throughout. The main role's
exact-span margin over each ablation was therefore `1.0` at every depth.

All eight implementable controls completed. PPM was again strongest on exact
span at `0.125/0.0/0.0` for D=`3/4/5`; every other implementable control had
exact-span accuracy `0.0`. The main role exceeded the strongest complete
implementable control by `0.875/1.0/1.0`, had lower bits at every depth and
appears in the recorded implementable Pareto frontier.

The main role's six-cell mean fit work was `3,760,496` operations, mean query
work `774.87`, mean critical path `56.33`, state `616` bytes and mean R16
workload `4,777,415.55`. Re-Pair used mean fit `240,489.5`, query `1,101.30`,
critical path `9.83`, state `4,122.5` and R16 `1,440,726.39`; PPM used mean fit
`48,904`, query `5,121.97`, critical path `7.33`, state `104,184` and R16
`3,291,327.55`. Exact completion therefore remains a quality/cost trade-off,
not universal cost dominance.

`privileged_conditional_masked_byte_v2` crashed before emitting a trial with
`TypeError: privileged support control requires privileged query`. The v10
candidate correctly accepts only `PrivilegedMaskedQuery`, but the inherited
v1 evaluator routes private targets only when the candidate name is in the
historical exact set `{"oracle_conditional_masked_byte"}`. The new versioned
name was absent, so `_run_case` passed a public `MaskedQuery`. The semantic
fixture called the control directly with the correct private type and therefore
did not exercise this evaluator-to-control routing boundary.

# INTERPRETATION

The second runner-random seed reproduces the large within-cohort stack-capacity
signature across a different byte permutation and selected cases: full stack
is exact, the depth-two source-identical intervention is never exact, and every
completed finite-context or grammar control fails whole-chain completion.
This is useful diagnostic consistency, but it is not a valid promotion-grade
replication because v9/v10 share the same local corpus and a mandatory
privileged control is still incomplete.

The crash is a second, distinct infrastructure integration defect. V10 fixed
target alignment inside the control but failed to register its new ID at the
evaluator's privileged-query dispatch boundary. A crash falsifies that
integration, not the observation-induced pushdown principle. The
preregistered all-controls-complete gate nevertheless fails, so the positive
outcome policy cannot be invoked.

The mechanism remains a specialized learned deterministic pushdown parser over
seven anonymous symbols with expensive exhaustive fit and sequential trace
processing. Nothing here supports a broad language, intelligence or scaling-law
claim, and the incomplete control prevents elevating even the narrow result.

# CONFIDENCE

Confidence that the implemented full stack causes the observed v9/v10
within-cohort exact-completion advantage is `0.90`: two runner-random seeds,
six cells per seed and both source-identical interventions agree. Confidence
that the principle transfers beyond this closed delimiter projection remains
`0.20`; there is no independent corpus, malformed/noisy grammar or adversarial
alphabet test. Because EXP-0040 fails the preregistered mandatory-control gate,
it is not used to raise formal hypothesis confidence. HYP-0050 remains
`uncertain` at `0.35`.

# ALTERNATIVE EXPLANATIONS

- The result may be fully explained by a narrow balanced-delimiter parser
  rather than a reusable learned operator.
- The seven-symbol closed world and point-mass target make grammar discovery
  unusually clean; irrelevant symbols or malformed traces may break it.
- The apparent Pareto position is quality-driven while Re-Pair and PPM retain
  substantially lower fit or critical-path costs.
- Reusing the same frozen corpus across the two runner seeds limits the
  independence of the repeated observation.
- After privileged routing is corrected, another untested supervisor boundary
  could still fail; a real `_run_case` smoke is required before another score.

# DECISION

Decision: **inconclusive; keep the exact rule frozen and do not replicate,
promote, tune or discard it**. The main and all implementable comparisons pass
their registered quality and Pareto gates, but the mandatory privileged support
control is incomplete. EXP-0040 remains immutable diagnostic history with its
crash. No second experiment or protected repair is performed in this wake.

# NEXT DISCRIMINATING EXPERIMENT

The next wake must be exactly one minimal protected service cycle creating
`heldout_parallel_masked_infilling_v11`. Preserve every v10 scientific field
and add only `privileged_conditional_masked_byte_v2` to the evaluator's explicit
privileged-query routing set. Add a real `_run_case`/real-trace regression that
proves the corrected control receives `PrivilegedMaskedQuery` and completes a
noncontiguous closure chain through the same evaluator path used by scoring.
Run all thirteen semantic nodes, the real-suite smoke, full pytest, preflight,
integrity and doctor; create no hypothesis, plan, seed or score. Only a later
wake may preregister the unchanged quick again. If another infrastructure-only
failure occurs after that end-to-end gate, stop repeated scoring and audit the
entire privileged-control path before any further evidence collection.
