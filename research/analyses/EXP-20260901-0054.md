# OBSERVATION

`EXP-20260901-0054` completed all seven registered roles and all nine K/depth cells at runner-random seed `1122686782`. The immutable plan is `research/plans/EXP-20260901-0054.json`, plan SHA-256 `0669cca273e3e1a72205bb3440fb99417d120b41e6460aaee0e46dd13d71ff74`; result SHA-256 is `13663b02aba10e61c3b2de3dd2477d64d7c5068edc6ff57fbdf7bc8b1ae8e2b2`. Every role completed without timeout, crash, budget failure or missing mandatory metric. Pre- and post-score integrity passed over 758 protected files with evaluator digest `ed0c9083cf224bafb6bdd75d2a5ce496f7f084b264efc239517b2b9a43189795` and candidate-bundle digest `3253a8e4b423d5e501b69d966c267abd949d43da0271ef3415c6fc6284d73f70`.

The learned discrete address obtained aggregate accuracy `0.409722`, versus `0.972222` for its source-identical dense scan, `0.659722` for the frozen encoder index, `0.479167` for the shuffled-representation index and `1.0` for raw nearest-neighbour scan. Its paired cell accuracy differences were `-0.5625 +/- 0.1538` screening SE against dense, `-0.2500 +/- 0.1197` against frozen, `-0.06944 +/- 0.04351` against shuffled and `-0.59028 +/- 0.16175` against raw nearest neighbour. Learned accuracy was `1.0` at all K=32 cells, but fell to `0.625/0/0` at K=320 and D=`1/4/8`, then `0/0/0.0625` at K=3200. Its minimum cell accuracy was therefore zero.

The learned role did preserve local insertion: continual-new-fact accuracy was `1.0`, update work `5,737` operations and neither encoder nor thresholds were refit. Peak state was `56,887,009` bytes, below the preregistered 64 MiB cap. The bounded verifier/fallback fixtures passed before scoring; observed comparisons accumulated across traversal depth as expected and did not alter the per-step cap.

The preregistered three-point K slope of complete `workload_ops_r4096`, computed from K=`32/320/3200` means, was `0.71491 +/- 0.09854` regression SE, failing the `<0.30` gate. Corresponding mean workloads were `446,965,556`, `1,565,003,348` and `12,025,206,164` operations. Although aggregate R4096 work was only `0.225x` dense scan and `0.191x` raw scan, this was not a matched-quality comparison: accuracy was lower by `0.5625` and `0.5903`. No preregistered reuse horizon achieved break-even at quality within `0.02`, so prospective break-even is `none`.

The learned role paid `11,351,683,200` fit operations, versus `135,196,800` for frozen and `1,228,800` for raw nearest neighbour. It is absent from both Pareto frontiers; the implementable frontier contains source-identical dense scan and raw nearest-neighbour scan. Thus the quality-margin, both source-identical ablation, R4096 slope, matched-quality break-even and Pareto gates failed independently. The one-seed cells are not replications; reported SEs quantify cell or three-scale regression heterogeneity, not seed uncertainty.

# INTERPRETATION

The exact learned flat-address rule did not discover stable entity-preserving bits as confusable knowledge grew. Its local-update and lower-search-work properties are real engineering signatures, but they came from bounded probes and fallback while representation collisions or false routing destroyed retrieval quality. The fact that the untrained frozen encoder was substantially more accurate, and the shuffled trained representation was also numerically better, is direct evidence against the registered pair-consistency loss as the causal source of useful addressing.

Dense use of the identical learned encoder retained high accuracy, so the encoder preserved enough continuous information for exhaustive comparison; the failure arose when its coordinates were thresholded into the registered 16-bit flat key and searched with the frozen bounded rule. That distinction does not authorize post-result changes to key length, loss, threshold, probes, bucket size, verifier or fallback. Those would be tuning the failed exact mechanism.

This valid quick supports HYP-0012's skeptical full-system account in one controlled synthetic family: learned routing achieved neither matched quality nor sub-0.30 total-cost scaling, while simple raw nearest-neighbour remained exact and Pareto-relevant. It does not establish a universal impossibility result for learned routing or memory decoupling.

# CONFIDENCE

Confidence is `1.00` in completion, immutable hashes, realized seed and pre/post integrity. Confidence is above `0.999` that the preregistered conjunction failed because the quality deficit is far beyond `0.02`, both causal ablation comparisons have the wrong sign, the measured slope is far above `0.30`, no matched-quality break-even exists and the learned role is off the implementable frontier. Confidence is `0.99` in discarding this exact flat-address rule without tuning or replication. Confidence is below `0.25` for generalizing the negative beyond this visible synthetic cohort and this one learned representation family.

# ALTERNATIVE EXPLANATIONS

The single runner seed may be unusually unfavorable to learned threshold bits. Replication cannot rescue a quick whose registered causal comparisons and quality gate already fail decisively on the realized seed.

The pair-consistency loss may preserve nuisance variation or fail to create balanced collision-resistant codes. Altering its coefficients, architecture or supervision would define a new mechanism after seeing the result and is forbidden for this direction.

A hierarchical learned routing process could avoid the brittleness of one flat threshold code while keeping logarithmic traversal. That is a fundamentally different causal mechanism, not evidence that this one should be repaired; it requires a fresh preregistration and a redundancy check against classical metric trees before implementation.

# DECISION

`discard`. Close the exact 24-32-16 pair-trained, 16-bit median-threshold, four-probe, eight-entry-bucket and 32-record-fallback learned-address rule. Do not create a v2, tune any registered constant, replicate it or combine it with another mechanism. Preserve its successful local insertion and sub-dense R4096 workload only as diagnostics, not evidence of matched-capability efficiency. Raise HYP-0012 confidence modestly from `0.86` to `0.87`; the evidence is valid but narrow and single-seed.

# NEXT DISCRIMINATING EXPERIMENT

Cycle 217 should use the unchanged `latent_entity_binding_retrieval_v2` cohort and preregister one quick of a fundamentally different **learned balanced hierarchical router**, not another flat hash. A shared self-supervised encoder and recursively learned binary partition must select a bounded beam down a frozen balanced tree; insertion may update only the traversed leaf/path statistics, never refit the encoder or rebuild the full tree. Before candidate implementation, freeze tree depth as `ceil(log2 K)`, beam width, split objective, stopping rule, verifier, fallback, all optimizer constants and R1/R4/R16/R256/R4096 accounting. Include source-identical dense traversal, label-shuffled routing, frozen-routing, classical metric-tree/raw-nearest-neighbour, local dense GRU and privileged-key controls. Reject the proposal before scoring if an exact redundancy audit shows it is only the failed 16-bit threshold code under another name.

Success must require accuracy within the existing `0.02` dense margin, strict improvement over frozen and shuffled routing in every K scale, continual-new-fact accuracy `1.0`, no global refit, three-point R4096 K slope `<0.30`, a prospectively defined matched-quality break-even and implementable Pareto non-dominance. One positive seed may only authorize unchanged three-seed replication; one negative closes this exact hierarchical rule without tuning.
