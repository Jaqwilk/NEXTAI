# OBSERVATION

`EXP-20260901-0015` is an immutable `complete_with_failures` quick under
`research/plans/EXP-20260901-0015.json` (SHA-256
`25b166e767022f11ac51a51ab438d6d701e2ebe729b0e90af201d7d41f97674e`).
The audited runner realized exactly one seed, `1803589890`, after preflight,
semantic-baseline, candidate-source and integrity gates. Pre/post integrity
passed over 587 files with evaluator digest
`af6f9c161d9993a6349a23db706dd480e0f1ee4012f809deb9ea115102632567`.
The immutable result SHA-256 is
`6e9ef2a07d04a9bef679437a6e8d7c16f3ed050e5ec95c5d8f4fe5483e09fe53`.

All three source-identical lifting roles crashed with the same `ValueError`:
`WT lifting tensors must have length 32`. Each reached the first H96 reveal,
where the evaluator correctly supplied `episode.target[:96]`. The candidate's
`_matrix(..., pad=True)` path padded targets shorter than 32 but rejected a
target longer than 32 instead of taking the preregistered first 32 update
samples. The traceback is preserved independently in all three candidate logs.
No lifting role completed a cell, so there is no NRMSE, causal contrast, cost
frontier position or scaling observation for the tested mechanism.

All eight frozen WT controls completed. Their aggregate NRMSE ranged from
`1.007129` for the control-level bank to `1.498773` for ridge FIR. The formal
implementable frontier contains persistence, pooled mean and control-level
bank only because the three tested roles have no complete metrics. These
control values verify harness execution but cannot be used as evidence for or
against the lifting hypothesis without a complete tested role.

# INTERPRETATION

This is a narrow implementation-interface failure: H16 was mechanically
padded to the frozen update depth, H32 matched it, and H96 was not truncated to
that same depth. The failure is shared by all causal roles because they use one
source, which supports source identity but leaves both causal interventions
untested. It does not discriminate multiresolution learning from single-scale
or frozen lifting and therefore does not falsify HYP-0036.

The exact correction is predetermined by the immutable fit/update horizon 32:
use the first `min(len(target), 32)` revealed samples and repeat the last only
when fewer than 32 exist. No basis size, ridge, update scale, clip, recursion,
cost formula, data or success threshold may change.

# CONFIDENCE

Confidence is above `0.99` in the diagnosed crash cause because all three
independent subprocess tracebacks identify the same input-length branch and
the H16/H32/H96 evaluator contract is frozen. Confidence in HYP-0036 changes
only from `0.10` to `0.08`: the crash supplies weak implementation-risk
evidence but no capability evidence. One realized seed was consumed and must
not be reused or searched.

# ALTERNATIVE EXPLANATIONS

Another defect could appear after the H96 boundary is corrected because no
lifting role completed a full scored cell. The focused tests covered H96 query
recursion but only an H32 reveal, so their coverage gap—not evaluator behavior
or data corruption—allowed this failure through. It also remains possible that
the corrected learner will be worse than the simple controls; EXP-0015 contains
no observation that resolves that scientific alternative.

# DECISION

`inconclusive`. Preserve EXP-0015 as a valid implementation crash and keep
HYP-0036 at `testing`; do not promote, falsify, tune or interpret the baseline-
only frontier as a causal result. Do not rerun in this wake.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, preregister one child quick before modifying candidate code.
Freeze exactly one interface correction: `_matrix(target, pad=True)` first
truncates to 32 samples, then repeats the last sample only when shorter than 32.
Add one source-identical test that sends H16, H32 and H96 reveals and proves
identical first-32 update semantics, local mutation and unchanged operation/
byte accounting. Change no algorithmic constant or protected evaluator rule.

After the test changes the protected digest, append-only invalidate the pre-seed
parent, freeze the bundle, create an identical child and score exactly one new
runner-random seed over the original K/H matrix and all eleven candidates. The
original success and kill gates remain unchanged. A second implementation
failure ends this exact implementation; a complete negative ends the exact
lifting rule without tuning; a conjunctive positive only authorizes unchanged
three-seed replication.
