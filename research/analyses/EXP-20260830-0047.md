# OBSERVATION

`EXP-20260830-0047` ran under immutable plan `research/plans/EXP-20260830-0047.json` (SHA-256 `ea72e380759d607d4f0067a90dbd1ad668a5923bc5e08172d849d502f511a70b`) on runner-realized seed `1816371237`. All ten processes completed all 18 cells. Evaluator SHA-256 `6c80b34fd455c688babddb696a25ad89402777b1f1c193792ad7d2057367a7d7` and candidate bundle SHA-256 `afc25144f6f349e92ef7a769b2b7652a8aa16398bf97b99c7a225eb2cfa1dc45` matched before and after scoring. The protected manifest contained 405 files; the runner source audit, 206 tests, integrity verification and doctor passed.

The numerical output is quarantined because a post-score semantic audit found a preregistered invalidation condition. `left_to_right_ppm_masked_byte` is a first-order transition predictor with sequential self-fill, not a faithful PPM implementation. `context_tree_weighting_masked_byte` is a fixed first-order transition/unigram mixture, not context-tree weighting. Therefore two mandatory controls named in the immutable plan were absent in substance even though their module names passed the mechanical source audit. The plan states that a missing control invalidates the cohort and forbids interpretation.

For auditability only, the quarantined numbers were: iterative learner mean `5.81348` bits/byte, worst span `7.83974`, accuracy `0.124946`, exact-span accuracy `0`, critical path `50`, R16 work `82,758,719` and state `526,848` bytes. Its source-identical forced one-pass ablation was `5.32897` bits/byte, worst span `6.15060`, accuracy `0.127441`, exact-span accuracy `0`, critical path `9`, R16 work `39,347,585` and the same state. At D=4 and D=6, iterative loss was respectively `5.97` and `6.14` versus `5.33` for one-pass. Empirical unigram produced `5.28245` mean and `5.94537` worst-span bits/byte. The privileged oracle was exact. These values are retained as implementation diagnostics, not evidence for or against HYP-0017.

# INTERPRETATION

No architectural interpretation is permitted. The observed iterative degradation is compatible with erroneous self-conditioning, but the registered experiment required comparison against real PPM and CTW controls. Accepting a negative conclusion after discovering that those controls were only labels would violate the same preregistration discipline intended to prevent favorable post-result reasoning.

The mechanical audit was insufficient: it checked imports, target/path access and frozen hashes, but not whether a module's algorithm matched the scientific role claimed by its name. This is an infrastructure/process defect, not a reason to rewrite the completed result or plan.

# CONFIDENCE

- `1.00` that the result files and immutable plan must be preserved.
- `0.99` that the cohort violates its registered mandatory-control requirement, based directly on the frozen candidate source.
- `0.99` that no promotion, rejection or confidence update for HYP-0017 is justified from this cohort.
- HYP-0017 remains `proposed` at confidence `0.16`; EXP-0047 is not added as supporting or opposing evidence.
- Numerical uncertainty is additionally large because there is one runner-random seed; the 18 cells are not independent replications.

# ALTERNATIVE EXPLANATIONS

- The valid one-pass ablation alone numerically beats iterative refinement, but the immutable outcome policy makes a missing mandatory control cohort-level invalidation rather than permission to use a convenient subset.
- A faithful PPM or CTW implementation might strengthen the negative result, weaken it or expose an evaluator accounting error. None may be inferred from the mislabeled approximations.
- The exact first-order bidirectional and parallel-power controls appear internally consistent, but their presence cannot repair the absent higher-order baselines.
- The evaluator, corpus, seed isolation and target boundary passed their audits. The invalidation is specifically semantic role fidelity, not leakage or hash failure.

# DECISION

`inconclusive` / scientifically invalid cohort. Preserve the plan, logs, result, ledger rows, manifest archive, candidates and this correction. Do not promote, discard, replicate or tune HYP-0017 from EXP-0047. The generated runner Pareto front is diagnostic only and must not be cited as a scientific frontier.

The CLI correctly refuses to mark a plan invalid after a result exists, so the correction is recorded append-only in this analysis and `research/events.jsonl`; the completed result is not edited. A later report must honor that correction registry before treating EXP-0047 as eligible evidence.

# NEXT DISCRIMINATING EXPERIMENT

The next wake is service-only, with no scoring: add an append-only post-result scientific-validity registry consumed by doctor/report; add semantic conformance tests using known higher-order sequences that separate real PPM and CTW from first-order Markov mixtures; replace only the candidate implementations under a new candidate-bundle digest; and verify that the evaluator digest remains unchanged. Because the user has redirected the scientific objective to shared transfer across heterogeneous worlds, do not automatically rerun masked infilling after repair. Instead, use the repaired validity machinery to specify the next cross-family screen: one unchanged, family-blind recurrent predictive-state learner trained jointly on the four existing probabilistic, predictive-state, local-dynamics and program-composition families, evaluated on disjoint runner-random worlds against source-identical independent fitting and the strongest registered contextual Chow-Liu, empirical-joint, autoregressive and oracle suites with full R16 accounting. Any protected cross-family protocol change requires its own service wake before preregistration.
