# OBSERVATION

EXP-20260901-0042 completed on frozen
`heldout_parallel_masked_infilling_v12` with runner-realized seeds
`199865405`, `1281302886` and `735132540`. The immutable semantic plan
SHA-256 is `d5d4afe7e290742f80a0b22f83705f0532e90f5d02929313cc5882aa8ae3fbcb`,
the plan-file SHA-256 is
`deb425574905f00be7f76d8971f744bb13ad449ac6a36072b7854156c53a36aa`,
and the result SHA-256 is
`1e64db032ccb1ec8ac1c867c7837d1b6dace0bba3d7d58041b2e8e04828f121b`.
Integrity before and after scoring verified 715 files with evaluator SHA-256
`bf0e4c57b3830177c296eb80eaaa4d9cc12f0935a0ddc8a76563ace3c59187f5`.
All twelve roles and all 216 raw trials completed.

The unchanged `learned_pushdown_masked_byte` achieved accuracy and exact-span
accuracy `1.0` and bits per byte `0.0` in all 18 seed/K/depth cells. Its
per-seed means were identically `1.0/1.0/0.0`, and its recorded seed accuracy
CV was `0.0`. Across 144 scored closure chains, all were exact. A naive
independent-binomial Wilson 95% lower bound is `0.974`; shared corpus grammar,
implementation and within-seed fitting make that an optimistic descriptive
bound rather than a transfer-confidence interval.

Both source-identical interventions failed every whole span: the depth-two
role had exact-span accuracy `0.0`, mean byte accuracy `0.5222` and bits
`3.8222`; the frozen-transition role had exact `0.0`, byte accuracy `0.0317`
and bits `15.9629`. The strongest implementable whole-span control was PPM.
It averaged exact accuracy `0.0347`, reached at most `0.25` in an individual
cell and was zero in most cells. The main's smallest per-cell exact-span margin
over any implementable classical control was therefore `0.75`; its margin over
both source-identical roles was `1.0` everywhere, with lower bits in every
comparison. The privileged support control was exact in all 18 cells and is
excluded from implementable Pareto evidence.

The main remains on the recorded implementable Pareto frontier. Its aggregate
fully charged quantities were fit `6,043,321`, mean query `773.56`, critical
path `75`, resident state `616` bytes, peak state `1,255,924` bytes and R16
workload `5,946,624`. Re-Pair used R16 `2,983,309`, PPM `4,511,280`, CTW
`4,450,171`, and parallel Markov BP `173,352,689`. Thus exact pushdown quality
cost about `1.99x` Re-Pair, `1.32x` PPM and `1.34x` CTW R16 work, while using
about `0.034x` the parallel-BP work. It does not dominate the cheaper inexact
controls on cost.

# INTERPRETATION

The preregistered result replicated on a path/hash-disjoint source corpus and
three independent runner byte permutations. It strongly supports the narrow
causal account that learned full stack capacity, rather than the identical
depth-two state or frozen transition map, produces exact closure-chain
completion beyond training depth. Finite-context probabilistic inference,
parallel Markov messages and Re-Pair did not reproduce whole-chain completion,
and every registered screen kill gate passed.

This remains a clean seven-symbol visibly nested language. It demonstrates a
learned sufficient state update with systematic depth extrapolation, but not a
general representation learner, malformed-input robustness, transfer across
task families, or superior LLM-scale economics. Pareto non-dominance reflects
a quality/cost trade rather than cost dominance. The cycle-200 primary-source
review shows that visibly pushdown execution, stack induction and deeper-Dyck
testing are established prior art. The result's value is its anonymous passive
role induction and audited comparison, not a new automaton class; it therefore
cannot justify `promising` or promotion.

# CONFIDENCE

Formal HYP-0050 confidence rises from `0.60` to `0.80` while status remains
`uncertain`. Confidence in the registered seven-symbol depth-transfer claim is
about `0.97`: one complete quick plus a complete disjoint-corpus three-seed
screen, exact causal ablations, all controls and zero main seed dispersion
agree. Confidence that the mechanism is robust outside clean balanced traces
is about `0.35`; confidence in reusable cross-family operators or better
LLM-scale economics remains below `0.15`.

# ALTERNATIVE EXPLANATIONS

- Exhaustive search may identify a particularly clean visibly-pushdown grammar
  rather than learn a broadly reusable operation.
- V12 is path/hash-disjoint but locally visible and composed of Python source;
  it is not a blind or externally independent replication.
- Training and test share one seven-symbol grammar and only change files,
  nesting depth and byte permutation.
- Exact-span quality keeps the main non-dominated, while Re-Pair, PPM and CTW
  remain materially cheaper at their lower capability.
- The frozen role's nonzero byte accuracy varies across seeds, showing that
  accidental first-occurrence mappings exist even though no full span is
  solved; cleaner alternative mappings might narrow partial-byte margins.

# DECISION

Decision: **keep**, without promotion, tuning or combination. EXP-0042 is
valid replicated narrow evidence and satisfies every preregistered screen gate.
HYP-0050 remains `uncertain` after primary-source review and pending one final
adversarial test that can distinguish robust learned stack operation from exact
identification of clean balanced delimiters.

# NEXT DISCRIMINATING EXPERIMENT

The cadence review was completed inside this scored cycle and recorded in
`research/reviews/GEN-1-LEARNED-PUSHDOWN-PRIOR-ART-CYCLE-200-2026-09-01.md`.
It identifies a close visibly-pushdown/stack-learning alias but leaves one
robustness question that the clean v12 contract cannot answer.

The next wake is exactly one minimal service-only migration to
`heldout_parallel_masked_infilling_v13`. It must freeze mechanically generated,
recoverable stack corruptions within the same anonymous seven-symbol alphabet,
preserve the v12 role implementations and full-cost boundary, and add only the
fixtures needed to prove that the target cannot be recovered by copying the
visible clean top-of-stack path. It creates no hypothesis, plan, seed or score.
The following wake may preregister one three-seed adversarial screen. Success
requires exact recovery in every seed and depth, the same causal margins, all
mandatory controls complete and implementable Pareto non-dominance. A valid
negative makes this exact family dormant without tuning; a positive remains
screening evidence and cannot promote without an independent non-synthetic
corpus.
