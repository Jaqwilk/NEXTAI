# OBSERVATION

`EXP-20260830-0055` was preregistered as the scientific-field-identical child of pre-implementation parent `EXP-20260830-0054`. Parent 0054 was append-only invalidated before any seed because the mandatory semantic registry changed the evaluator-role digest. The corrected manifest passed integrity over 438 files with evaluator SHA-256 `b0abb18ddf4ffc5619f2ff8a1c60b63ac41dd06c04ae4c437bb269a0d72de8c1` and candidate-bundle SHA-256 `655ae67b44478571beef50e9ba4a68645a0b7f9e1c15143c7b98b75a2aa1691d`.

Exactly one audited runner invocation was made for EXP-0055. The runner created all eight candidate log files and executed the candidate suite, then failed during final `experiment_result` validation because candidate summaries contained the preregistered `minimum_combination_accuracy` field but `schemas/experiment_result.schema.json` did not permit it. No immutable result or ledger row was written. The runner-random seed was realized in memory but was not durably recoverable. The plan is append-only invalidated and must never be rerun. No candidate score from this attempt is scientific evidence.

# INTERPRETATION

This is a harness/schema and failure-durability defect, not evidence for or against HYP-0021. The pre-seed gates worked, but the post-execution boundary did not preserve the seed or a failure artifact when result validation failed. A protected correction must version the cohort and test both the new summary field and durable post-seed failure recording before another plan.

# CONFIDENCE

`1.00` that no valid EXP-0055 result exists; `1.00` that its candidate outputs must be excluded from evidence; `0.99` that adding the missing schema field is necessary but insufficient without a regression test for durable runner failure. HYP-0021 confidence remains `0.18`.

# ALTERNATIVE EXPLANATIONS

The candidate executions themselves may have completed correctly, but their seed and summaries were not durably committed and therefore cannot be reconstructed or interpreted without violating the audited random-seed boundary. Empty candidate logs only show that the workers emitted no stderr/stdout; they are not score records.

# DECISION

`inconclusive` infrastructure failure. Preserve both immutable plans, invalidation events and all eight logs. Do not rerun EXP-0055, do not add it as hypothesis evidence and do not change confidence.

# NEXT DISCRIMINATING EXPERIMENT

First perform one user-authorized service-only migration to `heldout_mechanism_recombination_v2`: permit `minimum_combination_accuracy` in result summaries, add a regression fixture proving a complete v2 result validates, and make any post-seed runner failure durably record its realized seed and failure status without scientific scores. Freeze the new evaluator and pass full tests, integrity and doctor with no plan or scoring. Only the following wake may preregister `EXP-20260830-0056` as an otherwise identical child of EXP-0055 and execute one fresh runner-random quick.
