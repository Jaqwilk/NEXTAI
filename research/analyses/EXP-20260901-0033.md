# OBSERVATION

`EXP-20260901-0033` completed all eight registered roles and all nine cells at
the single runner-random seed `1749513871`. The immutable semantic plan SHA-256
is `592f52c91c3b5d8a3a1a7b4e1b87ec6cdf3aebc0056547b8bdf1146e2597d410`.
Evaluator integrity passed before and after scoring over 673 frozen files at
digest `2037fdebd9dbca996bb9a61bd668ab3007293c19adb9d5780ff19a0ff41b28cf`;
the candidate bundle digest was
`444df1ef3a792256685a468d1ac146996199d5e312e8f1037f69ac0318bd2089`.

The factorized role averaged normalized RMSE `0.253730`, accuracy `0.125000`,
damaged-input accuracy `0`, and stable-rollout rate `1.0`. The source-identical
monolithic role reached `0.174805`, `0.347222`, `0`, and `1.0`; local ridge
reached `0.165296`, `0.319444`, `0.041667`, and `1.0`. Factorization therefore
worsened NRMSE by `0.078925` versus monolithic (paired nine-cell screening SE
`0.024490`) and by `0.088433` versus ridge (SE `0.025105`). It improved NRMSE
by `0.870085` versus frozen learning, which exactly matched persistence at
`1.123814`, showing that learning was active but the antisymmetric restriction
was harmful relative to its direct causal control.

The reversal strengthened with rollout depth. At D4/D8/D16, factorized NRMSE
was `0.129635/0.191270/0.440283`; monolithic was
`0.115426/0.141727/0.267262`; ridge was
`0.087147/0.153672/0.255070`. Factorized accuracy fell from `0.375` at D4 to
zero at D8 and D16. Thus both preregistered held-out-depth margins failed in
the wrong direction, and worst-depth accuracy also lost to monolithic and
ridge. All outputs remained finite.

The structural cost signature did hold. Factorized mean query work was exactly
independent of dormant K=64/256/1024, with three-point log-log slope `0.0`,
SE `0.0` and R-squared `1.0`. Its depth-compute slope was `1.792481` over three
points. Full aggregate costs were `768,567` fit operations, `43,120` mean query
operations, `677` update operations, `1,312` resident bytes, `298,744` peak
bytes and `6,984,668` R16 operations. Monolithic used `728,631`, `29,530.67`,
`1,312`, `298,448` and `4,987,764`; ridge used `87,061`, `8,362.67`, `480`,
`232,528` and `1,297,585`. The factorized role therefore paid more for worse
quality than both direct controls.

The stored implementable frontier is empty because no implementable role met
the frozen `0.95` accuracy gate; this was not caused by a timeout, crash or
missing metric. The privileged support control was reported separately at
zero NRMSE and perfect accuracy. This is one-seed screening uncertainty, not a
replicated estimate of world-to-world variation.

# INTERPRETATION

This is a valid negative for the exact 39-feature reaction/exchange rule. The
semantic fixtures and zero K slope confirm that the intended antisymmetric
local mechanism was present and locally executable. The monolithic ablation's
large quality advantage isolates the failure to the exchange constraint rather
than to missing learning, rollout instability or the shared data boundary.

Exact antisymmetry is not preserved by the evaluator's redundant nonlinear
anonymous observation coordinates, and the target dynamics also contain local
reaction and directed effects that need not form a conservative pair flow.
Constraining one learned pair field to change sign on endpoint reversal removes
predictive degrees of freedom that monolithic regression and affine ridge use.
The result retains a genuine local-compute signature but supplies no matched-
quality capability-per-cost gain. Because ridge is both more accurate and much
cheaper, this is not a new Pareto tradeoff.

The experiment falsifies the fixed 15+24 feature representation, exact
half-difference construction, ridge `0.001`, LMS `0.05`, clip `1.5` and sparse
schedule as one system. It does not falsify all learned conservation laws in
coordinates where conservation is observable. Changing coordinates, features,
regularization or exchange symmetry now would be a new thesis and post-score
tuning of this exact direction.

# CONFIDENCE

Confidence is `0.995` in the recorded seed, complete statuses, hashes and
metrics. Confidence is `>0.999` that the preregistered success conjunction
failed: both primary causal comparisons and both held-out-depth margins moved
in the wrong direction, worst-depth accuracy regressed and the implementable
frontier was empty. Confidence is `0.98` in discarding this exact rule without
replication or tuning. Confidence remains below `0.20` for generalizing the
negative to every local conservation-based learner because this is one visible
synthetic family and one scoring seed.

# ALTERNATIVE EXPLANATIONS

The direct monolithic field performs fewer feature operations than the
antisymmetrized field, but its quality advantage alone is already decisive and
cannot be explained by accounting. The common primitive pair map and common
fit rows make it a direct causal null rather than an independently tuned model.

The four observation channels are a signed permutation of two latent and two
nonlinear redundant channels. A learned coordinate chart could potentially
make a conservation constraint more appropriate, but adding such a chart would
combine representation learning with exchange factorization and would not be a
repair of this preregistered one-factor test. The one realized world transform
could exaggerate the gap; replication is not warranted because the effect is
large, opposite-signed and accompanied by classical dominance.

# DECISION

Discard and falsify `HYP-0045` for this exact mechanism. Do not replicate,
promote or tune its features, ridge, LMS, clipping, coordinate system or
exchange rule. Preserve the implementation as diagnostic evidence that local
K-independent execution is insufficient when the structural constraint loses
matched quality. Together with `EXP-20260901-0022`, place continuous-local
finite-feature rule learning dormant rather than patching it again.

# NEXT DISCRIMINATING EXPERIMENT

Cycle 183 should be one minimal no-scoring identifiability selection on the
existing frozen mechanism-recombination cohort, not another continuous-local
variant. Test whether an anonymous learned hyperdimensional operator binding
can be distinguished from exact interpretation, MDL module search and the
already-falsified macro/behavioral compilers under held-out program depths
4/8/16 and fresh re-encodings. Reject the direction before planning if the
runner-random state conjugation makes binding equivariance unidentifiable.
Otherwise activate only a role-versioned successor with shared learned binding,
source-identical composition-disabled and frozen-code ablations, without a
candidate or score. Cycle 184 must then preregister and execute one quick; a
valid negative ends that exact binding rule without tuning, and a positive is
scout evidence only.
