# OBSERVATION

`EXP-20260830-0058` was preregistered at `research/plans/EXP-20260830-0058.json` with semantic plan SHA-256 `0b9f4cff9bdb09462434ac9d24847a304cba5c2f01095c78e405facc3934cd3b`, evaluator SHA-256 `ed1382186e00e9c6de1957ac23f0c20cd4e79dd14b52bab486c372e9d8a467f2`, candidate-bundle SHA-256 `89e98ba81125c6328574105721aff0e28e23d0149a77db6194517c7916afdeae` and runner-realized seed `747591598`. The preregistered candidate fixture passed before scoring with maximum held-out reconstruction error `3.22365e-5` and training-flight permutation error `1.22125e-15`.

Exactly one audited runner call was made. Ten candidates produced durable worker artifacts and all eleven have durable logs. `empirical_gaussian_joint_v1` reached the hard 180-second limit: its empty log begins at 23:05:05 and the next candidate begins at 23:08:05, but the supervisor-generated timeout row was retained only in runner memory and is absent from the post-seed artifact manifest. The remaining ten artifacts are hash-manifested in the append-only `experiment_runner_postseed_failure` event.

After candidate execution and cross-candidate aggregation, final result validation failed because `contextual_gaussian_chow_liu_v1` had continuous conditional log loss `-0.1548527656`, while `experiment_result.schema.json` incorrectly imposed a nonnegative minimum. A negative differential log loss is possible when a continuous density exceeds one. The runner atomically invalidated the plan at 21:09:03Z, cleared active state and wrote no result or ledger row. The event explicitly marks `scientific_evidence=false`.

For diagnostic preservation only, deterministic recomputation of the parent aggregation from the ten saved worker artifacts gives the shared learner mean NRMSE `0.70210`, horizons 1/10/50 `0.44148/0.63711/0.93553`, stable-rollout rate `0.999919`, minimum condition gain `-0.07065`, minimum trajectory gain `-0.09215` and oracle-gap closure `-0.001465`. Its K=8/K=32 NRMSE values were `0.79313/0.61107`. No-sharing pooled ARX had mean NRMSE `0.65433`; independent ARX had `0.88449`; contextual Chow-Liu and ridge ARX each had `0.66722`; RLS had `0.65433`. These are diagnostic snapshots from an invalid experiment, not scientific evidence, and are excluded from the ledger, Pareto frontier, hypothesis evidence and confidence.

# INTERPRETATION

The terminal defect is in result-schema semantics, not in the candidate or corpus: a schema for a continuous log-density reused a nonnegative constraint appropriate to discrete negative log probabilities. The missing timeout artifact exposes a second durability gap: worker-produced JSON is preserved, but supervisor-created timeout or memory-limit rows are not materialized before final validation.

The diagnostic shared-learner values would fail the preregistered positive-transfer and oracle-gap gates, but protocol v2 forbids turning them into evidence after terminal invalidation. No rerun, threshold change, rank change or selective K interpretation is permitted in this wake.

# CONFIDENCE

- `1.00` that EXP-0058 is terminally invalidated and cannot be rerun or cited as evidence.
- `1.00` that seed `747591598`, the runtime plan, ten worker artifacts and eleven logs are durably preserved.
- `0.999` that the final validation failure is caused by an invalid nonnegative schema bound for continuous differential log loss.
- `0.995` that the empirical-joint process hit the registered 180-second limit, based on runner sequencing and timestamps; no durable supervisor row exists, so this is not a complete candidate artifact.
- HYP-0023 remains at `0.22`; EXP-0058 causes no confidence change.

# ALTERNATIVE EXPLANATIONS

- The contextual implementation could have mislabeled a log density; however its registered formula is Gaussian log density and negative average negative-log-density is mathematically permissible.
- Filesystem timestamps are strong but indirect evidence for the empirical timeout. Only a persisted supervisor row would prove its exact termination metadata.
- A future valid seed may differ materially from the diagnostic one; no seed-level scientific inference is allowed here.

# DECISION

`inconclusive` and terminally invalid. Preserve the immutable plan, realized seed, runtime plan, ten worker artifacts, all eleven logs, invalidation event, candidate code and this analysis. Do not add EXP-0058 to HYP-0023 evidence, Pareto, falsification, replication or confidence. Do not rerun this plan.

# NEXT DISCRIMINATING EXPERIMENT

The next wake must be one protected service-only migration to `heldout_dronepropa_factor_recombination_v5`, with no hypothesis evidence, plan, seed or scoring. Remove only the erroneous `minimum: 0` from continuous `conditional_log_loss`; preserve every metric formula and direction. Before final result validation, atomically persist the supervisor-normalized row for every candidate, including timeout, memory-limit, audit failure and crash, and hash all rows in the post-seed event. Add regressions for a negative continuous log loss and an intentionally timed-out worker, then pass full tests, integrity, report and doctor. Only a later wake may preregister a new child of invalid EXP-0058 with the unchanged rank, matrix, controls and outcome rules.
