# OBSERVATION

`EXP-20260830-0051` used immutable plan `research/plans/EXP-20260830-0051.json` (SHA-256 `f35f8b90d5f1606b699e601acae19a015c3b830ca8d5f38a5ae0b2fe57e96bce`) and runner-realized seed `1684198279`. The evaluator SHA-256 was `edb44fcc9422f6c5e90eca118347d43eac97426227b166a6f187f55e5cedd958`; the candidate bundle SHA-256 was `83e5f5ee996beb7b105de677c5ad08a3ed9f4bea59bba08e068aae774b19157f` before and after scoring. Protected integrity and candidate audit passed.

The shared relation-fragment candidate crashed before completing a scored trial with `ValueError: max() iterable argument is empty`. The bounded pooled fragment set contained a selected position-zero fragment whose output length had no eligible fragment for a later output position. Consequently there is no shared transfer or cost estimate, and no shared-minus-independent causal estimate.

The source-identical independent induction completed all 24 cells. It achieved transfer accuracy `0.208333`, minimum-family accuracy `0.0`, and near-equivalent accuracy `0.125`. Family means were probabilistic `0.395833`, predictive `0.354167`, program `0.083333`, and local `0.0`. Its full accounting was: data acquisition `383,637.333` operations, fit `757,114`, meta-fit `10,461`, mean query `1,619.979`, update `397.667`, resident state `346,896` bytes, peak state `7,368,116` bytes, mean bytes touched `4,868`, and R16 workload `2,490,272.167` operations.

Contextual Chow-Liu achieved `1.0` transfer and `1.0` minimum-family accuracy and was the sole implementable Pareto point. Empirical joint achieved `0.932292` / `0.729167`; autoregressive achieved `0.838542` / `0.354167`. The privileged oracle was exact and remains a lower-bound control only. The completed independent graph therefore missed every preregistered capability threshold and was dominated on the declared frontier.

There is one scoring seed, and the 24 cells are not independent replications. The result status is `complete_with_failures`: completed control observations are valid, but the primary shared comparison is missing.

# INTERPRETATION

This exact shared implementation is rejected because its capacity rule did not preserve a total component-emission model. The crash is an implementation failure, not evidence that pooled relation fragments cannot transfer. The completed independent branch nevertheless shows that first-occurrence canonicalization, capped exact fragment counting, support matching and Jaccard component selection do not by themselves recover adequate structure in any balanced four-family sense.

The experiment is inconclusive for HYP-0020's central sharing claim because the shared candidate produced no trials. It is negative evidence against the registered implementation and makes a blind unchanged replication unjustified. A minimal pre-scoring repair must make component selection total while preserving the frozen representation, capacity, matching rule and source-identical shared/independent contrast.

# CONFIDENCE

- `1.0` that the preregistered shared implementation cannot complete this seed.
- `0.99` that the completed independent implementation fails the absolute transfer and minimum-family gates.
- `0.95` that no promotion, replication claim or shared-transfer conclusion is permitted from EXP-0051.
- `0.72` that the registered fragment induction is unlikely to meet HYP-0020 after a purely mechanical totality repair, because its independent form is far below all capability gates; the missing shared observation and single seed prevent a stronger conclusion.
- Reduce HYP-0020 confidence from `0.10` to `0.07` and keep it `testing`, with EXP-0051 explicitly recorded as partial negative/diagnostic evidence.

# ALTERNATIVE EXPLANATIONS

- Pooling may create cross-world fragments that materially outperform the independent models once the totality defect is repaired; EXP-0051 contains no observation of that effect.
- The global cap of 64 can discard correlated output-position fragments even when the underlying representation is useful. A total grouped-cap rule would change a causal factor and must not be smuggled into a bug fix.
- Jaccard matching may be the main bottleneck rather than fragment representation. Changing similarity after seeing this result would require a new hypothesis rather than repair replication.
- These are visible local worlds and one runner-random seed, so even a repaired positive would require configured replication and an adversarial variant.

# DECISION

`inconclusive` for HYP-0020 and `discard` this non-total shared implementation. Preserve the plan, candidate sources, logs, partial result and this analysis. Do not rerun, patch-and-score, tune capacity, change similarity or promote in this wake. The independent result is valid negative evidence; the shared crash is diagnostic history and not a zero score.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, preregister `EXP-20260830-0052` as a one-seed repair replication on unchanged frozen `cross_family_relation_fragment_transfer_v4`. Before scoring, add a hand-verifiable regression fixture in which cap-64 pooling retains position zero but omits a required later position; require both shared and independent candidates to return a deterministic answer without exception. Make `_best` total using only the already frozen eligible fragment set and a preregistered deterministic fallback that does not change capacity, structural representation, Jaccard score, family blindness or full-cost axes. Then compare the same six candidates at K `8/32`, D `1/4/6`, Q `8`. Abort before scoring if the repair changes the evaluator digest, semantic contract or any other causal factor. A completed quick may only decide discard versus replication; it cannot promote.
