# OBSERVATION

`EXP-20260830-0056` completed all six registered cells for each of eight candidates under immutable plan `research/plans/EXP-20260830-0056.json` (SHA-256 `b98c030883ce16f4c38a5084fd8f17d88afdfc7bf42f520aade5e541a58f854c`) and runner-realized seed `1143710411`. Integrity passed before and after scoring with evaluator SHA-256 `0f3be05e3843e424be4e3a98a115000972a729ef49c25f6c5d671da70c78e3b0` and candidate-bundle SHA-256 `0aafca973bc4a5f112ad464051acdec6a7f8413634396a6fe6e30d9ca70c5395`.

The shared learner scored `0.125` mean held-out accuracy and `0.0` minimum-combination accuracy. This misses the preregistered `0.95` and `0.90` quality gates by `0.825` and `0.90`. Its source-identical independent and no-cross ablations both scored `0.0`, so the mean advantage was `0.125`, but at least one evaluated combination remained entirely unsolved.

The exact-MDL module-library control also scored `0.125` mean accuracy and `0.0` minimum-combination accuracy. It matched the shared learner on every registered cost summary: acquisition `1896`, fit `1320`, meta-fit `5192`, mean query `7.333333`, update `1`, resident state `16432` bytes, peak state `151456` bytes, mean bytes touched `117.333333`, workload `2894.333333`, and R16 workload `3774.333333`. Thus the observed shared result has no quality or fully charged cost distinction from exact finite structure search.

Other implementable controls scored `0.020833` (order-5 Markov) or `0.0` (unigram, nearest-template). The privileged composition-graph oracle scored `1.0` mean and minimum-combination accuracy, showing that the frozen task is solvable with the withheld mechanism graph. The oracle is not implementable evidence.

There is one runner-random seed and six K/depth cells sharing that seed; they are not independent replications. The large quality-gate failure and exact equality with exact-MDL do not require a promotion replication, but they support only rejection of this exact learner/cohort mechanism, not all modular or representation learning.

# INTERPRETATION

The shared rule extracted a small amount of reusable finite structure relative to the causal ablations, but it did not recover the held-out ordered composition. Its numerical identity to the exact-MDL control means the result is adequately explained by the same bounded finite module search, not by a distinct transferable learned representation. The decisive null is therefore both absolute (quality gates fail) and comparative (the declared exact-search alternative explains the entire signal at identical full cost).

This does not falsify the broader question of a common learner across heterogeneous worlds. This cohort tests one opaque finite recombination interface and supplies the shared state/function boundary. Any different representation, objective, support, composition split or inference rule is a new hypothesis and must not be tuned against this visible seed.

# CONFIDENCE

- `1.00` that EXP-0056 is complete and valid under the frozen v2 protocol.
- `0.995` that the exact registered shared learner fails its preregistered quality gates.
- `0.99` that exact-MDL explains the observed capability at identical declared cost.
- `0.90` that this exact mechanism should not be replicated or tuned; broader modular-learning conclusions remain limited by one seed and one synthetic family.
- Reduce HYP-0021 confidence from `0.18` to `0.02` and mark it `dormant`.

# ALTERNATIVE EXPLANATIONS

- One scoring seed may be unusually adverse, but recovering only one eighth on average and zero in at least one combination is far below both preregistered thresholds.
- The finite support may underdetermine the composition for implementable learners even though the privileged oracle is exact. That would be a cohort-identifiability limitation, not positive evidence for the learner.
- A different learned latent representation might transfer, but changing it after seeing this result would define a new hypothesis rather than rescue HYP-0021.
- The common opaque Feistel interface is evaluator-supplied, so even a positive result here would not establish spontaneous representation discovery across naturally different data modalities.

# DECISION

`discard` the exact shared latent mechanism learner and make HYP-0021 `dormant`. Preserve the plan, seed, result, logs, candidates and this analysis. Do not replicate EXP-0056 and do not tune its search, representation, interface, support size, held-out composition or controls.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, perform one design/service-only cycle with no scoring: specify a frozen multi-world transfer cohort using three existing, genuinely different benchmark families and one unchanged family-blind learner interface. Freeze train worlds versus unseen test variants, prohibit family labels and hand-written ontologies, include source-identical independent learners plus contextual Chow-Liu, empirical joint, autoregressive and oracle controls, and require full acquisition/fit/query/update/state/bytes/R16 accounting. Add an analytic pre-scoring identifiability test and a negative-control test showing that finite exact enumeration cannot be numerically identical to the proposed shared learner. Only a later wake may preregister one quick screen; if those distinctions cannot be frozen without evaluator-supplied common semantics, record the blockage and run no experiment.
