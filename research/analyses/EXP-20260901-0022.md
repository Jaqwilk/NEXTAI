# OBSERVATION

EXP-20260901-0022 completed as a valid one-seed quick screen on `continuous_local_cellular_v1` with runner-random seed `217025178`. All eight roles and all nine K/D cells completed. Evaluator SHA-256 remained `e465f3fe47a405aef549ab1b99b67f429c31b20c88f44a3c0c775b035fbd90f9`; the pre-seed semantic gate, 439 tests, transitive candidate audit, preflight certificate, and integrity check passed.

The sparse and dense learned roles had exactly equal clean NRMSE and accuracy in every cell (maximum observed difference `0.0`), isolating scheduling as intended. Sparse query work was K-independent (`knowledge_compute_slope=0.0`), while dense work scaled approximately linearly (`0.999975`). At K=1024, sparse versus dense mean query operations were respectively `11036` versus `1880084` at D4, `36740` versus `3760148` at D8, and `132212` versus `7520276` at D16.

The learned representation failed the preregistered quality gates. Overall accuracy was `0.208333`, minimum-cell accuracy `0.0`, continual retention `0.111111`, and NRMSE `0.205696`. The raw affine ridge control was both more accurate overall (`0.263889`) and lower-error (`0.161621` NRMSE), so the required `0.01` quality advantage had the wrong sign by `0.044075`. At D16 the learned-versus-ridge NRMSE values were K64 `0.174867` versus `0.211476`, K256 `0.254908` versus `0.259222`, and K1024 `0.249489` versus `0.212200`: the required margin failed at every K and reversed at K1024. The kernel control was worse (`0.737708`), which does not rescue the conjunctive baseline gate.

Damage-recovery accuracy was `0.194444` for learned sparse and `0.0` for frozen, a real `+0.194444` signal but below the frozen `+0.25` success threshold. The sparse role paid `4196131` fit operations, `1183` update operations, `3136` state bytes, and `12842882` R16 workload operations, versus ridge's `87061`, `156`, `480`, and `1297585`. Stable finite rollout was `1.0` for all complete roles. The privileged support control was exact (`accuracy=1.0`, `NRMSE=0.0`).

The emitted implementable Pareto list was empty because the privileged control occupied the full frontier before the post-hoc implementability filter. This diagnostic cannot support a Pareto claim. It does not affect the negative decision because independent preregistered accuracy, retention, repair, ridge-margin, and full-cost gates already fail decisively. The frontier construction must be corrected by excluding privileged roles before recomputing the implementable frontier, without changing this completed result.

# INTERPRETATION

Sparse active-cone execution is a genuine and clean scheduler-level scaling signature: it preserves the exact learned computation while making cost independent of dormant world size. The quadratic learner itself is not a successful transferable representation. It extrapolates less accurately than a much smaller affine ridge rule, fails worst-cell accuracy and retention, and its learned consistency repair is too weak to meet the frozen threshold. The large fit and workload gap means the representation cannot be defended as a quality-per-cost improvement.

This result falsifies the exact conjunction of affine consistency repair, frozen quadratic basis, ridge fit, normalized-LMS update, and sparse scheduling. It does not falsify sparse causal execution in isolation, nor all learned continuous local rules. Changing the feature basis, ridge, repair threshold, update rate, clipping, or training distribution now would be post-result tuning and is prohibited.

# CONFIDENCE

Confidence that this exact preregistered learner fails its quick-screen claim is `0.99`. The conclusion is supported by exact source-identical sparse/dense equivalence, three dormant-area scales, three depths, complete mandatory controls, and large quality and cost failures. Uncertainty remains high about other representation-learning mechanisms because this was one seed on a locally visible synthetic cohort. That uncertainty cannot reverse the failed gates for this frozen rule.

# ALTERNATIVE EXPLANATIONS

The quadratic basis may be a poor inductive bias for repeated held-out-amplitude rollout, and the clean-only affine consistency estimator may underfit nonlinear redundancy. Those are plausible causal explanations, but replacing either mechanism would define a new hypothesis rather than repair this one. The favorable D16 margins over ridge at K64 and K256 show some regime dependence, yet reversal at K1024 and failure of the required all-K rule reject a robust transfer claim. The empty implementable frontier is a reporting defect, not evidence for or against the quality failures.

# DECISION

Discard and mark HYP-0039 falsified at confidence `0.01`. Do not replicate or tune this learner. Return the broader HYP-0006 family to dormant while preserving the isolated observation that source-identical sparse causal scheduling achieved zero K-slope and large K1024 query savings without changing output.

# NEXT DISCRIMINATING EXPERIMENT

First perform one minimal protected service wake that changes only implementable-frontier construction: exclude privileged support roles before Pareto computation, add a regression where a privileged global dominator coexists with two implementable trade-off candidates, then require full pytest, preflight, integrity, and doctor PASS. This is a real integrity repair, counts as one no-scoring cycle, and must not alter EXP-20260901-0022.

In the immediately following wake, preregister one fundamentally different breadth quick reviving HYP-0008 on the existing real `heldout_wt_changepoints_prequential_v2` cohort: learn a compact action-conditioned predictive state from anonymous public histories using a frozen train-only low-rank Hankel factorization, then apply a local predict-then-observe state update. Use identical representation code for learned, frozen-state, and observation-history ablations; compare against persistence, LMS, RLS, ridge FIR, transition-bank, bounded replay, coverage-aware spectral PSR, and a train-only discretized CSSR control. Require better held-out changepoint NRMSE at H32/H96, no worst-file regression, bounded state and local update cost, K-independent query work, and implementable Pareto non-dominance at full R16 cost. One seed can only select the mechanism for unchanged replication; a valid negative ends that exact factorization/update rule without tuning.
