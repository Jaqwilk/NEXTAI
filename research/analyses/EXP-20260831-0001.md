# OBSERVATION

`EXP-20260831-0001` is a valid one-seed quick on frozen `heldout_three_family_continuous_transfer_v1`. The immutable plan is `research/plans/EXP-20260831-0001.json`, plan SHA-256 is `45594568f6c4028eac6f9ae7a2d471b134d5265dad17fdc4123fbd5b794f25ba`, evaluator SHA-256 is `cbe30bce16db4d47eb879cfccbc526e7d9602b7f5d4b4a407cd30295e7274f8d`, candidate-bundle SHA-256 is `64675cca21ba11c028b1205c6ee4c636a2a9eedb630a229e32a5e2ecd69e1585`, and the runner-realized seed is `156875115`. All eleven candidates completed; integrity passed before and after scoring. The result file SHA-256 is `9caa03af78578e7d70305ffa925bfebf814eebfb56ccf30135d2e9c3c7ed4b55`.

The shared rank-4 masked spectral learner had overall transfer accuracy `0.375074`, minimum-family accuracy `0.00000127`, normalized RMSE `262936.46`, stable-rollout rate `0.6667`, and minimum shared-versus-independent gain `-346589.82`. By family, its mean NRMSE / minimum causal gain / stability were `788807.71 / -346589.82 / 0.0` on DS08a, `1.15065 / +9968.71 / 1.0` on DronePropA, and `0.51488 / +0.84436 / 1.0` on continuous-event worlds. Thus the preregistered every-family shared-transfer criterion failed decisively.

The cross-family-only assignment also failed in every family: minimum cross-family gains were `-682142.92` on DS08a, `-890561.75` on DronePropA and `-0.14483` on continuous-event worlds. Its overall stable-rollout rate was `0.3333`. Foreign-family training therefore did not beat support-only adaptation anywhere on the realized seed.

The simple persistence control was the strongest robust aggregate control: transfer accuracy `0.524656`, minimum-family accuracy `0.453087`, NRMSE `0.927927`, stability `1.0`, and R16 work `9.6922440197e9`, versus shared R16 work `1.0790331596e10`. The shared learner used less resident state (`42240` versus `746496` bytes), but did not satisfy its causal or capability guardrails. The declared Pareto axes were retained, yet both frontiers are empty because causal-gain fields exist only for their corresponding intervention rows and are missing from ordinary controls. This prevents a non-dominance claim but does not affect the already decisive causal failure.

Uncertainty across runner seeds is unestimated because quick used exactly one seed. The six cells cover three families and K=`4/9`, but they are not independent scoring replications.

# INTERPRETATION

The exact coordinate-aligned operator prior transfers some regularization between DronePropA and continuous-event worlds, but the same prior catastrophically destabilizes DS08a. More importantly, training only on foreign families hurts every target family relative to support-only fitting. The evidence therefore rejects the proposed portable representation rather than merely missing a global average threshold.

The most plausible mechanism is that padded column identity is not a transferable semantic coordinate system. Mean-plus-subspace shrinkage mixes unrelated controls and states across families, so its apparent benefits in two families are ordinary regularization within coincident columns, while DS08a exposes the incompatibility. This falsifies the fixed rank-4, ridge-0.001, shrinkage-0.5 learner, not all family-blind representation learning.

# CONFIDENCE

Confidence that this exact learner merits replication falls from `0.12` to `0.02`. Confidence is `0.99` that the preregistered positive conclusion is false on the realized seed: shared transfer is negative in DS08a, foreign-family transfer is negative in all three families, and rollout stability is below one. Confidence is `0.78` in coordinate mismatch as the main explanation because only one seed was scored and the fixed linear/shrinkage mechanism is a narrow realization of family-blind transfer.

# ALTERNATIVE EXPLANATIONS

- The realized seed may produce unusually ill-conditioned DS08a support, although persistence remains stable and both K values fail, so seed variance is unlikely to reverse the exact every-family rule.
- Fixed shrinkage `0.5` may be too aggressive, but tuning it after this result would be outcome-informed and is not an admissible replication.
- Absolute-state linear rollout may be the failure source rather than representation sharing; a bounded residual or exchangeable-channel learner can discriminate this without reusing the coordinate-aligned operator thesis.
- Empty Pareto frontiers reveal a reporting-contract defect for candidate-specific causal metrics. It blocks promotion/non-dominance claims but cannot turn the negative causal contrasts into positive evidence.

# DECISION

`discard` this exact learner and mark HYP-0024 `falsified`. Do not replicate, tune rank, tune shrinkage or promote. Preserve the complete result as valid negative evidence. No claim about an LLM successor, general intelligence or a scaling law is supported.

# NEXT DISCRIMINATING EXPERIMENT

The next wake should be one protected service-only v2 migration with no hypothesis, plan, seed or scoring: define Pareto capability axes that are present for every complete implementable row, keep intervention-specific causal gains as hard promotion gates rather than universal dominance axes, and add a regression test proving a non-empty frontier when all controls complete. After that PASS, the next scientific plan should test one source-identical permutation-invariant bounded residual learner that treats channels exchangeably and predicts clipped increments around persistence under the same shared, independent, cross-family-only and support-only assignments. This distinguishes coordinate mismatch from a broader absence of cross-family transferable dynamics without tuning the failed spectral learner.
