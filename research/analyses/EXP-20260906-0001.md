# EXP-20260906-0001 — WT-01 recurrence attribution on visible development

Cycle 297. Immutable plan: `research/plans/EXP-20260906-0001.json`.
Canonical plan SHA-256:
`56d9783f1503f9dd4d3546ce66acdd733e6fe03c6c3236cf1e2185df8f731997`.
Frozen evaluator SHA-256:
`870fe289903f3c7d42a41526124b4084072c5088b24d0772fe57076d5d1ea8d0`.
Authority: `WT-01-LIFECYCLE-REPLACEMENT-20260906-V1`. The runner executed
the one authorized replacement for invalidated pre-seed `EXP-20260905-0006`.

## OBSERVATION

The audited runner completed from 2026-09-05 22:57:27 to 22:59:07 UTC with
status `complete`. All nine frozen roles completed all 18 K/H/file trials: 162
of 162 trials were complete and stable. The sole runner-random channel
permutation seed was 1274935977. Fit used the frozen files 0-5; every recorded
evaluation row names visible-development file 6 or 7. No row names files 8-9,
and the frozen evaluator verifies only files 0-7 while deliberately not opening
or hashing files 8-9.

The preregistered primary contrast was
`NRMSE(R0,U1,C1) - NRMSE(R1,U1,C1)`. Aggregate NRMSE was
0.8288822815689805 without recursive self-feeding and 0.6660885563231256 with
it, a contrast of 0.1627937252458549. This is 4.87 times the frozen smallest
effect of interest, 0.03343253453162794, and a 19.64% relative reduction from
the R0 value. The file-level contrasts were independently positive within the
two-file development sample:

| Visible development file | R0-U1-C1 NRMSE | R1-U1-C1 NRMSE | Contrast |
|---:|---:|---:|---:|
| 6 | 0.8685640266800028 | 0.6825945819400236 | 0.1859694447399791 |
| 7 | 0.7892005364579581 | 0.6495825307062275 | 0.1396180057517306 |

The contrast was also positive at H=16, 32 and 96 on each file; its observed
range across the six file/horizon summaries was 0.0792684201681565 to
0.2513917525516840. Post-reveal RLS gave smaller secondary reductions: from
0.8346214485004590 to 0.8288822815689805 under R0 and from
0.6739068240718056 to 0.6660885563231256 under R1. The clipping switch had no
measurable effect in any paired role in these trials.

The direct controlled VAR(2)/ARX implementation matched R1-U1-C1 across all 18
trials. The maximum absolute difference over the prediction-derived quality,
stability, state and declared operation fields was
3.552713678800501e-15, below the 1e-12 absolute and relative tolerances. The
prediction artifact hashes were not byte-identical because the two algebraic
expressions differ at floating-point rounding level; the preregistered gate was
numerical equivalence, which passed.

Recurrence was not free. Mean query operations rose from 470 to 22560. For the
primary U1/C1 pair, workload operations rose from 10,186,052 to 10,583,672 at
R=1 reuse and from 10,312,952 to 16,674,872 at R=16 reuse. Both roles used
16,896 state bytes and reported a 1,616,768-byte peak. All candidate source
audits, return codes and before/after integrity checks passed. No retry,
download, second seed, second replacement or external model/API was used.

## INTERPRETATION

The frozen visible-development test supports the narrow causal statement that
recursive self-feeding materially improves prediction quality inside this
specific affine WT mechanism on the two already visible recordings. It also
shows that the successful mechanism has a complete classical controlled
VAR(2)/ARX representation. Therefore the result does not support architectural
novelty, and its higher recursive query work does not establish an end-to-end
economic advantage.

The zero clipping effect says only that the bound did not change these observed
outputs; it does not establish that clipping is generally unnecessary. The
small positive RLS effect is descriptive and was not the primary frozen
contrast.

## CONFIDENCE

Confidence is high in the execution integrity, numerical contrast and causal
factor isolation for these two files. Confidence remains low for generalization:
there are only two already visible physical recordings and one channel
permutation seed. K/H cells and episodes are repeated measurements, not
independent physical replications, so no meaningful population confidence
interval or replication claim is available. HYP-0028 remains dormant at 0.2.

## ALTERNATIVE EXPLANATIONS

The result can be fully explained by a classical affine controlled VAR(2)/ARX
predictor with slot-local RLS. Dataset-specific dynamics may favor recursive
rollout, and the two recordings may not represent other devices, regimes or
tasks. Files 8-9 are previously visible historical diagnostics rather than
fresh replication data, while `wt_walks_v1` changes the operation and can only
serve as an adversarial transfer-style test. Neither source can repair the
missing same-protocol physical replication.

## DECISION

KEEP the immutable result as descriptive causal-mechanism support and stop at
WT-01-DECISION. Do not promote the architecture, claim replication, transfer,
economic advantage or novelty, access files 8-9, tune, retry, register another
replacement or realize another seed. The one replacement registration, one
development attempt and one scoring run are fully consumed.

## NEXT DISCRIMINATING EXPERIMENT

No further experiment is currently authorized. After a separate user decision,
the exact discriminating next step is to acquire and freeze at least five
independent physical recordings generated by the same WT protocol, then
preregister the unchanged eight-cell recurrence factorial contrast and
VAR(2)/ARX control before inspecting those outcomes. Files 8-9 and
`wt_walks_v1` must not be counted as those replications.
