# PC-01-INTEGRATION-V1 — cycle 286

## OBSERVATION

The user explicitly authorized one additional service cycle, at most 60 minutes,
without training or scoring. Authorization is immutable in
research/laboratory/PC-01-EXTENSION-20260905-V1.json. The clocked start was
2026-09-05 09:27:06 UTC, with a binding deadline of 10:27:06 UTC. Work was briefly
interrupted by the user and resumed in the SAME cycle at 09:35:04 UTC, not as a
new service allowance. No live nextai/pytest experiment, STOP, PAUSE or run lock
was found at resume. State 285 meant last completed cycle, not a reset.

Initial doctor passed before maintenance. On resume it correctly failed on the
partially edited protected sources and stale report. These were the logged
authorized maintenance changes, not a candidate-result integrity failure. They
were tested before freezing; the retained SuiteSparse cohort stayed maintenance.
The maintenance-start event's manually assigned created_at=09:35:00 is an
approximate bookkeeping timestamp, NOT the measured cycle start. The clocked
authorization start/deadline above are authoritative for the service budget.

Implemented an independent diagnostic plan/schema and CLI route through the
ordinary nextai run dispatcher. It has no fictitious K/D axes or architecture
promotion role. Source audit precedes final seed realization. The trusted worker
owns the fixed optimizer, sampling, complete dev curve, checkpoint selection,
legal uniform/unigram/bigram controls, source-identical frozen control, precision,
causality, synchronized timing, environment and explicit cost-scope records.
The actual candidate model was NOT implemented or instantiated on real tensors.

The parent grants fit after starting its timer and supervises fit/worker time,
process-tree RSS, device peaks, payload and disk reserve. Every start precedes
launch in an append-only receipt. Failures, malformed outputs and timeouts become
inconclusive with raw artifacts retained. A lost parent leaves an unresolved
start and full fit reservation, blocking a silent retry. Measured overshoot is
charged, not clipped away. Final-series authentication checks all registered
plans/attempts, source/runtime/result hashes and three distinct runner seeds;
post-freeze development, source changes, replay and incomplete subsets fail.

Conformance: **132 passed, 0 failures/errors/skips, 13.690 seconds**, in
research/laboratory/PC-01-INTEGRATION-CONFORMANCE-V1.xml. This includes 49 new
integration tests, 59 prior primitive tests, 20 restart tests and four legacy CLI
tests. New tests use isolated temporary repositories and synthetic metric
records. Real subprocesses deliberately crash, hang or exceed limits. The model
layout is checked using meta tensors (no storage, forward or training). The
prior raw CUDA attention fixture is inference-only, not a learned candidate.

Full regression on the sealed code: **797 passed, 0 failures/errors/skips,
86.714 seconds** (XML suite duration), saved as
research/laboratory/PC-01-INTEGRATION-REGRESSION-V1.xml. Earlier diagnostic XML
reports are retained. TEST-2 had one fixture failure: installing a second wrapper
captured the already patched supervisor rather than the original function. The
fixture now retains the original callable; the production timeout/seed/quality
thresholds were not relaxed. TEST-1, TEST-3 through TEST-6 and the final
conformance/full-regression reports are retained. No failed report was removed.

Doctor passed after the logged maintenance freeze: **857 protected files**,
516 candidate audits, 427 sources, 59 hypotheses, **99 historical EXP results**,
and no pending EXP. History verification preserved 349 immutable artifacts and
event/source/registry prefixes, exact former evaluator bytes and 11 old sealed
source files archived under research/laboratory/archive/PC-01-HARNESS-V1.
The integration seal commits 25 source/document/report artifacts.

Current evaluator SHA-256:
73a32119275003a01a44764173f8f0b9bd7426ed4c8154ee8dc0f71a3e8ce711.
Candidate-bundle SHA-256 remains unchanged:
8d31045c9742fceaafeb88ec468702e358591ecc0616490855d21a30d767085e.
The separately registered conformance certificate binds the code, required tests
and current evaluator. It does NOT grant scoring authority or certify learning.
The old maintenance preflight certificate was archived and refreshed normally.

## INTERPRETATION

The four original integration areas now have implemented and tested preparation
paths. This is evidence about software conformance and failure accounting, not
an empirical positive control, an economic advantage or transfer. Diagnostic
envelopes have no architecture/Pareto rows; source provenance remains accessible.

The present production laboratory is still preparation_only and scoring=False.
An actual start additionally requires a separately authorized prospective
activation contract and tested gate transition, a new active PC-01 cohort freeze
and its matching versioned certificate. Merely flipping old SuiteSparse to active
is forbidden. No activation-policy transition was performed or certified here.

The detailed boundary and future commands are documented in
research/laboratory/PC-01-INTEGRATION-RUNBOOK-V1.md. Prior completed seals and
validators remain unchanged. The new validator resolves old sealed source bytes
from the archive rather than redefining historical assertions about live paths.

## CONFIDENCE

High in the recorded regression, numerical fixtures, history preservation and
isolated lifecycle/parent-process checks. Moderate in the unexecuted torch recipe
integration: no real 5000-update model has run, so no training-time, final-quality
or end-to-end learned-control claim is supported. Meta-layout validation cannot
prove real forward/backward behavior. The first preregistered dev run must still
discriminate implementation failure, budget infeasibility and actual learning.

## ALTERNATIVE EXPLANATIONS

Passing synthetic worker records cannot demonstrate correct model training. The
subprocess fixtures intentionally substitute the worker command and isolate
production authorization gates; separate tests verify real maintenance denial.
Parent RSS/storage/device telemetry is sampled every 50 ms, not an OS-enforced
RSS quota or native-code security sandbox. Short overshoot is possible and is
retained and charged. The local corpus remains inspectable, not a blind holdout.

The real worker now records attention profiler events and environment metadata,
but those production branches remain unexecuted. Matmul operation estimates omit
non-matmul work and are labeled estimates; complete worker wall time is distinct.
Exact past data-acquisition wall time is unavailable and must not be treated as
zero. No energy measurement or matched-quality economic comparison exists.

## DECISION

KEEP the integrated preparation code. Learning remains INCONCLUSIVE/unmeasured.
Close the single extra service cycle at a conservative **40-minute charge out
of 60**; elapsed through validation at 10:02:02 UTC was 34 minutes 56 seconds,
including the interruption. The 40-minute charge reserves closing work; it is
not a claim of exactly 40 minutes of measured tool execution. Final closure must
occur before 10:07:06 UTC for this reservation (and before the absolute deadline).
Unused minutes do not authorize a second extension cycle.

Original service accounting stays 2/2 cycles and 120/120 reserved minutes.
Extension accounting is separately 1/1 cycle, 40/60 reserved minutes. Total
reserved preparation: 160/180 minutes. Actual PC-01 development attempts 0/3,
final replicates 0/3, training 0 seconds; no scientific seed was realized.
Synthetic fixture randomness is not a scientific seed or a new EXP result.

No candidate source, completed hypothesis/result, old G1 window, CAL or BELIEFS
draft was changed. No dependency installation, download, external model/API,
schedule, deployment, branch merge, GitHub publication or destructive cleanup
was performed. Temporary pytest fixture outputs and failed reports were retained.

## NEXT DISCRIMINATING EXPERIMENT

Request a separate user decision on activation and the first development attempt.
That workflow must first implement/validate the prospective activation contract,
freeze the NEW PC-01 cohort and a new matching certificate, preserving all old
contracts. Then preregister one dev EXP with seed 1103 BEFORE implementing the
frozen Candidate architecture. Run only through nextai run, with all controls,
at most 1200 seconds fit and 1800 seconds worker, no final-test access. Report
complete/inconclusive objectively; never silently extend the recipe or budget.
