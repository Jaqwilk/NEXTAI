# OBSERVATION

The audited runner realized exactly one preregistered seed, `389123087`, and
wrote its immutable runtime plan. A hard Codex-turn interruption then terminated
runner PID 28304 after atomic supervisor artifacts had been written for only two
of fourteen planned candidates. The main error-gated role and frozen-credit role
each completed 18 trials; no aggregate result was written, twelve mandatory
roles were not completed, and no Pareto frontier or promotion gate can be
computed. The two preserved artifacts are diagnostic history only. Their
aggregate NRMSE values were respectively `1.002571393596196` and
`1.0071290063754978`, far smaller than the preregistered meaningful margin of
`0.1325268421060828`, but this incomplete comparison is not scientific evidence.

# UNCERTAINTY

The shuffled-credit, dense-credit and all ten classical controls are missing.
Consequently the strongest-control comparison, H32/H96 causal margins, dense
update-cost contrast, worst-case gate and universal Pareto status are unknown.
The interruption occurred outside the candidate outcome protocol, so it says
nothing about the mechanism's scientific validity.

# INTERPRETATION

`EXP-20260901-0047` is `invalid_infrastructure_hard_interruption`, not positive,
negative or null evidence for HYP-0052. Restarting the public runner would realize
a different random seed, while manually invoking remaining workers would bypass
the audited harness. The valid recovery is therefore append-only invalidation,
preservation of the runtime seed and partial artifacts, and no confidence or
falsification change.

# CONFIDENCE

HYP-0052 remains `testing` at confidence `0.08`. EXP-0047 is excluded from its
evidence set and cannot authorize replication, promotion, tuning or rejection.

# DECISION

Inconclusive due solely to an infrastructure interruption. Preserve all files;
do not score another plan in cycle 208.

# NEXT DISCRIMINATING EXPERIMENT

In the next wake, preregister one unchanged one-seed quick child of the exact
HYP-0052 rule on `heldout_wt_changepoints_prequential_v4`, with the same fourteen
roles, constants, matrix, success threshold and invalidation rules. Run it once
through `uv run nextai run --plan ...`. Do not tune the error gate, trace decay,
learning rate, clipping, features or residual bank. Before that run, add no
runner-resume framework; the archived interruption is sufficient diagnostic
motivation for a later, separately authorized service improvement.
