# OBSERVATION

`EXP-20260830-0052` completed all 24 cells for each of six preregistered candidates under immutable plan `research/plans/EXP-20260830-0052.json` (SHA-256 `f57ed32a32da43181ce385a3a8f045c5d1a691c2da651f6f020e60352d83a062`) and runner-realized seed `368403295`. The evaluator SHA-256 remained `edb44fcc9422f6c5e90eca118347d43eac97426227b166a6f187f55e5cedd958`; candidate bundle SHA-256 `32a251727a844ae433751aa0cf7aba34a3f4b2789b7fc2afa318496969547514` matched before and after scoring. The cap-64 missing-position regression, protected v4 semantic fixture, complete test suite, source audit, registered baseline checks and integrity gate passed.

The repaired shared graph achieved transfer accuracy `0.223958`, minimum-family accuracy `0.0`, near-equivalent accuracy `0.151042` and minimum-cell accuracy `0.0`. Family means were probabilistic `0.5`, predictive `0.354167`, program `0.041667` and local `0.0`. The source-identical independent graph achieved `0.197917`, `0.0` and `0.078125`, with family means probabilistic `0.333333`, predictive `0.375`, program `0.083333` and local `0.0`. Sharing changed overall accuracy by `+0.026042`, minimum-family by `0.0` and near-equivalent accuracy by `+0.072917`; it missed the preregistered `+0.05` gate on overall and the absolute `0.95` / `0.90` gates by large margins.

The shared graph used `383,637.333` acquisition operations, `681,690` fit operations, `4,238` meta-fit operations, `8,261` mean query operations, `397.667` update operations, `141,104` resident bytes, `7,358,300` peak bytes, `18,793.667` mean bytes touched and `9,266,291.5` R16 workload operations. Its depth-compute slope was `0.05108` across three points (SE `0.01189`, R-squared `0.94856`); its K slope `0.53540` has only two points and is screening-only. Low compute slopes do not compensate for failed capability.

The registered contextual Chow-Liu suite achieved `0.989583` overall, `0.958333` minimum-family and `0.875` minimum-cell accuracy and was the sole accuracy-gated implementable Pareto point. Empirical joint achieved `0.921875` / `0.6875`; autoregressive achieved `0.838542` / `0.354167`. The privileged oracle was exact and remains a lower-bound control only.

There is one scoring seed; its 24 cells are not independent replications, and seed dispersion is unavailable. EXP-0051 used a different seed and its shared branch crashed, so it is not a paired replication. EXP-0052 is scientifically valid and complete.

# INTERPRETATION

The preregistered totality fallback successfully isolated the EXP-0051 implementation defect: the same graph now completes without changing the evaluator or frozen representation rule. Its completed behavior decisively rejects the exact cap-64 canonical-edge/Jaccard/component-emission mechanism on this screening cohort. Pooling gives a small average advantage but does not repair local-family collapse, barely transfers program composition, and remains far below all capability thresholds.

This is evidence against the exact registered mechanism, not against all graph learning or all compositional representations. However further changes to capacity, cap grouping, similarity, fragment granularity or composition would change the thesis rather than replicate it. After recurrent state, dictionary readout and explicit relation fragments have all failed the same four-family boundary, another local representation patch has low expected information gain.

# CONFIDENCE

- `0.99` that this repaired implementation fails every absolute capability gate.
- `0.98` that its measured pooled advantage is insufficient: overall gain is `0.026042` and minimum-family gain is exactly zero.
- `0.97` that unchanged replication or tuning within v4 is unjustified because one family collapses completely, program transfer is near zero and only a registered specialist reaches the implementable frontier.
- `0.82` that the broader cap-64 canonical fragment/Jaccard mechanism is a poor route on this boundary; the single visible seed limits a stronger architectural-family statement.
- Reduce HYP-0020 confidence from `0.07` to `0.02` and mark it `dormant`.

# ALTERNATIVE EXPLANATIONS

- A grouped capacity allocation could preserve output-position coverage, but that changes induction rather than repairs totality and would be post-result tuning.
- Learned rather than fixed similarity might align fragments across worlds, but it constitutes a new representation-learning hypothesis and needs its own controls.
- The lossless public stream may make the shared sufficient statistic unnecessarily hard to learn even though family-native specialists solve the worlds; this is part of the intended cross-family transfer challenge, not a scoring defect.
- One local runner seed could understate a real pooled advantage, but it cannot plausibly explain the `0.0` local mean or the gap from `0.223958` to the `0.95` gate without a qualitatively different mechanism.

# DECISION

`discard` the repaired shared and independent relation-fragment implementations and make HYP-0020 `dormant`. Preserve EXP-0051 and EXP-0052 plans, candidates, regression, logs, results and analyses. Do not tune or replicate v4. No promotion claim is permitted.

# NEXT DISCRIMINATING EXPERIMENT

No EXP-0053 may be preregistered in the next wake because both configured review cadences are now due: 12 completed experiments since the last portfolio reflection and 6 since the last literature review. Use one combined review-only cycle with no candidate change, plan, seed or scoring. Recompute the valid-evidence Pareto portfolio, explicitly compare the recurrent, dictionary and relation-fragment failures, audit whether the four-family evaluator rewards family identification rather than transferable sufficient statistics, and review primary work on learned modular program induction, hierarchical Bayesian task representations and object-centric relational learning. The review must select one genuinely new, family-blind mechanism and specify its cheapest pre-scoring identifiability gate; it must not reopen v3/v4 by changing capacity, similarity or decoder constants.
