# OBSERVATION

`EXP-20260831-0004` used immutable plan `research/plans/EXP-20260831-0004.json`
(SHA-256 `ff2b61fd9b1c7de9176a44d13506bffc432d56ee4a35cedacb565bf84fd9e4d3`),
runner-realized seed `1759197748`, evaluator SHA-256
`6d721631f70dd384b0cd66c6b193df1a7f33842cde64332bac6c6efa925fa4d6`,
candidate-bundle SHA-256
`1852dbd70d0d098d24d07dcd152a94b60f4c13c328e0130982992ec3e4f8d5b3`, and
result-file SHA-256 `bfd068984ffa32b073a68a279667a9228c3f0b1c544d2005c974e88a9f98397c`.
Integrity passed before and after runner execution.

The result status is `complete_with_failures`. Shared, independent and cross-family-only
predictive-index roles each crashed before recording a trial with the same `ValueError`:
`could not broadcast input array from shape (0,) into shape (1,)`. The traceback reaches the
empty-bucket fallback in `tensor_indexed_local_operator_core.py`. The mandatory
`tensor_random_projection_hash_v1` control independently crashed at the same line and with the
same error. All four failures are preserved in the immutable result and candidate logs.

The support-only predictive-index role completed nine cells because its occupied-bucket pattern
did not trigger that path. It had transfer accuracy `0.353247`, minimum-family accuracy
`0.000004285`, normalized RMSE `77787.4519`, stable-rollout rate `0.942708`, mean query work
`45400`, state `818808` bytes and R16 workload `9958074697` operations. These diagnostics cannot
stand in for the missing shared causal roles. Persistence completed with transfer accuracy
`0.526066`, minimum-family accuracy `0.463253`, normalized RMSE `0.918832`, stability `1.0`, mean
query work `6083.33`, state `746496` bytes and R16 workload `9692234995.67` operations.

Because three causal roles and a mandatory matched control have zero trials,
`shared_vs_independent_gain`, `cross_family_transfer_gain`, all preregistered per-family/K margins
and implementable Pareto membership are undefined. The frontier is empty. Uncertainty about
HYP-0027 is therefore not statistical one-seed uncertainty: the tested causal contrast was never
observed.

# INTERPRETATION

This is a shared infrastructure/control execution defect, not scientific evidence for or against
predictive-equivalence transfer. The identical mandatory random-hash failure shows that the crash
is not unique to the learned representation. An empty selected bucket combined with a fully
occupied 32-coordinate input leaves the inherited persistence slice empty while the evaluator
requires a visible output coordinate.

The support-only diagnostics are also negative-looking, but interpreting them would violate the
immutable conjunction: the matched random control and both transfer contrasts are mandatory. No
learner hyperparameter, code bit, moment, bucket cap or threshold may be changed from this result.

# CONFIDENCE

HYP-0027 remains at prior confidence `0.10`. `EXP-20260831-0004` is terminally invalid and must not
enter evidence, Pareto, confidence, falsification, replication, `promising` or `promoted` logic.
Confidence is high (`>0.99`) in the immediate defect classification because four independent
audited candidate processes, including the mandatory random control, report the same exception
and source line. Confidence in the scientific hypothesis is unchanged because no shared causal
trial completed.

# ALTERNATIVE EXPLANATIONS

- A candidate-specific representation failure cannot explain the identical random-hash control
  traceback, although learned-bucket occupancy may make the common defect more frequent.
- The first realized world may expose a mask geometry absent from the prior development smoke;
  that is still a missing semantic fixture and therefore an infrastructure defect.
- The completed support-only role suggests poor quality for this seed, but without shared,
  independent, cross-family-only and random-hash observations it cannot discriminate transfer
  from local bucket occupancy.

# DECISION

`inconclusive` scientifically and append-only invalidate `EXP-20260831-0004` with code
`invalid_mandatory_control_execution`. Preserve the plan, seed, result, logs, implementation and
all completed/failed rows as diagnostic history. Do not replicate, tune or use it as evidence.
The active benchmark enters maintenance until the empty-bucket contract is corrected in a new
version and a discriminating real-file fixture passes before any new seed is realized.

# NEXT DISCRIMINATING EXPERIMENT

The next wake must be one protected service-only migration to
`heldout_three_family_continuous_transfer_v4`, with no hypothesis, plan, seed or scoring. Preserve
v3 and EXP-0004 unchanged; define an output-width-safe empty-bucket fallback shared by learned and
matched index controls, and add a real-file fixture with all 32 input coordinates visible, at
least one required output coordinate and a deliberately empty selected bucket. Require the true
random-hash control and all four frozen learned roles to complete that fixture, plus full pytest,
preflight, integrity and doctor PASS. Only the following wake may preregister a corrected quick
with the exact HYP-0027 learner and thresholds unchanged; it is a new test, not a replication of
invalid EXP-0004.
