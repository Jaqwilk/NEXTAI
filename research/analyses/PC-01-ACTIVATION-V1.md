# PC-01 activation validation — cycle 287 preparation

## OBSERVATION

User approval: "zatwierdzam etap", scope bound by
research/laboratory/PC-01-ACTIVATION-20260905-V1.json. One development attempt,
pc01_byte_gpt_v1, seed 1103, 1200 s fit / 1800 s worker, no final series/access.
The original restart and closed service budgets remain unchanged. An effective
hash-bound authority overlay enables only this scope, not legacy plan creation.

Before mutations doctor passed with 99 results, 857 protected files and no
pending plan. Exact prior manifest and 25 integration artifacts were archived.
The new validator verifies 349 immutable historical files, ledger prefixes and
36 archived harness/integration artifacts. No historical candidate source was
changed. The candidate-bundle digest did change because its retained SuiteSparse
conformance test now checks the archived cohort identity and current PC-01
identity; its mathematical baseline assertions and implementations are unchanged.

Initial scoped tests: 137 passed, zero failures/errors/skips (TEST-1.xml).
First full suite: 805 passed, one failure due to report freshness after config
change (REGRESSION-1.xml); retained. Report regenerated without altering the
freshness gate. Final suite: 806 passed, zero failures/errors/skips, recorded in
PC-01-ACTIVATION-CONFORMANCE-V2.xml. Execution certificate V2 and generic
preflight both verify. Doctor PASS and history validator PASS precede any EXP
registration or candidate implementation.

Evaluator: ad311a51ff3652e5bdf9ac283b9b4d24ea1a9734c7f2a43641426793183a0f77.
No dependency installation, data download, model fit or final evaluation occurred
in activation preparation. Initial disk probe: 111318978560 free bytes.
Preparation wall-time is separate overhead, not fit time or old-budget reuse.

## INTERPRETATION

Software activation is ready for the one authorized preregistered development
attempt. Tests establish policy/numerical/process conformance, not competent
learning, economic superiority or transfer. The production 5000-update recipe
has not yet been executed.

## CONFIDENCE

High for checked artifact identity and tested scope denial. Actual full-model
execution feasibility remains unverified. Resource enforcement is sampled,
not an OS isolation or hard real-time guarantee; overruns must remain visible.

## ALTERNATIVE EXPLANATIONS

A later crash or timeout could arise from implementation, measurement or runtime
feasibility rather than failure of the transformer architectural family.

## DECISION

Keep the validated activation. Permit only one registered dev attempt. No final
series freeze, automatic retry or architecture promotion.

## NEXT DISCRIMINATING EXPERIMENT

Register PC-01 dev through nextai plan new before implementing Candidate; then
implement the already frozen byte GPT layout, validate without fitting, freeze
candidate-only changes with the evaluator digest unchanged, and run once through
nextai run. Retain the complete or inconclusive outcome and request its review.
