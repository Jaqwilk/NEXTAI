# PC-01-CONTRACT-V1 — cycle 284 service close

## OBSERVATION

Completed the first of two PC-01 preparation cycles. Immutable design:
`research/plans/PC-01-CONTRACT-V1.json`; scientific specification:
`research/laboratory/PC-01-CONTRACT-V1.md`. Acquired 1,115,394 bytes of pinned
Tiny Shakespeare; verified the payload and three fixed split hashes without
displaying corpus text or computing model quality. Source ledger now has 427
records, with four new primary sources. No model code or weights were downloaded.

Doctor PASS: 837 protected files, no pending EXP, 99 completed historical results.
Full pytest run exited 0. Design validator PASS: five deliberately invalid
contracts rejected, 335 historical files unchanged, ledger prefixes preserved.
These five checks concern design consistency, not the future learning evaluator.
No candidate training, seed realization, scientific scoring or legacy CAL rerun.

## INTERPRETATION

The next calibration is now specified, but no positive learning result exists.
RTX 4070/BF16 availability is verified; training throughput and quality are not.
The contract separates a learned effect from contextual quality and economics,
fixes train/dev/final access and requires three final seed replications without
inter-replicate tuning. One corpus cannot establish independent transfer.

## CONFIDENCE

High in data identity and recorded infrastructure checks; moderate in recipe
suitability; no empirical confidence yet in meeting 5000 updates within 1200 s
on this device. A timeout is budget-inconclusive, not architectural falsification.

## ALTERNATIVE EXPLANATIONS

Successful static checks cannot exclude future target/optimizer/timing bugs.
The final interval is locally accessible, not a genuinely blind holdout.
Historical Windows checkout validation is not a native Linux/cloud installation.

## DECISION

KEEP the design, without promotion or scoring activation. Preparation-only and
maintenance remain. Service budget conservatively charges 60/120 minutes
(reservation, not a precise measured duration); 1/2 service cycles used. Training
0/120 minutes; development attempts 0/3; final replicas 0/3. No scheduler, push,
deployment or background training started. Old G1, CAL, BELIEFS and EXP history
remain unchanged. This turn neither merged nor synchronized GitHub branches.

## NEXT DISCRIMINATING EXPERIMENT

First complete PC-01-HARNESS, the last permitted service cycle (at most 60
remaining preparation minutes), with no candidate training/scoring. Implement
and test the new evaluator/gates, one-seed deep workers, three-distinct-seed
series enforcement, budget enforcement and progress-aware restart state. Freeze
the new cohort/certificate before EXP registration and candidate implementation.
The current static restart pointer still says PC-01-CONTRACT; use the appended
lab_milestone_progress completion to avoid repeating it. At the service cap,
report ready or the exact blocker, without renaming/resetting the milestone.

The first subsequent registered dev test is seed 1103, optimizer on versus the
same frozen initialization, on the declared corpus, with no final evaluation.
Use only the audited runner once the cohort is ready, one EXP per cycle.
