# EXP-20260905-0002 — completed positive-control dev, scoped measurement caveat

Immutable plan: research/plans/EXP-20260905-0002.json.
Canonical plan SHA-256: 6b617d2c0880ea619b705930b1f12dedb9f128919c4c40e3ddf61769d4175988.
Immutable result: research/results/EXP-20260905-0002.json.
Result file SHA-256: 53744bbbf3d8980c95fce59e86083f387b4659ba78012e5391f0bde9bc80616c.
Authority/new cohort contract: research/laboratory/PC-01-DEV2-20260905-V1.json.

## OBSERVATION

Exactly one authorized fresh development run completed through the audited runner,
2026-09-05 14:12:59–14:18:09 UTC. Global dev attempt 2, seed 1103, cohort
pc01_byte_lm_learning_measurement_v2. Runner status is complete, error=null,
exit=0, 5000 updates. No final-series freeze or final evaluation occurred.
The original candidate, initialization, data/split, optimizer, controls and
5000-update recipe were unchanged. No checkpoint was resumed or tuning performed.

All ten executable measurement controls passed: known-probability loss, broken
target fixture, frozen parameter identity, causal suffix perturbation, split and
target alignment, fit-time holdout denial, batch partition invariance, dev-only
checkpoint selection, synchronized real-output timing, and budget/seed enforcement.

| Dev measurement (55,769 target bytes) | Bits per byte (lower is better) |
|---|---:|
| Initialized / frozen model | 8.128573081664243 |
| Train-only unigram | 4.756898572647639 |
| Train-only bigram | 3.5932131487929446 |
| Selected trained checkpoint, update 1500 | 2.1919434488637624 |
| Last trained checkpoint, update 5000 | 2.5899228994577608 |

Frozen-minus-trained = 5.936629632800480 bpb; unigram-minus-trained =
2.564955123783877; bigram-minus-trained = 1.401269699929182. The best checkpoint
was selected by the preregistered dev-only rule, not by final data. All 21
checkpoints (initial plus every 250 updates) are retained in the curve. Later
dev deterioration is disclosed, not hidden by reporting only the selected model.
FP32 dev=2.191900047285583 versus BF16 dev=2.1919434488637624;
absolute difference 0.0000434015781794 bpb, within the frozen 0.02 guard.

Initial and frozen logical state hashes are identical:
71e047ed5806d7f370865529f5899025dbcb1e6de8d72d0a4547cd0566c94d51.
Selected logical state: 63375911d51f917400240ae6765ac5ec558bde6aef573f535ca317adf40b3c9d.
New checkpoint file: research/pc01_payload/EXP-20260905-0002/best.pt,
43,288,032 bytes, SHA-256 bd44320649116a0bb2ae6438c9050951f2cfe566031b94651fcb808d85443431.
Runner verified the loaded selected state against the registered curve.

Fit including checkpoint evaluation/selection: 300.4259332 s (cap 1200).
Full supervised worker: 310.0255656 s (cap 1800). Peak RSS 1,418,985,472 bytes;
CUDA allocated 1,768,945,152 and reserved 2,174,746,624 bytes (10 GiB caps).
Total accounted local PC-01 payload 87,694,906 bytes (2 GiB cap);
free disk 110,525,431,808 bytes (10 GiB reserve).
The repaired writer recovered eight one-retry conflicts; the parent recorded
three read conflicts and maximum unavailable interval 0.061666 s. No timeout,
partial-JSON failure or concealed permanent I/O error was observed in this run.

Synchronized B1 next-byte latency: p50 1.712600 ms, p95 2.703230 ms,
100 measured repetitions after warm-up, context 256, no KV cache. Teacher-forced
throughput: B1 140,139.9; B8 785,091.2; B32 1,077,020.6 output bytes/s.
These batched teacher-forced rates are NOT autoregressive generation rates.
Timing includes conversion, H2D, forward, argmax and D2H; acquisition/service
costs remain in separate receipts, not zero. MAC/FLOP outputs are estimates
excluding non-matmul work. No energy or matched-quality economic win was measured.

Environment: RTX 4070, torch 2.6.0+cu124, CUDA 12.4, Python 3.13.13,
Windows 10 build 19044; BF16 autocast with FP32 parameters, no TF32/compile.
Actual profiler events show efficient SDPA. The runtime is a dirty master
checkout at ac3db74f36e5c33a9f3334fe825f11c2fb9ad992; immutable file/audit hashes,
not that commit alone, identify the executed code.

IMPORTANT completeness caveat: environment.json has
gpu_driver_clocks_load_snapshot=null. The executable controls/schema allowed
this, although the design requests driver, clocks and background load. Thus
runner complete does NOT establish complete environmental/timing certification.
The frozen result is not edited to erase this omission.

A separate post-run read-only diagnostic at 14:20:58 UTC reproduced NVML failure
(exit 255) under the worker's sanitized environment, while the ordinary
environment succeeded (driver 551.78). Adding ProgramFiles alone or ProgramW6432
alone restored this query; ProgramData, LOCALAPPDATA or APPDATA alone did not.
This supports an environment-dependent NVML initialization problem. No policy
was changed. These post-run observations cannot recover historical clocks/load
or replace the missing in-run snapshot. Evidence is in
research/laboratory/PC-01-DEV2-GPU-METADATA-DIAGNOSTIC-V1.json.

## INTERPRETATION

The unchanged conventional positive control learned a substantial local dev
signal and completed the full recipe and executable controls. This is useful
evidence against explaining the earlier interruption as inability to learn.
It also shows the telemetry repair survived an actual complete workload;
it does not establish universal Windows reliability.

This is one development seed on one visible corpus, with checkpoint selection
on that same dev set. It is not a replicated final positive, an independent
holdout result, a novel architecture, transfer, or an economic advantage.
V1 and V2 are separate measurement cohorts; the interrupted v1 curve is not
pooled as a second successful replica. Missing driver/load provenance limits
interpretation of the otherwise retained latency/throughput measurements.

## CONFIDENCE

High confidence in this run's observed dev loss reduction, completed update count,
source identity and preserved budget/history. Across-seed uncertainty cannot be
estimated from one completed seed. The 55,769 byte targets and correlated dev
checkpoints are not independent replicas; no confidence interval is manufactured.
Final-set performance and robustness remain unknown. Timings describe this
local run and its unrecorded instantaneous load, not algorithmic complexity.
Corpus hash verification reads the local payload; logical final-evaluation denial
is not an OS-isolated blind holdout inaccessible to the research agent.

## ALTERNATIVE EXPLANATIONS

Dev selection favors its measured minimum; the rise from 2.192 to 2.590 later
in training is compatible with overfitting, not a reason for post-hoc shortening
of the registered recipe. This familiar transformer control does not demonstrate
a new computational principle. GPU numerical stochasticity can yield different
trained trajectories despite the same declared seed. System load, driver and
kernel behavior can affect latency; the omitted in-run snapshot prevents their
full retrospective attribution. No causal claim about superior inference cost
follows from quality relative to the unigram/bigram alone.

## DECISION

KEEP the unchanged recipe as a successful development learning control and
retain the complete result. Do NOT promote an architecture or certify the full
PC-01 final learning/measurement gate. The environmental metadata gap remains
an explicit apparatus limitation requiring review before final-series approval.
Both authorized dev registrations are consumed; no automatic third dev, final
freeze/access, retry, publication or schedule change is permitted.

## INTEGRITY AND BUDGET

Integrity before/after: all 872 protected files passed, evaluator
bbb174b5f8bab4717c483a4b152e27bdbf5cebc803a37d3aa1b5a88ae1233773.
Candidate source unchanged at 426bfa8b9da43fb7cd991e104e48dfd366cf037dd9e840330d671b2174706aaa;
transitive candidate audit 27c90b510333990d4943b0c79f12286912971be60bcb00eb2f532421a5a73d3d.
The broader candidate-bundle digest changed because a retained-baseline test's
current-cohort assertion changed, not because any candidate implementation did.

All 868 pre-change protected files are archived and verified; 348 older static
historical artifacts and ledger prefixes plus the first dev result/checkpoint
and telemetry repair receipts are preserved. Original v1 schemas remain exact;
v2 dispatch changes only the benchmark identity. Original service accounting
2/2 plus completed extension 1/1 is not reset. Scope conformance first exposed
one test's incorrect expectation of a 1200 s fixture reservation; its synthetic
supervisor intentionally caps fit at 1 s. Both failed and corrected test reports
are retained. Full pre-run regression: 851 passed, 0 failed/error/skipped.

Original crash remains charged 1200 s. Cumulative PC-01 fit charge now
1500.4259332 / 7200 s, leaving 5699.5740668 s of design capacity, NOT authority
to use it. Completed results=101, cycle=289, no pending/running experiment.
The completion receipt binds this analysis and post-run verification separately.
BELIEFS, hypothesis events, historical G1 window and CAL are unchanged.

## NEXT DISCRIMINATING EXPERIMENT

First propose a bounded NO-TRAINING metadata conformance step: under the actual
sanitized worker environment, compare the existing NVML probe with a narrowly
scoped ProgramFiles/ProgramW6432 solution, record explicit success/error evidence,
and test missing/failed probes fail the declared measurement-completeness gate.
Do not broaden the candidate environment casually or patch the completed result.
Any implemented harness change requires prospective version/provenance review
and new certification; do not silently reuse an old evaluator certificate.

Only after that review and separate user authority, freeze the unchanged model,
recipe, selection rule and claim-specific final contract, then preregister three
fresh final-seed replicas through the audited runner (one per bounded cycle).
Account for the new evaluator identity explicitly before choosing a legal series
transition; no automatic new dev just to replenish or bypass an old gate.
Do not tune to dev deterioration or inspect final outcomes before freezing.
WT revalidation remains downstream of PC-01 review, not started here.
