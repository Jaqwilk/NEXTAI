# OBSERVATION

EXP-20260901-0021 completed as a valid one-seed quick screen on `heldout_mechanism_recombination_v6` with runner-random seed `734827148`. Integrity passed before and after scoring. All ten roles completed. The learned macro DAG was exact in every evaluated cell: accuracy, warm accuracy, near-equivalent accuracy, minimum-combination accuracy, new-fact accuracy, and retention were all `1.0`; false reuse was `0.0`.

The preregistered scaling and full-cost gates failed. The learned mechanism's depth-compute slope was `0.883577`, versus `0.883461` for the exact interpreter: it was worse by `0.000116`, rather than at least `0.002` better. At depth 16 and the declared 16-query horizon, its workload was `203264` operations versus `114191` for the interpreter, a `77.93%` increase rather than a `>=1%` reduction. Its overall 16-query workload was `149906.78` versus `82804.78` for the interpreter, an `80.97%` increase.

The only favorable signal was a small warm-query saving at depth 16: `2410.5` operations versus `2424.5` for the interpreter (`14`, or `0.58%`, fewer) and `2423.5` for the frozen source-identical ablation (`13`, or `0.54%`, fewer). This did not repay learning and state costs. The shared learner's depth-16 workload was also `5.63%` higher than the frozen ablation (`203264` versus `192427`), so the ablation gate failed. The learned role appeared on the universal Pareto frontier only because of this tiny query-cost trade-off; frontier membership did not satisfy the preregistered conjunctive success criteria.

# INTERPRETATION

The implementation learned and reused exact persistent macros, as shown by perfect correctness, zero false reuse, the small warm-query saving, and its advantage over rebuilding macros. The tested principle nevertheless failed: input traversal remained the dominant query cost, while macro admission, updates, and retained state added substantial end-to-end cost. At the declared horizon, partial evaluation shifted a few operations out of warm inference without producing a better scaling signature or a full-cost advantage.

# CONFIDENCE

Confidence that this exact preregistered mechanism fails its quick-screen claim is `0.98`. The conclusion is supported by deterministic operation accounting across three depths and three knowledge scales, exact source-identical ablations, and large full-cost margins. The single seed and synthetic cohort limit generalization to other forms of learned partial evaluation, but they cannot plausibly reverse the failed gates for this fixed rule without changing the hypothesis or horizon.

# ALTERNATIVE EXPLANATIONS

A substantially longer query horizon could amortize learning cost, but that was not the declared workload and extrapolating to it would be post-result rescue. A different segmentation, admission threshold, representation, or input index could alter the outcome, but each would change the tested causal rule and invite benchmark-specific tuning. The result therefore falsifies this exact macro-DAG rule, not every possible compiler or partial evaluator.

# DECISION

Discard and mark HYP-0038 falsified for the tested rule. Do not replicate, tune the threshold, change segment lengths, or create a rescue variant. Preserve the small warm-query reduction as diagnostic evidence that persistence worked, while treating the preregistered scaling, ablation, and full-cost failures as decisive.

# NEXT DISCRIMINATING EXPERIMENT

Use the next wake for one bounded breadth-mode experiment on a fundamentally different mechanism: a learned non-exhaustive continuous local cellular rule with sparse active updates and no family labels or hand-written ontology. Test at three inactive-area scales with identical code across learned, frozen-rule, sparse-event, and dense-synchronous roles; compare against exact/probabilistic finite-state, event-simulation, and local-linear controls at matched quality and full end-to-end cost. The discriminating signatures are sublinear cost growth with inactive area, local update cost without full retraining, OOD depth/area generalization, and emergent redundancy or repair. First determine whether an existing frozen continuous cohort exposes those signatures. If it does, preregister and score one quick in that wake; if it cannot, spend that wake only freezing the smallest new cohort version, and require the immediately following wake to score the quick. This is a conditional revival of HYP-0006 under its recorded revival criterion, not further optimization of HYP-0038.
