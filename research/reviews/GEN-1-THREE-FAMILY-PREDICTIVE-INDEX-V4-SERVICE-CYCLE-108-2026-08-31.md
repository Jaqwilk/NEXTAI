# GEN-1 — three-family predictive-index v4 service cycle 108

## Scope

This was exactly one user-authorized protected service-only cycle. It created no hypothesis,
experiment plan, scoring seed, scored candidate, result or confidence change and did not invoke
`nextai run`. Historical v3, immutable EXP-20260831-0004, its plan, result, analysis, logs and
scientific invalidation remain unchanged. V4 preserves every world, split, tensor, normalization,
learner bit, target moment, bucket count/cap, local ridge operator, support update, metric,
direction, threshold, K value, budget, seed policy, Pareto axis and causal promotion gate.

The user also clarified one protocol-v2 governance rule during this same no-scoring service wake.
`program.md` now records rolling portfolio targets of roughly 60–70% evidence-backed signal
development, 20–30% causal failure resolution and 10% genuinely new principles. It also makes the
repository and audited NEXTAI harness the only official evidence source: external Lab B outputs
and scores may propose hypotheses but can never enter evidence without independent local
reconstruction, preregistration and official-harness evaluation. This changes no v4 benchmark
semantics or scientific result.

## Defect and minimal correction

EXP-0004 exposed one inherited execution defect. If an indexed candidate selected an empty bucket
while all 32 current coordinates were public controls, slicing trailing state coordinates produced
an empty vector for a non-empty output. The existing tensor persistence baseline already handled
the same geometry by zero-padding missing state coordinates.

V4 adds only that identical padding operation to the shared indexed-operator fallback. Available
trailing state coordinates are copied and the remainder is zero. It does not alter the key,
projection, learned representation, bucket occupancy, operator, fit, cost model or query scaling,
and it cannot read a target. The same line serves raw index, random hash and all four predictive
index roles through their existing shared core.

## Semantic gate

The new development-only fixture uses the existing real `continuous_event` world at fixed seed
`1500003`. It verifies 32 visible history coordinates, 32 visible future-public control
coordinates and at least one required output coordinate, then deliberately empties the bucket
selected after real-file fit/adaptation. `tensor_random_projection_hash_v1` and the shared,
independent, cross-family-only and support-only predictive-index modules all return finite
`(50, 32)` outputs. This directly covers the shape that invalidated EXP-0004.

All ten unique registered semantic baseline nodes passed. The complete suite collected 336 tests
and passed. The prospective v4 plan schema retains K=`4/6/9`, the four source-identical roles,
both matched index controls, universal capability/full-cost Pareto axes and the two causal
promotion gates.

## Frozen integrity

- evaluator SHA-256: `0d50ce3241e5d6dc2dc3d8f1f8ee2888241800fd9bb80687840f44d8de294c43`
- candidate-bundle SHA-256: `97f5c072ddfbfd73faea5047a2d14e668d2de0024acda0d2234421879e899456`
- manifest SHA-256: `79c874d8470a17f9d2f11a2da4cc01877336f4e047c15c650092cb9c722e2acf`
- preflight certificate: `231a1e81ec9bec7233ba89685a2d59bd4465005729c9983a2f62a7ef4c47317a`
- archived v3 manifest: `research/manifests/heldout_three_family_continuous_transfer_v3-protocol-v2-8a5784a64a46.json`
- archived v3 manifest SHA-256: `7c0dfcaa268e6bc47c55997e4a2fb55a021e69e867bcfd7c7dde10061076a4ba`
- archived intermediate v4 manifest: `research/manifests/heldout_three_family_continuous_transfer_v4-protocol-v2-9195582e9a62.json`
- archived intermediate v4 manifest SHA-256: `9a4dd0d8695eb077f82f520f60f73602dd98efbb5ab8cc0d2183ce63826f48c5`
- semantic gate: `research/checks/three_family_predictive_index_empty_bucket_v1.json`
- integrity checked files: 507

## Decision

`keep` the v4 infrastructure. This correction is not scientific evidence and gives HYP-0027 no
capability, transfer, novelty, scaling or confidence credit. EXP-0004 remains terminally invalid
and cannot be a replication parent.

## Exact next discriminating experiment

In the next wake only, keep HYP-0027 confidence at `0.10`, preregister
`EXP-20260831-0005` on frozen v4 before any implementation change, and run one quick with the
unchanged five-bit learner, target moments, representation ridge `0.125`, 32 buckets, cap 8,
local ridge `0.001`, development thresholds, K=`4/6/9`, D=`1`, one runner-random seed, all four
source-identical roles and all nine frozen controls. This is a corrected new test, not a
replication of EXP-0004. A valid negative ends the exact learner without tuning; a conjunctive
positive can authorize only an unchanged three-seed adversarial replication in a later wake.
