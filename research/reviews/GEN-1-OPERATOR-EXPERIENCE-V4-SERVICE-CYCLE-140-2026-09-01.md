# GEN-1 — operator-experience v4, service cycle 140

## Scope

This was exactly one protected service-only cycle. It created no hypothesis,
experiment plan, experimental candidate, scoring seed or scored result and
changed no scientific evidence or confidence. All v1-v7 cohorts, plans,
results, analyses and manifests remain append-only.

## Frozen discriminator

`heldout_mechanism_recombination_v4` measures a missing qualitative signature:
whether experience with raw-distinct executable views permits exact inference
cost to decline at measured exposures 1, 4 and 16. Training uses only anonymous
144-state tables derived from the three already frozen mechanism sources.
Length-one and length-two compositions provide labeled positive re-encoding
pairs and shape-matched pair-breaking negatives. The held-out program is the
unseen length-four composition `CBAC`; primitive names and evaluator canonical
tables never enter the candidate boundary.

Fresh programs differ through node IDs, association, identity insertion and
adjacent-table fusion. A local mutation replaces one leaf, after which the
evaluator tests both the changed program and retention of the original. Every
cell charges acquisition, fit/meta-fit, raw input scan, query, update, bytes,
resident/peak state and R1/R4/R16 workloads.

Seven mandatory controls separate recursive interpretation, raw exact-key
caching, flattened whole-result caching, full composite-table canonicalization,
verified first-order anti-unification, nearest canonical retrieval with exact
fallback, and random leakage detection. Their specifications, implementation
hashes and semantic nodes are registered before any plan.

## Development verification and limits

The complete development matrix has 9 cells per control: K=8/32/128 by measured
exposure=1/4/16. Every exact control obtained minimum combination accuracy 1.0;
random obtained 0.0. The largest state was 40,320 bytes under the 4,194,304-byte
cap. All seven candidate audits, the benchmark-boundary audit and three unique
semantic conformance nodes passed.

The frozen evaluator digest is
`2a2f97b330afccc05ea60fa5137586ffb8c496616f14fe5fa1186d7b2f4ca279`;
the manifest SHA-256 is
`6706d79d49de2527d9f83ad374de1302c0a75e995f84ac3cf19e73a36998f4bb`;
the preflight content digest is
`207076c100b786eb1b123b2f21bc076454d86589bb771939e4ac8b7a672a9fe1`.
The final suite passed 377 tests, integrity verified 546 files, and doctor passed.

This visible synthetic cohort can only select a direction. It cannot promote,
mark promising, authorize replication or increase confidence. The finite tables
make exact execution possible, so any future learned mechanism must beat the
interpreter and canonical controls after full cost rather than claim capability
from a task the evaluator already solves.

## Decision and exact next experiment

Decision: activate v4 for one later cheap scout. In the next wake, create one
low-confidence hypothesis and immutable quick plan before implementing the
source-identical `experience_operator_compiler` and its independent/no-pairing
ablations. One runner-random seed must test all K and exposure cells against all
seven controls. Success requires exact warm, near, mutation and retention
quality, zero false reuse, a measured fall in warm inference cost from exposure
1 to 4 to 16, useful K scaling and implementable Pareto non-dominance. A valid
null or negative ends this exact direction without tuning. A positive scout may
only justify a separately preregistered rigorous test, never promotion.
