# GEN-1 — repository sequence v4 conditional-execution service, cycle 154

## Scope

This was exactly one protected service-only migration from active masked
infilling v3 to repository compression v4. It created no hypothesis, plan,
candidate, scoring seed, result or evidence and did not invoke the runner. It
is no-scoring cycle `1/3` after EXP-20260901-0011. The next wake must score the
conditional-execution scout unless a genuine pre-seed gate fails.

## OBSERVATION

Repository compression v3 preserves the correct real-file evaluator but its
machine-readable contract permanently names the completed reservoir roles.
Using those identifiers for a conditional-execution learner would make the
plan semantically false. V4 directly re-exports v3 and preserves all 43 file
hashes and roles, 367,255 acquisition bytes, anonymous-byte input,
predict-then-reveal execution, K=`8/20/32`, D=`4/16/64`, eight segments per
cell, every metric and cost formula, the 4 MiB state boundary, seed policy,
Pareto axes and all six registered controls.

The only prospective semantic change is the causal-role tuple:
`learned_conditional_execution_byte`,
`source_identical_all_experts_byte` and
`source_identical_frozen_router_byte`. The v4 schema rejects old or mislabeled
roles. A separate regression proves that corpus, training constructor,
evaluator entry point and static contract are exactly the v3 objects. Existing
masked-v3 tests now construct their own explicit historical configuration
instead of assuming that cohort remains globally active.

All 404 tests and six required semantic baseline tests passed. Integrity covers
575 files with evaluator digest
`0ec084ef1aace9db27ec782eaa9c4c6072fc10dfe83976c8976bc2e8d4836e29`;
preflight and doctor passed. No prospective conditional-execution candidate
module exists.

## INTERPRETATION

This migration changes no numerical task. It prevents the next experiment from
misrepresenting a learned router as a reservoir and makes the source-identical
causal comparison machine-checkable. The all-experts role isolates sparse
execution from expert learning, while the frozen-router role isolates learned
routing from a fixed sparse path. PPM-D, CTW and LZ remain strong classical
nulls under unchanged full-cost accounting.

## CONFIDENCE

Confidence is `0.99` that v4 preserves v3 numerical evaluation because every
evaluator object is re-exported and the static contracts compare equal.
Confidence in the prospective conditional-execution hypothesis remains low
until its exact constants are preregistered and a blinded quick completes.

## DECISION

`activate_v4_for_one_conditional_execution_scout`. Do not spend another wake on
review or infrastructure unless a real gate fails. No hypothesis confidence or
scientific evidence changed in this service cycle.

## EXACT NEXT DISCRIMINATING EXPERIMENT

Create HYP-0035 and one immutable quick before implementing any candidate.
Freeze one small expert bank, anonymous context representation, router,
initialization, chronological fit/update rule, active-expert count, learning
rates and a development-only meaningful bpb threshold. The main role uses the
learned sparse router; the source-identical all-experts role executes the full
bank; the source-identical frozen-router role uses the same sparse count without
router updates. All three must share every other source and constant.

Run exactly one runner-random seed over all nine K/D cells against uniform,
unigram, actual PPM-D, actual CTW, bounded LZ and dense autoregressive controls.
Require the preregistered bpb advantage over both causal ablations in every
cell, strictly fewer expert operations than all-experts, K-independent query
work at fixed D, bounded state and implementable Pareto non-dominance after
full acquisition, fit, query, update, bytes and R1/R4/R16 cost. One seed cannot
promote. A valid negative ends the exact router/expert configuration without
tuning.
