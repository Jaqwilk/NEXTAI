# Scope

Cycle 185 was exactly one no-scoring portfolio-identifiability and protected
service cycle. It created no hypothesis, experiment plan, candidate, scoring
seed, result or confidence change. Completed artifacts remain immutable. No
external model, API, dependency, cooldown or second cycle was added.

# Rejected causal-state merger

An anonymous learned causal-state merger on WT v3 was rejected before migration.
It is not distinct from HYP-0040's action-conditioned predictive-state learner
and the already mandatory train-only CSSR and coverage-aware spectral-PSR
controls. Valid EXP-20260901-0024 showed that the learned predictive state lost
aggregate NRMSE to the exact control-level residual bank (`1.258247` versus
`1.007129`), had much worse worst-file NRMSE (`2.682275` versus `1.134350`),
higher R16 work and no implementable Pareto membership. Relabeling the state
partition as a merger would duplicate a falsified, classically covered factor.

# Selected discriminator

The frozen whole-I/O task can distinguish a fundamentally different claim
without new data: whether meta-experience can reduce exact constraint-search
work while leaving the complete MDL solver and its answer unchanged. This is
not a repair of the failed learned latent VM in EXP-20260830-0022. That learner
replaced exact inference with continuous optimization and recovered only 11/48
programs. The prospective v3 role must prove the same optimum as exhaustive MDL.

Three future roles share the complete branch-and-bound solver, objective, tie
handling, constants, support parsing, program execution and output:

- `learned_amortized_constraint_order_vm` uses meta-training only to prioritize
  branches and public constraints;
- `source_identical_support_only_constraint_order_vm` derives priorities from
  the current public support only;
- `source_identical_frozen_constraint_order_vm` uses a frozen order.

The exact enumerative MDL solver remains the decisive classical control, with
random, nearest, dense whole-I/O and privileged latent execution retained.

# Minimal v3 migration

`program_induction_from_whole_io_v3` delegates world/task creation and trial
execution to frozen v2 and adds only prospective roles, K=`16` for a three-point
K matrix, and explicit acquisition, peak state, bytes touched and R1/R4/R16
accounting. V2 and EXP-20260830-0022 are not reinterpreted. The schema freezes
K=`8/16/32`, D=`1/4/6`, Q=`8`, all roles, controls and the source-identity
contract. Reference tests show exact MDL still achieves exact induction and the
new full-cost workloads increase strictly from R1 through R16.

The prior manifest is archived append-only at
`research/manifests/continuous_local_cellular_v3-protocol-v2-f46eeb7907b5.json`
with SHA-256 `c72e9e17e5aaf8141ef1ea4839f43d8eb71df339cd78907d14795e892f9fa602`.

# Verification and decision

All 504 tests passed. Integrity verified 680 files. Evaluator digest is
`99d6a128ee89a923e05bdbb53daf6a6ee0b9e8ad3b2ac08462271620eb21f6e9`,
candidate bundle is
`f634e684458932900267a4600841ca3abcbe5a77810f026cd752381e2015e5ec`,
and preflight certificate is
`db76a2fddeae1fd74b957a15549e1b973770724a9024b8ef7344e8e331a9e21e`.
Integrity and doctor passed with zero pending plans.

Decision: reject the causal-state alias and activate whole-I/O v3 for exactly
one cheap breadth scout. This cycle changes no scientific evidence. It is the
first consecutive no-scoring cycle; cycle 186 must score.

# Exact next discriminating experiment

Cycle 186 must create low-confidence HYP-0047 and immutable
EXP-20260901-0035 before implementing one source-identical exact-search core or
realizing a seed. Freeze the branch-and-bound lower bound, priority features,
meta-fit rule, frozen/support-only orders, node/program-operation counters, tie
handling and a semantic fixture proving identical optimum under deliberately
different orders.

Run one quick over K=`8/16/32`, D=`1/4/6`, Q=`8`. Success requires exact `1.0`
accuracy and executable-program recovery in every cell, fewer cold search nodes
and lower full R1/R4/R16 work than both source-identical ablations and complete
enumerative MDL, no loss at D6, bounded state, and implementable Pareto
non-dominance. A valid negative ends this exact priority rule without tuning; a
positive can only authorize unchanged multi-seed/adversarial replication.
