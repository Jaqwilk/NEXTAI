# GEN-1 — breadth exploration cadence, service cycle 139

## Scope

This was one service-only cycle. It created no hypothesis, experiment plan,
candidate, scoring seed or scored result and changed no scientific evidence or
confidence. The historical v6 migration was audited without rewriting it: its
v5-to-v6 evaluator diff added exactly four recurrent-residual `ROLE` entries and
three source-identical `BASE_IMPLEMENTATION` aliases. Existing regression tests
freeze all earlier role mappings and verify that the four v6 roles use one
implementation and constants while differing only in evaluator-private source
assignment.

## Cadence correction

Cycles 128 through 138 were eleven consecutive no-scoring cycles. They produced
useful negative contract evidence, but exceeded a productive breadth cadence.
The active heartbeat instruction now permits at most three consecutive
no-scoring cycles. After that, the next cycle must either run one cheap
preregistered scout of a fundamentally different mechanism on an adequate
frozen cohort or freeze
the smallest benchmark needed to discriminate it before implementing the
candidate. Scout outcomes may select directions but cannot promote a mechanism
or increase confidence. `AGENTS.md` and `program.md` remain byte-identical
because both are frozen inputs of the historical repository-compression cohort.

## Decision and exact next cycle

Decision: end the audit sequence. Cycle 140 may not be another review, dataset
search or feasibility audit. Existing cohorts do not expose a sequential,
identity-free experience curve that distinguishes learned reusable operators
from exact-key caching or supplied canonicalization. Therefore cycle 140 must be
a protected service-only freeze of the minimal experience-curve operator
acquisition benchmark: mechanically paired but syntactically re-encoded public
observations, pair-breaking negatives, unseen operator compositions, local
mutations, and per-occurrence quality/query/update/state measurements at least
at three exposure counts. It must freeze exact-key cache, whole-result cache,
canonicalization/anti-unification, nearest and random controls. Candidate code,
hypothesis, plan, seed and scoring remain forbidden until a later wake.
