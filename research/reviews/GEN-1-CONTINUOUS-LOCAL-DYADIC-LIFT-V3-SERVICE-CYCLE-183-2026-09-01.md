# Scope

Cycle 183 was exactly one protected service-only breadth selection and role
migration. It created no hypothesis, immutable experiment plan, runner-random
seed, candidate implementation, scored result or confidence change. Completed
v1/v2 plans, results and analyses remain untouched. No external model, API,
dependency, cooldown or second cycle was added.

# Rejected directions before migration

Anonymous hyperdimensional operator binding was rejected before implementation.
The existing operator-algebra and macro-DAG experiments already expose exact
equivalence under runner-random conjugation. Exact tables/interpreters solved
those worlds while learned binding, routing and macro acquisition paid far more
full-system work. This fails the explicit HYP-0005 revival condition that exact
symbolic storage must be inapplicable and privileged cleanup must approach the
frontier. Renaming conjugacy keys as hypervectors would duplicate a dormant,
classically dominated direction.

Recursive probabilistic circuits were also considered. EXP-0040 leaves a real
open question only for heterogeneous distributions with at least two recursive
context/independence levels and strong mixture-Chow-Liu, tensor/decision-diagram
and generic-elimination controls. The current one-selector evaluator cannot
discriminate that claim. Building the necessary evaluator and controls in this
bounded wake would be a new framework rather than a minimal scout preparation,
so the direction remains dormant rather than being tested weakly.

# Selected discriminator

The active continuous-local evaluator already supplies three world scales,
three rollout depths, anonymous OOD amplitudes, damaged inputs, continual
updates and full R1/R4/R16 cost accounting. It can therefore test a different
qualitative claim without new data or metrics: can a data-learned lifted local
operator be composed at dyadic depths while retaining the quality of sequential
application and reducing depth-dependent inference work?

The prospective v3 roles are:

- `learned_dyadic_lifted_local_propagator`;
- `source_identical_sequential_lifted_local_propagator`;
- `source_identical_frozen_lift_dyadic_local_propagator`.

They must resolve to one implementation and share anonymous inputs, lifted
features, constants, rows, initialization, fit/update order and output bounds.
Only dyadic versus sequential composition and learned versus frozen lift are
allowed to differ. This is not a repair of the falsified 39-feature
reaction/exchange rule: no reaction/flux split, antisymmetry basis, repair
threshold or fitted coefficient from EXP-0033 may be reused or tuned.

# Minimal protected migration

`continuous_local_cellular_v3` directly re-exports the frozen v1 evaluator.
V1, v2 and v3 resolve to the same `run_trial`, `run_suite` and world generator.
The migration changes only the active role identifiers, their source-identity
contract and a pre-seed invalidation against horizon-specific fitting or
post-score changes. Data, OOD support, matrices, metrics, directions, thresholds,
cost formulas, budgets and all five controls are unchanged.

The v3-only schema branch freezes its role triple. Regression tests prove that
v1 and v2 retain their historical roles and that v2 roles cannot be substituted
into v3. The prior v2 manifest is archived append-only at
`research/manifests/continuous_local_cellular_v2-protocol-v2-cf138c9b920a.json`.

# Verification and decision

Focused role/evaluator tests passed 21/21. The complete suite passed all 497
tests. Integrity verified 674 files. The evaluator digest is
`cb7587ba06e71d0334360796d0ee217f6f6f4bf490a7c1e32d9f29039d36e48e`,
the candidate-bundle digest is
`444df1ef3a792256685a468d1ac146996199d5e312e8f1037f69ac0318bd2089`,
and preflight certificate
`d3375b8ad8ef45161ee8d5893898311c55238a6c03da505e746264e18f19ff6b`
matched. Doctor passed with zero pending plans.

Decision: reject hyperdimensional binding for this portfolio state, defer the
under-controlled recursive-PC benchmark, and activate v3 for exactly one cheap
breadth scout. This is infrastructure and selection, not scientific evidence.
It is the first no-scoring cycle after EXP-0033; cycle 184 must score.

# Exact next discriminating experiment

Cycle 184 must create low-confidence `HYP-0046` and preregister immutable
`EXP-20260901-0034` before implementing the single lifted-propagator core and
three wrappers or realizing a seed. The plan must freeze the anonymous lift,
lift dimension, regularization, dyadic construction, composition order,
stability bound, update rule, operation/state accounting, a hand-checkable
dyadic-versus-sequential fixture and the one-seed quick policy.

Execute once over unchanged K=`64/256/1024`, D=`4/8/16`, Q=`8` against both
source-identical ablations and all five controls. Require finite stable rollout,
NRMSE better than ridge by the frozen `0.01` margin, no worse damaged-input or
D16 behavior, learned-lift benefit over frozen, materially lower depth-work
slope than the sequential role at matched quality, zero dependence on inactive
K and implementable Pareto non-dominance. A valid negative ends this exact lift,
dimension, ridge and composition rule without tuning. A positive is scout
evidence only and cannot promote.
