# GEN-1 — three-family local-update-law v7 service cycle 126

## Scope and blocking diagnosis

This was exactly one protected service-only migration. It created no hypothesis,
experiment plan, scoring seed or scored result, and implemented no learner. It
did not change any completed plan, result or analysis.

Cycle 125 selected transfer of a learned local update law as the next causal
contrast, but frozen v6 admitted only the four bounded-recurrent-residual role
names. Reusing or replacing those implementations would have reinterpreted
EXP-20260831-0008. Therefore no scientific plan could be registered honestly on
v6; a prospective cohort was required before implementation.

## Minimal migration

`heldout_three_family_continuous_transfer_v7` continues to call the same shared
evaluator. The common role table gained exactly four prospective names:

- `shared_local_update_law_v1` → shared;
- `independent_local_update_law_v1` → independent;
- `cross_family_only_local_update_law_v1` → cross-family-only;
- `support_only_local_update_law_v1` → support-only.

All three ablations resolve to the not-yet-implemented
`shared_local_update_law_v1` module. The only allowed difference is the already
frozen assignment scope of three, one, two or zero source families. No learner
source or constants exist yet and none were selected in this cycle.

For these four new roles only, `update_ops` reports mean support-adaptation work
per test-world session. The complete adaptation work was already charged in
`adaptation_ops` and R1/R4/R16 workload and remains unchanged. Every pre-v7 role
still reports exactly its historical `update_ops=0`. Tests cover all old role
maps, the source-identical new mapping, assignment cardinalities and this
conditional accounting boundary.

Data, whole-world splits, masks, tensor shapes, normalization, loss, family
balancing, horizons, thresholds, acquisition, preprocessing, fit, query,
adaptation, state and workload formulas are unchanged. CLI/gate/schema support
was extended only to recognize v7 and its exact four names. V7 adds `update_ops`
to its universal full-cost Pareto axes; v1–v6 retain their prior axis list.

## Append-only integrity

The prior active v6 manifest remains archived as
`research/manifests/heldout_three_family_continuous_transfer_v6-protocol-v2-5952d0491972.json`
with SHA-256
`b8b1681d493896219372b5635083a3491cb05df1418a0dedf049d84bac3cee39`.
The temporary maintenance and pre-final schema-hardening freezes are retained as
the v7 manifests ending `16742c275ca7.json` and `36cea1b8e22b.json`; neither is a
scientific cohort result. The final active v7 manifest file SHA-256 is
`7f342d6932d822b80bd2cf83f9571660ffc61e06ddeff4240240a03da5e9b604`,
with evaluator digest
`b642acabc10510aa991f0e1fc16de0d9f578e05ac275ed0e0d7a59948addc29d`.
No earlier result is reaggregated or reinterpreted.

## Verification and decision

- focused role/schema/protocol tests: PASS, 50 tests;
- full pytest while v7 was in maintenance: PASS, 372 tests;
- full pytest after final activation: PASS, 372 tests;
- semantic baseline/preflight gate: PASS;
- integrity: PASS, 531 protected files;
- final preflight content digest:
  `1a12a1c397c5ff574f36b6f8b8540580fbdc4b0b6683564554818447c7032d5e`;
- doctor: PASS with active v7 and zero pending plans.

Decision: `activate_for_later_preregistration`. Scientific evidence, all
hypothesis confidences and the 59 completed-experiment count are unchanged.

## Exact next experiment

In the next wake, create one low-confidence hypothesis for transferring only a
family-blind local update law and preregister one runner-random quick on v7 before
implementing the candidate. Freeze one source-identical implementation and
constants across the four causal roles; initialize every test-world predictor
identically and forbid transfer of fitted predictor weights or hidden world
state. Compare against all nine frozen controls at K=4/6/9 and charge acquisition,
fit, every local adaptation, query, state, bytes and R1/R4/R16 workload.

The quick is negative if shared fails to beat independent in any family,
cross-family-only fails to beat support-only in any family, worst-family quality
regresses, stability is below one, or a fixed classical updater Pareto-dominates
it. Freeze a development-only meaningful effect before scoring. One positive
seed may authorize only an unchanged three-seed replication; one valid negative
ends this exact direction without tuning optimizer depth, features, learning
rates or state size.
