# GEN-1 — operator-experience v5 aggregation repair, service cycle 142

## Scope

This was exactly one protected service-only cycle. It created no hypothesis,
experiment plan, seed or scored result and changed no scientific evidence or
confidence. EXP-20260901-0002 and its immutable plan, result, analysis and logs
remain unchanged and scientifically invalid because its required aggregate
metric was absent.

## Minimal repair

`heldout_mechanism_recombination_v5` preserves every v4 source table, split,
term, candidate, control, K value, exposure count, query count, mutation,
formula, direction, budget and seed rule. Its evaluator delegates to the exact
v4 `run_suite` and `static_control_gate`. The common aggregator now carries
`continual_new_fact_accuracy` from trials into candidate summaries, and the
result schema explicitly accepts that bounded summary field.

The benchmark contract now declares the universal Pareto axes. Quality,
stability, full cost, memory and R1/R4/R16 workload participate. The diagnostic
`reuse_coverage` does not: a costly cache cannot avoid domination merely by
covering more keys. The v5 regression matrix proves every declared axis is
non-null, the frontier is non-empty, the lower-cost exact interpreter is on it,
and the more expensive experience compiler is not. This is development-only
service evidence, not a scored scientific result.

## Verification

All 383 tests passed, including the full v4 semantic suite, v5 final-schema and
frontier regression, and serializability of every aggregated summary field.
Seven semantic baseline registrations passed. Integrity verified 553 protected
files and doctor passed. The evaluator digest is
`62a12fd340bfb536ad37c9cf02930cf839b88d75c20440078385dbf2d73b1db4`,
the manifest SHA-256 is
`74805ebd4462df5677dad510d16542e12594cc6edb0c2cca0c294317e84a54aa`,
and the preflight certificate digest is
`a56a4c911fdb4e4409b7dd0342f960b2e7178099885ed28f06422284d1db205f`.

## Decision and exact next experiment

Decision: activate v5 for one unchanged quick rerun of HYP-0030 in the next
wake. Preregister a fresh immutable plan, realize one runner-random seed only
after the candidate audit and unchanged preflight certificate, and use the
unchanged compiler, ablations, controls, constants and criteria from the
invalid EXP-20260901-0002. Do not use the invalid result as a replication
parent or evidence. If the valid run again places the exact interpreter above
the compiler on full-cost Pareto, falsify this exact mechanism without tuning.
A surprising positive remains a scout signal only.

The program remains in breadth mode. Counting this service cycle, no more than
two additional consecutive no-scoring cycles are permitted before a cheap,
fundamentally different scout must run; audit work must support rather than
replace exploration.
