# GEN-1 — three-family recurrent-residual v6, service cycle 122

## Scope and reason for versioning

This was exactly one protected service-only cycle. It created no hypothesis,
experiment plan or scoring seed, implemented no candidate, ran no scoring and
changed no scientific evidence or confidence.

Reactivation of v5 was rejected after inspection because its plan schema freezes
the four predictive-index role identifiers. Reusing those names for a recurrent
residual learner would make the declared algorithm false and prevent honest causal
gain attribution. V6 is therefore the smallest truthful cohort successor.

## Preserved and changed semantics

V6 preserves v5 world generators, whole-world train/test splits, anonymous tensor
contract, training-only normalization, K=4/6/9, depth 1, all nine frozen controls,
metrics, directions, thresholds, state boundary, seed policy, universal Pareto
axes, causal promotion gates and all acquisition/fit/query/update/state/R1/R4/R16
costs. Completed v1-v5 plans, results, analyses, manifests and hashes remain
append-only and are not reinterpreted.

The shared v1 evaluator received only four additive `ROLE` entries and three
source-identical ablation aliases. V6 has a separate wrapper and schema branch.
Regression tests freeze every pre-v6 role and base-implementation mapping and
confirm that wrappers v1-v6 use the same `run_suite`. The four new roles resolve
to one candidate class and identical constants; their only permitted difference
is evaluator-private training assignment: all three families, own family, the
other two families, or no global training worlds. No data, split, normalizer,
metric, cost, threshold or old-role execution path changed.

## Verification and decision

- focused schema, evaluator and semantic-registry tests: PASS;
- semantic baseline gate: PASS, 9 required controls and 10 conformance nodes;
- full pytest after freeze and certificate: PASS;
- integrity manifest: 524 protected files, SHA-256
  `4cc4fd372e4bebfb12f1eec08887f02fd38c9feb224c2a9513169bd54be7d195`;
- evaluator digest: `3e219128c0a00030c60529e1f2549f834b1dc395f78425d2d7e2652c9d61ac5c`;
- preflight content digest:
  `7f85d1882f129a4cdc88ab53e7319a537017c9aec5b0022cd4823e50d21aa43c`;
- preflight file SHA-256:
  `a381773c76cf8283f67e526a5b90f7e21d78ade778d5bcf78612447eae5a478b`.

Decision: activate `heldout_three_family_continuous_transfer_v6` for later
preregistration. No scientific claim follows from this service migration.

## Exact next experiment

Only a later wake may preregister one runner-random quick on v6. It must freeze one
source-identical bounded recurrent residual implementation, ridge 0.001, residual
bound plus/minus four standardized units and the same update law for shared,
independent, cross-family-only and support-only roles. Compare at K=4/6/9 with all
nine frozen controls and full end-to-end costs. Before any seed, freeze the minimum
meaningful effect from development-only data and require strictly positive
shared-vs-independent and cross-family-only-vs-support-only gains in every family,
no worst-family regression, stability 1.0 and universal Pareto non-dominance. A
negative or null quick ends this exact mechanism without tuning; a positive quick
can authorize only an unchanged multi-seed replication in a later wake.
