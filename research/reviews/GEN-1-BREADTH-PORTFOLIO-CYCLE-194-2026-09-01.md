# GEN-1 breadth portfolio reflection — cycle 194

This mandatory twelve-result reflection closes the same bounded cycle that
scored EXP-20260901-0039. It creates no second hypothesis, plan, seed,
benchmark, candidate, or scored experiment. The reviewed result window is
EXP-20260901-0024, 0027, 0029, 0030, 0031, 0032, 0033, 0034, 0036, 0037,
0038, and 0039. Crashes and incomplete controls remain visible but are not
converted into positive evidence.

## 1. What was objectively learned?

Eleven of the twelve screens produced no promotable mechanism. Across real
prequential prediction, masked inference, active sensing, continuous local
rules, whole-I/O search, selection, and invariant residual learning, bounded
or local computation was often attainable but did not preserve matched
quality and full-cost Pareto standing against simpler controls.

EXP-0039 is the first large systematic depth-transfer signal in this window:
the unchanged observation-induced pushdown learner achieved exact-span 1.0
and zero excess bits at every K in {8, 32} and unseen depth in {3, 4, 5}; the
source-identical depth-2 finite-state ablation achieved exact-span 0.0 in all
six cells, and the frozen ablation also achieved 0.0. The strongest completed
implementable control reached exact-span 0.125 at depth 3 and 0.0 at depths 4
and 5. The main learner was implementable-Pareto-nondominated. However, the
mandatory privileged support control crashed on the new noncontiguous mask,
so the preregistered requirement that all mandatory controls complete failed.

## 2. Which assumptions were falsified?

The window falsifies the practical assumption that sparse state, exact local
updates, amortized search, shared factors, population selection, or invariant
residuals are by themselves evidence of a successor principle. Their useful
compute signatures did not survive matched-quality and full-cost comparison.
It also rejects rescue-by-tuning for HYP-0039 through HYP-0049: each frozen
rule received its discriminating screen and should not be revisited through
nearby ranks, regularizers, thresholds, or representation variants.

## 3. Which results replicated?

No new positive result replicated in this window. The dominant replicated
pattern is negative: classical or source-identical simpler controls retain
the quality/cost frontier in diverse cohorts. EXP-0039 used one runner-random
seed and therefore remains a scout signal only; its six K/depth cells are
within-experiment conditions, not independent replications.

## 4. Is the portfolio trapped in one family?

Not by mechanism label: the window spans predictive state, probabilistic
masked inference, active identification, graph/grammar structure, continuous
local dynamics, program search, evolutionary selection, and pushdown memory.
The portfolio is nevertheless at risk of a synthetic-task trap and excessive
evaluator version churn. After the narrowly required v10 repair, no new
benchmark or schema is justified unless the unchanged pushdown rule first
survives the existing holdout and later independent-corpus tests.

## 5. Are we optimizing implementation details rather than principles?

Frequently yes. Many recent cycles repaired contracts or tested specialized
implementations whose qualitative claim failed immediately. The correct
response is not further tuning but one exact repair of the failed mandatory
control interface, followed by the unchanged preregistered rule. The candidate
must remain frozen while the evaluator bug is repaired.

## 6. What result would most change current beliefs?

The strongest belief-changing result would be the unchanged learner retaining
exact OOD-depth transfer and implementable Pareto non-dominance with every
mandatory control complete, then repeating across three independent seeds and
an adversarial corpus with different symbol permutations and unseen nesting
depths. A loss to a complete privileged or simpler implementable control would
instead collapse the current HYP-0050 confidence.

## 7. Which prior work already contains the apparent novelty?

Deterministic and visibly pushdown automata already contain stack-mediated
generalization across nesting depth; PPM/CTW contain variable-context sequence
prediction, and Re-Pair contains grammar-based compression. The current result
does not establish algorithmic novelty over those families. Its narrower open
question is whether the pairing and pushdown roles can be induced from
anonymous observation statistics under symbol permutation at competitive
full cost. No novelty or promotion claim is made, so a fresh literature claim
is not warranted by this incomplete scout.

## 8. Which next test has the highest expected information gain?

Cycle 195 must be service-only: create `heldout_parallel_masked_infilling_v10`
by correcting the privileged conditional target map to align target bytes with
`masked_positions`, add a noncontiguous-mask regression, freeze the cohort,
and run the full semantic/preflight/integrity/doctor gates without a hypothesis,
seed, plan, or scoring. The following wake may preregister one unchanged quick
for HYP-0050. If all controls complete and the scout remains positive, the next
scientific step is an unchanged three-seed replication plus an adversarial
independent corpus; otherwise stop this exact rule without tuning.

## Portfolio decision

Keep HYP-0050 `uncertain` at confidence 0.35. Do not promote, replicate yet,
tune, or reinterpret EXP-0039. Preserve breadth allocation, but spend exactly
one no-scoring cycle on the concrete control-interface defect because it is the
only blocker between the observed signal and a valid discrimination.
