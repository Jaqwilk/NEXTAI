# Scope

This is a protected service-only cycle after valid negative EXP-0048 and EXP-0050. It creates no experiment plan, candidate implementation, scoring seed or scored result. The user-authorized cross-family objective and service-first rule permit a new versioned cohort when the protected mechanism contract must change.

# Evidence motivating the migration

The same width-32 recurrent support/query state failed with two materially different readouts. EXP-0048's linear template readout achieved `0.015625` shared transfer and EXP-0050's fixed fragment dictionary achieved `0.046875`; both had zero minimum-family accuracy, both lost to their source-identical independent fits, and contextual Chow-Liu was the sole implementable Pareto point. Different runner seeds prevent a paired effect estimate, but each experiment independently and decisively failed its own gates. Further recurrent width, decoder or retrieval tuning is therefore stopped.

# Frozen v4 boundary

`cross_family_relation_fragment_transfer_v4` reuses the v2 lossless family-neutral encoder, the same probabilistic, predictive-state, local-dynamics and program-composition generators, training seeds `1103/2207/3301`, runner-random disjoint test worlds, state budget and four semantically registered native suites. The only changed causal mechanism is a graph of at most 64 anonymous relation fragments with `typed_equality_join_then_component_emit_v1` composition.

The pooled learner and independent ablation must be source-identical. They may not receive family labels, native types, field names, oracle objects or test targets. Atom relabeling and anonymous-world permutation must commute with extraction, composition and output. Full-example retrieval without fragment decomposition and an explicit multi-fragment trace invalidates the cohort.

# Semantic discriminator

The protected fixture contains two training fragments and a translated nested query whose correct output has two components. Any lookup returning one complete training example has a one-component output and fails. Future shared and independent candidates must both return the translated two-component output, expose a composition trace of at least two fragments, use the exact capacity and rule, and remain family-blind.

# Accounting and success

Every future v4 plan must declare transfer accuracy, minimum-family accuracy and near-equivalent accuracy, plus acquisition, fit, meta-fit, query, update, resident/peak state, bytes touched and R16 workload directions. Success requires at least `0.95` overall, `0.90` minimum-family, a `0.05` shared advantage on both transfer guardrails and no implementable Pareto dominance. A one-seed quick cannot promote.

# Exact next step

Only after this evaluator is frozen and every end gate passes may the next wake preregister EXP-20260830-0051. Candidate code must not exist before that immutable plan.
