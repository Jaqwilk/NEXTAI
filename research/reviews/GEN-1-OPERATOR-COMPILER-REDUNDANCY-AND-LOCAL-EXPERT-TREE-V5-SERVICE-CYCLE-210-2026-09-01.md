# GEN-1 — operator-compiler redundancy gate and local-expert-tree v5 service, cycle 210

## Scope

This was exactly one protected service-only cycle. It created no hypothesis, experiment plan, candidate implementation or scoring seed and called no scoring runner. Completed plans, results, analyses and evaluators remain append-only.

## Observation

The proposed shared observation-learned intervention-operator compiler is not a new causal discriminator on the existing mechanism-recombination worlds. `experience_operator_compiler` already learns behavioral probes from public operator examples, installs locally after observations and mutations, composes unseen anonymous operators and transfers across fresh encodings. Valid `EXP-20260901-0003` was exact but full-cost dominated by the exact interpreter. `learned_macro_operator_dag` already learns and reuses composed operator subcomputations across depths 4/8/16; valid `EXP-20260901-0021` was exact but saved only 14 warm operations and had worse depth scaling and R16 workload than the interpreter. The cycle-206 equation-rewrite audit independently found the narrower algebraic variant redundant or vacuous. Another compiler role would therefore repeat a falsified observable primitive rather than add breadth.

## Minimal successor

Activated `continuous_local_cellular_v5` as a role-only wrapper over the byte-identical v1 evaluator. It preserves all 384 training transitions, OOD amplitude split, anonymous channel encoding, corruption cases, K=`64/256/1024`, D=`4/8/16`, Q=`8`, targets, metrics, costs, thresholds and five frozen controls. V5 adds only three prospective role identifiers:

- `error_novelty_split_local_expert_tree_v1`;
- `source_identical_shuffled_split_local_expert_tree_v1`;
- `source_identical_frozen_partition_local_expert_tree_v1`.

The three roles are contractually one implementation with identical anonymous features, constants, initialization, fit order, prediction, update, output bounds and accounting. Only the error/novelty-aligned, preregistered shuffled or frozen partition decision may differ. Candidate files remain absent. Constants and the development-only meaningful-effect rule must be preregistered before implementation in the next wake.

This successor is observationally distinct from the failed fixed random program population and from the frozen eight-nearest-row kernel control: it tests whether data-triggered partition growth plus one-leaf local updates discovers reusable local operators without scanning or refitting the complete training set.

## Verification

- Historical v12 source-corpus hashes remained valid after moving the new regression to a separate test file.
- Historical WT v4 protocol tests are now independent of whichever cohort is active; no WT evaluator or role behavior changed.
- Five mandatory semantic baselines and six unique conformance nodes: PASS, including the real evaluator cell.
- Focused continuous-local, historical-corpus and WT regressions: PASS.
- Full pytest: PASS, 562 tests.
- Protected manifest: PASS, 733 files.
- Preflight certificate digest: `8d76c7d6dd7f07f575c4b8894c9435e1b7d78703a135cdd01020145dcdf7136a`.
- Evaluator digest: `af865df28ad29228ae185f24972b2320e1bd9dbb03973906a21e6e25455b99fa`.

## Decision and next discriminator

Reject the intervention-operator compiler alias without implementation. Keep the v5 local-expert-tree roles as a prospective breadth scout only; this service cycle changes no evidence or confidence.

Cycle 211 must score. Preregister expected `HYP-0053` and `EXP-20260901-0049` before candidate implementation, freeze one bounded error/novelty-split tree and its shuffled/frozen source-identical wrappers, pass hand-checkable split, permutation, local-update and cost fixtures, then run exactly one runner-random quick on v5. Compare all three roles with persistence, affine ridge, quantized FSM, eight-nearest kernel event and privileged support. Require at least `0.01` aggregate NRMSE improvement over both ablations and the strongest complete implementable control, the same direction at every K for D8/D16, stable rollout, no worst-scale or damaged-input regression, one-leaf update accounting, three-scale workload reporting and implementable Pareto non-dominance. A negative ends the exact tree rule without tuning; a positive is only a scout signal and cannot promote.
