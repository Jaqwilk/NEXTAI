# GEN-1 — macro-operator DAG v6 migration, service cycle 161

## Scope and portfolio decision

This was exactly one protected service-only breadth cycle. It created no
hypothesis, experiment plan, candidate, scoring seed or scored result and did
not change evidence or confidence. The preceding EXP-20260901-0020 was a valid
negative result; it was not rerun or tuned.

The selected direction is deliberately narrower than a new framework and
causally different from HYP-0030. HYP-0030 learned a whole-operator behavioral
fingerprint and lost to exact interpretation on full cost. The prospective v6
mechanism instead learns reusable partial-evaluation subgraphs (macro DAGs) and
is judged primarily on whether total charged work crosses below interpretation
as unseen composition depth grows. It may not use primitive names, source
families, native types, manual ontology, an external model or an API.

## Minimal protected migration

`heldout_mechanism_recombination_v6` reuses the frozen v4/v5 source tables,
length-one/two training programs, anonymous term encoding, seven controls,
K=8/32/128, eight queries, metric formulas, state budget and runner-random
conjugation. The only evaluator generalization is an optional test sequence;
the historical default remains `CBAC`, and a regression test proves the old
deterministic output and operation accounting are unchanged.

V6 freezes three genuinely held-out program lengths: `CBAC` (4), `CBACABAC`
(8), and `CBACABACBCABCBAC` (16). Every cell performs sixteen fresh
re-encoding exposures. The future shared, rebuild and frozen-macro roles are
bound by one source-identical contract and may differ only in preregistered
cross-query macro learning and reuse. No implementation of those roles exists
in this service cycle.

## Verification and decision

The real-control fixture completed all three depths exactly and input work rose
strictly with depth. The historical-default regression, v6 contract/schema
test, 27 focused semantic tests and all 422 repository tests passed. Seven
mandatory semantic baselines passed. Integrity verified 595 protected files;
the evaluator digest is
`0545b5b00535213af901639fb2d578bb2152c22c6b7273f4c273e63c6ab595e7`.
The preflight certificate digest is
`a7945f69f6303ec8a9301d73a066876f99c46ab9d992a0ae2eb5ed81453cddbc`,
and doctor passed. V6 is active. This is the first consecutive no-scoring cycle
after EXP-20260901-0020, below the hard maximum of three.

Decision: activate v6 for exactly one cheap scout in the next wake. Create
HYP-0038 and immutable EXP-20260901-0021 before implementing the three
source-identical roles, then realize exactly one runner-random seed and score
the complete K-by-depth matrix through the audited harness. Freeze the macro
selection rule and all constants before scoring. Require exact warm, near,
mutation and retention quality with zero false reuse; a strictly better
depth-compute slope than the exact interpreter; and full R16 work below the
interpreter at depth 16 after acquisition, fit, query, update, bytes and state
are charged. A valid negative ends this exact macro rule without tuning. A
positive scout only authorizes unchanged multi-seed replication and cannot
promote.
