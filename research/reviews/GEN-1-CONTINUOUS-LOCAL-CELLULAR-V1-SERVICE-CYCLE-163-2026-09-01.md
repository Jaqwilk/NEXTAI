# Continuous local cellular v1 — service cycle 163

## OBSERVATION

EXP-20260901-0021 validly rejected the exact macro-DAG rule and reset the no-scoring cadence. The existing `continuous_event_predictive_state_v1` varies irrelevant channels but does not expose inactive spatial area or local repair. `nonlinear_local_state_transfer_v1` exposes sparse scheduling and repair, but its complete discrete transition table let exact finite-state propagation reproduce the learned NCA more cheaply. Neither cohort discriminates the recorded revival condition for HYP-0006: continuous non-exhaustive local-state learning with emergent repair and a cost signature tied to the active causal cone.

## SERVICE CHANGE

Activated new cohort `continuous_local_cellular_v1`. It freezes 384 clean one-step continuous neighborhood observations with latent fit amplitude at most `0.65`, runner-random anonymous signed channel permutations, held-out impulse amplitudes `0.85–1.10`, ring sizes `64/256/1024`, depths `4/8/16`, and eight queries per cell. Targets remain evaluator-private. A one-channel damaged view is scored against the clean target. The active causal cone grows with depth while dormant area grows with K.

The future learned sparse, source-identical dense and source-identical frozen roles are named but not implemented. Mandatory controls are persistence, raw local ridge, quantized finite-state lookup, kernel event simulation and evaluator-only privileged support. The schema requires every role and the exact matrix. Universal Pareto axes include quality, repair, stability, acquisition, fit, query/update work, state, bytes and R1/R4/R16 workload. The meaningful NRMSE margin is frozen at `0.01`, more than five times the maximum deterministic development perturbation response.

## INTEGRITY

No hypothesis, experiment plan, scoring seed, candidate or scientific result was created. No scoring occurred. Full pytest passed `431` tests. The frozen evaluator digest is `8e0cf515bfc7f7acfc785a85959f4ce32791a8e6425536b769e23f4849ce53ba`; integrity verified `602` files; preflight certificate digest is `72422fe106811847c774d9f3696120020773440506d90d6502708146591101ca`; doctor passed. Historical manifests and all experiment artifacts remain append-only.

## DECISION

Activate v1 for exactly one breadth quick. This is service infrastructure, not evidence for HYP-0006, and cannot change confidence or authorize promotion.

## NEXT DISCRIMINATING EXPERIMENT

In the next wake, create one low-confidence HYP-0039 and immutable EXP-20260901-0022 before candidate implementation. Freeze one source-identical rule: learn anonymous-channel consistency from clean observations, fit one bounded quadratic local transition, execute it with sparse active-cone scheduling; dense and frozen ablations differ only in schedule or learning. Compare all five frozen controls at K=`64/256/1024`, D=`4/8/16`, Q=`8`, one runner-random seed. Require stable finite output, candidate accuracy at least `0.95`, at least `0.01` NRMSE improvement over ridge and kernel controls, damage recovery above the frozen ablation, lower K-query slope than dense execution, retention after one local update and universal Pareto non-dominance. A valid negative ends this exact rule without tuning; a positive authorizes only unchanged multi-seed replication.
