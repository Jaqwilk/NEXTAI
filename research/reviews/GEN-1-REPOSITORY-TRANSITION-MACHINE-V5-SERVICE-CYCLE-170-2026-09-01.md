# Repository transition-machine v5 service cycle 170

## Observation

This was exactly one protected service-only cycle. It created no hypothesis,
experiment plan, candidate, scoring seed or scored result. The active cohort is
now `heldout_repository_sequence_compression_v5`. Its evaluator directly
re-exports v4, preserving all 43 frozen whole-file hashes and roles, anonymous
byte access, predict-then-reveal execution, K=`8/20/32`, D=`4/16/64`, Q=`8`,
metrics, full-cost formulas, budgets, seed policy, Pareto axes and all six
classical controls. Historical v1-v4 artifacts and the prior masked-v5 manifest
remain append-only.

Only the prospective causal role contract changed. A future quick may compare a
surprise-gated transition machine with source-identical dense-clock and
frozen-transition ablations. The three roles must share state width, transition
map, constants, initialization, chronological data order, context, input,
reveal-update and output rules except for the preregistered surprise gate, dense
clock and transition-learning interventions. Before seed realization, focused
fixtures must prove predict-before-reveal ordering, byte-relabeling equivariance,
sparse no-event state preservation, dense execution on every reveal and explicit
event/dense update cost.

Six registered controls passed their semantic gate, including reference
variable-order PPM-D and recursive-mixture CTW tests plus the fixture that rejects
a first-order Markov impostor. A development-only real-file smoke ran every
control through v5 at K=8, D=4, Q=1 and produced five complete file rows per
control. All 461 tests passed. The manifest froze 632 protected files; integrity
and doctor are required again after the append-only records are written.

## Interpretation and uncertainty

The cohort can discriminate whether sparse learned state transitions improve
held-out byte prediction and charged update/query work over a dense-clock version
of the same rule. It uses real unseen repository files rather than another toy
world and is mechanistically different from the failed learned energy iteration.
However, held-out byte contexts alone do not establish systematic operator
composition, and sparse event gating may collapse to ordinary change detection
or lose useful low-surprise updates. Confidence is deliberately low before any
candidate or score exists.

## Decision and next discriminator

Keep v5 as the active, frozen breadth cohort. This is the first consecutive
no-scoring cycle after EXP-20260901-0027, and the next wake must score rather than
perform another audit. In that wake, create HYP-0041 and preregister immutable
EXP-20260901-0028 before implementing anything. Freeze one exact state width,
surprise statistic and threshold from a development-only fixture, one transition
map and update rule, and identical input/output/cost accounting for all three
roles. Then run exactly one runner-random quick seed over the full K/D/Q matrix
against all six controls. Success requires a frozen meaningful bits-per-byte gain
over both source-identical ablations and the strongest complete implementable
control, no worst-file or cold regression, lower update/query work than the dense
role, bounded state, fixed-D K-independence and implementable Pareto
non-dominance. A valid negative ends this exact rule without threshold or width
tuning; a positive authorizes only unchanged multi-seed replication.

Machine-readable check: `research/checks/repository_transition_machine_v5_service_cycle_170.json`
(`79a9a737a7f3c24a74356055269216b09185300079ca31956cf02f5839fa8952`).
