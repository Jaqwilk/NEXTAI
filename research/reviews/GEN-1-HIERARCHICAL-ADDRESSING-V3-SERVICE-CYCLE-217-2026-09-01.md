# Hierarchical addressing v3 service — cycle 217

## OBSERVATION

Start gates passed with no STOP, PAUSE, run lock, pending plan or integrity
problem. `latent_entity_binding_retrieval_v2` could not validly preregister the
next proposed mechanism: its plan generator and hashed
`entity_addressing_protocol` still named the completed flat-address roles from
EXP-0054. Supplying new hierarchical candidate names would therefore make the
candidate list disagree with the plan's own causal contract.

The service migration created `latent_entity_binding_retrieval_v3`. It reuses
the v2 world generator and execution path without changing raw observations,
bursts, held-out views, K=`32/320/3200`, D=`1/4/8`, Q=`16`, insertion case,
metrics, directions, costs, budgets or seed policy. V2 and EXP-0054 remain
immutable. V3 freezes four new prospective role identifiers around one future
node-conditional hierarchical-routing core; none of those four candidate files
exists in this cycle.

The new mandatory `raw_balanced_kd_tree_v1` control is implemented and
semantically registered. It recursively selects the maximum-variance raw
coordinate, uses stable median splits, performs exact lower-bound-pruned nearest
neighbour search and inserts one record along one path without rebuilding. On
the fixed development world it matched full raw scan for all tested D=`1/4/8`,
used multiple node-local axes, preserved exact new-fact accuracy and changed
exactly one existing child pointer on insertion.

No hypothesis, experiment plan, plan-registry entry, runner seed, main
candidate implementation, scored result or confidence change was created.

## INTERPRETATION

This is an infrastructure and control correction, not evidence for or against
HYP-0012. A separate v3 cohort is necessary because changing the old plan
contract in place would reinterpret EXP-0054. The exact raw k-d tree prevents a
future conventional metric-tree effect from being attributed to representation
learning. The proposed learned mechanism remains discriminable only if it uses
sequential node-conditional partitions and cannot be reduced to the failed
global 16-bit threshold code.

## DECISION

Activate `latent_entity_binding_retrieval_v3`. Keep v2 and its flat-address
result append-only. Permit no v3 scoring until one immutable plan freezes the
four source-identical roles, pair loss, recursive median construction, beam
width four, 64-node cap, zero fallback, local path-only insertion, full reuse
curve and matched-quality gates before the absent main candidate is implemented.

## VALIDATION

- Cohort v2+v3 focused tests: 19 PASS before freeze.
- V3 focused tests plus repository-wide source audit: 7 PASS after the only
  audit correction (the shared baseline core received the required `Candidate`
  export).
- Semantic baseline gate: six unique nodes PASS for raw k-d tree, raw full
  scan, local dense GRU and privileged exact key.
- Full pytest: 592 PASS.
- Evaluator digest:
  `30160e664a5652fb5071b3ce83d8fa4f6ce20e497114f1a8e98adaef381707ba`.
- Candidate bundle digest:
  `56d55b1b2b5707a8489cf8bed5ad9bf42ef16f6361b886691751d245b48f2a5d`.
- Preflight certificate:
  `726dc605a8e1be99f281cda8f4459ed31c5f2b0d1f198aa28723c579cc8085a8`.
- Integrity: PASS over 762 protected files. Doctor: PASS.

## NEXT DISCRIMINATING EXPERIMENT

Cycle 218 may create `research/plans/EXP-20260901-0055.json` under HYP-0012
and no parent. It must name exactly:

1. `learned_balanced_hierarchical_router_v1`;
2. `source_identical_dense_hierarchical_representation_v1`;
3. `source_identical_frozen_hierarchical_router_v1`;
4. `source_identical_shuffled_hierarchical_router_v1`;
5. `raw_balanced_kd_tree_v1`;
6. `raw_nearest_neighbour_scan_v1`;
7. `local_dense_transition_gru_v1`;
8. `privileged_exact_entity_key_v1`.

The quick uses one runner-random seed and the frozen v3 K/D/Q matrix. Before
candidate code it must state the exact EXP-0054 pair loss and fixed training
order, balanced node-local maximum-variance median tree, beam four, 64 visited
nodes per step, squared learned-distance verifier, no fallback and path-only
insert. Success requires accuracy within `0.02` of dense, strict advantage over
frozen and shuffled at every K, new-fact accuracy `1.0`, R4096 K slope below
`0.30`, a prospectively declared matched-quality break-even and implementable
Pareto non-dominance. A negative closes this exact hierarchy without tuning; a
positive only authorizes unchanged three-seed replication.
