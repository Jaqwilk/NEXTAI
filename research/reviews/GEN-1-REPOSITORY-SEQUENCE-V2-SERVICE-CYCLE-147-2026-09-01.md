# GEN-1 — repository sequence v2 protected service, cycle 147

## Scope

This was exactly one protected service-only migration from
`heldout_wt_changepoints_prequential_v1` to
`heldout_repository_sequence_compression_v2`. It created no hypothesis,
experiment plan, candidate under test, scoring seed, result or scientific
evidence. It did not invoke `nextai run`.

This is consecutive no-scoring cycle `2/3` after scored cycle 145. The next
wake must preregister and execute the cheap layer-local credit-assignment scout
unless a real start, integrity or pre-seed gate fails. Another voluntary audit
or design-only cycle is not allowed.

## OBSERVATION

The v2 evaluator reuses the v1 evaluator implementation and all 43 immutable
whole-file records byte for byte: 33 training, 5 validation and 5 test files,
367,255 acquisition bytes, the anonymous byte interface, predict-then-reveal
ordering, 128-byte segment multiplier, loss formulas, state limit and full-cost
accounting. V2 freezes K=`8/20/32` KiB, public context D=`4/16/64` and eight
128-byte query segments per cell.

The audit found that historical unmasked candidates named `ppm_byte` and
`context_tree_weighting_byte` implemented heuristic Markov mixtures rather
than the exact registered algorithms. Their immutable v1 result remains
untouched. V2 uses new unambiguous IDs, `ppm_d_order5_byte` and
`ctw_depth2_byte`, backed by the existing hand-checked PPM-D and multinomial
CTW reference implementations. PPM-D fixes order 5, escape-D, full exclusion,
longest-suffix backoff and update order. CTW fixes raw 256-byte symbols, depth
2, KT(1/2), recursive half local/half child-product weighting and bottom-up
finalization.

Six controls are now semantically registered and checked before a scoring seed:
uniform, KT unigram, PPM-D, CTW, bounded LZ dictionary and dense lag-product
autoregression. Reference tests show PPM variable-order backoff, CTW recursive
mixing and failure of a deliberately first-order model to satisfy the PPM/CTW
fixtures. Every control completed a real frozen-file development cell. A
complete-control Pareto frontier remained nonempty after adding a recorded
timeout row.

The prospective neural roles are frozen only as identities and causal scope:
`layer_local_goodness_byte`, source-identical end-to-end gradient, and
source-identical frozen hidden. Their topology, constants, initialization,
data order, update count, input representation and output rule must be exactly
the same; only credit assignment may differ. No neural implementation exists
yet, as required before preregistration.

Full validation passed: 388 tests, six semantic baselines, 557-file integrity,
preflight certificate and doctor. The evaluator digest is
`a5207b25760a74c1bd18839e40baba4069370ecbf8fbaf89920b2d086423ff7e`.

## INTERPRETATION

The migration supplies a narrow discriminator for the selected portfolio gap:
hidden-layer learning without a reverse credit path. It does not introduce a
new corpus or reinterpret EXP-0044. Strong actual universal coders make the
system-level null credible, while the two source-identical neural ablations
separate useful hidden learning from ordinary global credit and a fixed random
representation.

## CONFIDENCE

Confidence is `0.97` that v2 preserves the intended v1 data and public
prediction boundary, `0.95` that the pre-seed baseline gate now distinguishes
the named PPM-D/CTW algorithms from first-order approximations, and only `0.15`
that the future layer-local scout will survive matched global credit and the
classical frontier. No hypothesis confidence changed in this service cycle.

## DECISION

`activate_v2_for_one_layer_local_credit_assignment_scout`.

The active cohort is `heldout_repository_sequence_compression_v2`. Historical
v1 plans, results, analyses and manifest are preserved append-only. No scoring
occurred and no result can be inferred from maintenance checks.

## EXACT NEXT DISCRIMINATING EXPERIMENT

Cycle 148 must create low-confidence HYP-0032 and one immutable runner-random
quick plan before implementing any neural role. The plan must freeze a single
small byte network, fixed-width family-neutral representation, initialization,
optimizer/update count, deterministic train-only wrong-byte construction and a
development-only meaningful bits-per-byte effect. The three neural roles must
share all source and constants and differ only in layer-local goodness,
end-to-end gradient or frozen-hidden credit assignment.

Run all three roles and all six registered controls at every K/D cell through
the audited harness. Success requires layer-local mean, cold and worst-file
bits per byte within the frozen meaningful effect of global credit in every
cell, strict improvement over frozen hidden, lower fit operations or peak fit
state than global credit without higher query/resident state, and universal
Pareto non-dominance. If global credit passes and layer-local fails, discard
the exact rule without tuning. If both neural routes fail or a simpler
classical control dominates capability and full cost, end the direction. One
positive seed can only authorize unchanged three-seed replication.
