# Post-hierarchical-routing portfolio correction — cycle 221

## Scope

This is one append-only service cycle after valid negative
EXP-20260901-0056. It creates no hypothesis, experiment, plan, scoring seed,
candidate, evaluator version, or scientific evidence. It does not edit the
completed plan, result, analysis, or logs of EXP-0056 or EXP-0031.

## Observation: the recorded next experiment is redundant

The EXP-0056 analysis proposed a learned posterior-partition decision DAG on
`heldout_raw_sensor_active_identification_v2`. That exact prospective family
has already received a valid, auditable negative quick in EXP-20260901-0031:

- immutable plan file SHA-256:
  `3324493d15c7048d7dcb5e82582cad63f1491bee4f234a24a80976d28269363f`;
- result SHA-256:
  `a9fa610881c25ec7370956c53ef00a73ed75cb9b46afaebb73cb9de285a6ac1e`;
- analysis SHA-256:
  `568743122bbc2d7fe0a85c48e58be3d386e81c68e9f6ed72e1ff81972a4ebb22`.

In all nine recorded cells, learned shared, support-only, and frozen-policy DAG
roles each scored 250/288 = 0.868056. Gaussian and kernel controls reached
0.930556 and 0.920139 respectively. Learned routing reduced query work but did
not match the stronger controls' quality; observe-all remained on the
capability-gated frontier. HYP-0043 was therefore falsified at confidence 0.02.

This is a selection correction, not a reinterpretation of EXP-0056 and not a
new scientific result. HYP-0012 confidence and evidence must remain unchanged.

## Portfolio interpretation

Re-running raw-sensor DAG, tuning the closed flat/hierarchical address family,
or relabelling previously tested operator-algebra mechanisms would spend a
wake on an already answered question. The existing portfolio also contains
direct screens of recurrent residuals, relation fragments, operator closure,
population programs, invariant modules, verified indexes, eligibility traces,
surprise gates, and partial macro DAGs. A new quick should therefore test a
source-identical learned mechanism with a distinct causal intervention rather
than another benchmark-specific rule.

## Selected prospective mechanism

The next scientific direction is a **permutation-equivariant induced sparse
set memory** shared without code or hyperparameter changes across the four
already frozen cross-family worlds. It must consume only their existing
lossless anonymous public serialization. It may not inspect family labels,
native Python types, paths, class or field names, semantic channel names, test
outcomes, or hand-written ontologies.

This is not a novelty claim. Set Transformer inducing points, product-key
memory, and learned sparse routing are established prior art (SRC-0216 through
SRC-0218). The discriminating question is whether one local learner transfers
across unseen worlds at matched quality and better full-system cost than dense
source-identical attention and the frozen classical specialist suites.

### Required source-identical roles

1. `shared_sparse_set_memory_v1`;
2. `independent_sparse_set_memory_v1`;
3. `source_identical_dense_set_attention_v1`;
4. `source_identical_frozen_sparse_router_v1`;
5. the existing contextual Chow-Liu, empirical-joint and autoregressive
   specialist suites;
6. the existing privileged-support control, labelled as privileged rather
   than implementable evidence.

Shared, independent, dense, and frozen-router roles must use the same lossless
tokenization, embedding width, set encoder, memory width, initialization, fit
order, query/update/output path, and accounting. The shared/independent
intervention may change only pooled versus per-world training state. The
sparse/dense intervention may change only top-k memory access versus access to
all memory slots. The frozen-router intervention may change only whether the
routing keys are learned. No role may receive a family identifier.

### Frozen evaluation boundary

Reuse the exact four training/test world families, splits, public views,
quality metrics, K/D/Q cells, horizons, update events, cost terms, memory terms,
and specialist controls already frozen by `cross_family_shared_representation_v3`.
Do not create new data or change a metric formula. Because the current schema
hard-codes recurrent-residual role IDs, introduce a disjoint role-only cohort
named `cross_family_sparse_set_memory_v5`; the underlying evaluator and worlds
should be reused byte-for-byte wherever possible.

The eventual immutable quick must freeze all numerical constants before
candidate implementation. At minimum it must require: overall accuracy at
least 0.95; minimum-family accuracy at least 0.90; shared-minus-independent
gain at least 0.05 both overall and in the worst family; learned sparse better
than the frozen sparse router; quality within 0.02 of source-identical dense
attention; structurally fewer accessed slots and bytes than dense attention;
no worse full R16 cost than dense attention; complete stability; and
implementable Pareto non-dominance. A single positive seed can authorize only
an unchanged three-seed replication. A negative closes this exact rule without
post-result tuning.

## Decision and exact next wake

**Decision: correct the portfolio direction; do not score in cycle 221.** This
is consecutive no-scoring cycle 1 after scored cycle 220.

Cycle 222 must be exactly one protected, service-only activation of
`cross_family_sparse_set_memory_v5`: add only the disjoint role contract and a
thin evaluator/config wrapper over the frozen cross-family v3 worlds; prove
old v1-v4 roles are unchanged, mixed role contracts are rejected, anonymous
public views contain no family labels, and all specialist semantic gates still
pass. It must create no hypothesis, plan, seed, candidate, or scoring result.
Only after full pytest, preflight, integrity, and doctor PASS may cycle 223
preregister and score one bounded quick under a new hypothesis. Another
design-only wake after that migration is not permitted unless integrity fails.
