# GEN-1 Pareto correction and prospective WT predictive-state v3 — cycle 165

This was one bounded service-only cycle. It created no hypothesis, immutable
plan, candidate implementation, scoring seed or scored result. Completed
experiments remain `72`.

## Append-only correction to EXP-20260901-0022 interpretation

The completed EXP-0022 result and analysis remain immutable. A source-level and
synthetic execution check disproved one sentence in the later interpretation:
the runner already removes privileged candidates before calling Pareto. With a
globally dominating `privileged_*` row plus two implementable quality/cost
trade-offs, both implementable rows remain on the frontier. With implementable
accuracies `0.208333` and `0.263889`, the frontier is empty because the frozen
screen capability gate is `0.95`.

Therefore EXP-0022 has no frontier bug. Its negative scientific decision is
unchanged and strengthened: no implementable candidate reached the minimum
capability required for Pareto evidence. No runner code was modified.

## Prospective v3 contract

The service prepared `heldout_wt_changepoints_prequential_v3` as a strict
re-export of WT v2 numerics. It preserves the ten frozen file hashes, split,
normalization, evaluator, artifact-before-reveal boundary, K/H matrix, metrics,
costs and eight historical baselines. The prospective schema changes only the
three future roles to action-conditioned predictive state, frozen predictive
state and observation-history ablations, and names coverage-aware spectral PSR
and train-only discretized CSSR as additional mandatory controls.

Focused tests passed, but preflight correctly refused activation because those
two new controls do not yet have implementations, semantic registry records and
discriminating fixtures. V3 therefore remains inactive maintenance. The active
benchmark stays `continuous_local_cellular_v1`; no completed cohort is changed
or reinterpreted.

## Exact next cycles

Cycle 166 is the second necessary no-scoring service wake: implement only the
two controls, prove their named semantics on hand-checkable train-only fixtures,
register exact hashes, activate WT v3, and require full pytest, preflight,
integrity and doctor PASS. Do not create HYP-0040, a plan or a seed.

Cycle 167 must preregister and score exactly one HYP-0008 predictive-state quick
on WT v3. Freeze rank, history/future windows, control conditioning, update law
and effect threshold before implementation. A valid negative ends that exact
rule without tuning; a one-seed positive only authorizes unchanged replication.
