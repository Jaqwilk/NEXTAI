# Scope

Cycle 181 was exactly one protected service-only breadth selection. It created
no hypothesis, immutable experiment plan, scoring seed, tested candidate,
scored result or confidence change. Completed v1 plans, results and analyses
remain untouched. No external model, API, dependency, cooldown or second cycle
was added.

# Selection from existing evidence

The most recent exact recursive equality grammar was a valid negative and may
not be tuned. The earlier continuous-local scout was also a valid negative: its
quadratic monolithic update preserved the desired sparse workload signature but
lost quality to ridge. Repeating that representation, repair threshold, ridge
or update law would be optimization of a falsified exact rule.

The existing continuous-local evaluator can nevertheless distinguish a
different structural claim without a new benchmark. It already supplies
anonymous continuous observations, OOD amplitudes and depths, three inactive
world scales, continual updates, a damaged input channel, full cost accounting
and classical local controls. The selected next mechanism is therefore a
learned factorization of local evolution into a center reaction and
antisymmetric neighbour exchange. Its causal controls are a source-identical
monolithic local flow and frozen learning. This is a test of decomposed local
interaction structure, not another feature/ridge variant, and uses no family
label, channel meaning, latent coordinate or hand-written world ontology.

# Minimal role-only migration

`continuous_local_cellular_v2` re-exports the v1 evaluator. V2 preserves the
world generator, training/scoring supports, splits, anonymous encoding,
matrix, seeds policy, metrics, directions, thresholds, cost boundary, budgets
and all five mandatory controls. Only three prospective role identifiers and
their source-identity/invalidation wording changed. The v1 schema branch now
freezes the historical role triple explicitly; the v2 branch freezes the new
triple. Regression fixtures prove that v1 rejects v2 substitution and v2
rejects v1 substitution, while both wrappers resolve to identical evaluator
objects.

No implementation exists for any new role in this service cycle. Exact
factorization, constants, update order, development-only fixtures, seed policy,
invalidations and success gates must be preregistered before implementation or
seed realization.

# Verification and decision

The two continuous-local test modules passed all 15 tests, including existing
candidate/control semantics and the new history/identity fixture. The complete
suite passed all 491 tests. Integrity verified 668 frozen files. The evaluator
digest is `2037fdebd9dbca996bb9a61bd668ab3007293c19adb9d5780ff19a0ff41b28cf`,
the candidate bundle digest is
`35d5717bdee442edd29690d0f6c6db42fca93febbd18f6ee19a7e892019fe4f7`,
and preflight certificate
`3164d4ffd508451169e674edccc943d6dee24f02698b632c64c2e286605da73b`
matched. Doctor passed with zero pending plans.

Decision: activate v2 and preserve v1 append-only. This is infrastructure and
selection, not scientific evidence. It is the first no-scoring cycle after the
scored cycle 180, and the user-mandated exploration rule makes the next wake a
scoring wake.

# Exact next discriminating experiment

Cycle 182 must create low-confidence `HYP-0045` and preregister immutable
`EXP-20260901-0033` on v2 before implementing any tested role or realizing the
runner-random seed. Execute exactly one quick comparing the learned factorized
reaction/exchange learner with its source-identical monolithic and frozen
ablations plus all five controls. Freeze the exact anonymous representation,
antisymmetry construction, constants, update order, development-only semantic
fixtures and a meaningful NRMSE margin before scoring. Require stable finite
rollout, quality beyond ridge/kernel by the frozen margin, benefit over both
ablations, no worse damaged-input or worst-depth behavior, a sparse query-cost
slope near zero across K=64/256/1024, and implementable Pareto non-dominance.
A valid negative ends this exact factorized mechanism without tuning; a
positive remains scout evidence only and cannot promote.
