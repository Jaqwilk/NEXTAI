# GEN-1 — WT multiresolution lifting v2 service, cycle 156

## Scope

This was exactly one protected service-only migration from repository sequence
compression v4 to `heldout_wt_changepoints_prequential_v2`. It created no
hypothesis, plan, candidate, seed, result or evidence and did not invoke the
scored runner. It is no-scoring cycle `1/3` after EXP-20260901-0013. The next
wake must score the bounded scout unless a genuine pre-seed integrity gate
fails.

## OBSERVATION

The previously proposed asynchronous temporal-binding direction was already
tested by EXP-20260830-0028. Its learned polychronous binder was exact but was
deterministically dominated by the timed-automaton control: query operations
were 14.55% higher, update operations 39.34% higher, state 35.29% higher and
workload 12.54% higher. Repeating or tuning that family would add little
information, so this cycle rejected the duplicate before changing protected
files.

The strongest source-specific signal still worth explaining is the WT bounded
recurrent residual result, including its replicated relabeling behavior, while
the same family failed three-family transfer and is dormant. The new scout is
therefore not another residual variant. It asks whether learned local lifting
operators in a dyadic multiresolution representation provide a qualitatively
different signature: reusable cross-scale composition, horizon extrapolation,
slot-local updates, bounded state and query work independent of the number of
support files.

WT v2 directly re-exports v1. It preserves all ten CSV hashes, train/test
separation, anonymous channel permutation, normalization, artifact-before-
reveal ordering, K=`18/36/54`, H=`16/32/96`, metrics, meaningful-effect
threshold `0.1325268421060828`, the 16 MiB boundary, seed policy, full cost,
Pareto axes and all eight registered controls. The only prospective semantic
change is the causal-role tuple
`wt_multiresolution_lifting_v1`,
`wt_source_identical_single_scale_lifting_v1`, and
`wt_source_identical_frozen_lifting_v1`, plus a machine-readable contract that
requires the roles to share representation constants, initialization, fit
order, update schedule and output code except for the preregistered cross-scale
composition and lifting-learning factors.

The development-only real-file smoke passed on development seeds 6 and 7 with
`test_seeds_read=false`. The first full test run detected exactly the expected
stale preflight certificate after protected changes; no candidate or numerical
scoring ran. After freezing the new cohort and regenerating the certificate,
all 409 tests, eight semantic baseline checks, 582-file integrity verification,
preflight and doctor passed.

## INTERPRETATION

This migration changes no data or numerical evaluation. It makes the next
causal comparison truthful and blocks accidental reuse of the completed
recurrent-residual roles. The single-scale ablation tests whether hierarchy is
causal; the frozen-lifting ablation tests whether learning the local operators
is causal. Source identity prevents architecture drift between those claims.

## CONFIDENCE

Confidence is `0.99` that v2 preserves v1 numerical semantics because its
evaluator objects are direct re-exports, their static contracts compare equal,
and the real-file smoke passed without reading test seeds. Confidence in the
multiresolution mechanism remains low until the exact constants are
preregistered and one blinded quick completes.

## DECISION

`activate_heldout_wt_changepoints_prequential_v2_for_one_multiresolution_lifting_scout`.
No hypothesis confidence or scientific evidence changed. Do not spend the next
wake on another review or migration unless a real gate fails.

## EXACT NEXT DISCRIMINATING EXPERIMENT

Create one new low-confidence hypothesis and one immutable quick before any
candidate implementation. Freeze the dyadic levels over history length 32,
lifting parameterization, initialization, local chronological fit/update rule,
stability bound and the existing development-only meaningful NRMSE threshold.
The main, single-scale and frozen-lifting roles must use the same source,
constants and anonymous permutation-equivariant channel operations, differing
only in their declared causal factor.

Run exactly one runner-random seed over every K/H cell and all eight frozen WT
controls. Require the main role to beat both causal ablations by at least
`0.1325268421060828` NRMSE in every K cell at H=32 and H=96, avoid worst-file
and transition-window regression, remain stable, match or beat the strongest
classical control on quality, and be Pareto non-dominated under full
acquisition, fit, query, update, memory, workload and declared-horizon cost.
Also require lower update operations and bytes than LMS/RLS at every K,
K-independent query work at fixed H, bounded state and success beyond the fit
horizon. One seed can only select an unchanged replication. A valid negative
ends this exact lifting rule without tuning.
