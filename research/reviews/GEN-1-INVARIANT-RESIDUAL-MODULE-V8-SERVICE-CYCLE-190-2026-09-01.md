# Cycle 190: invariant residual-module identifiability and v8 role activation

## Scope

This was one bounded service-only cycle after valid negative
`EXP-20260901-0037`. It created no hypothesis, experiment plan, candidate,
scoring seed, runner invocation, result, evidence or confidence update. It did
not read a new scoring target. Historical plans, results, analyses and
manifests remain append-only and were not recomputed.

## Observation

The WT evaluator does not expose an environment grouping to a candidate. It
flattens six fit files event-major into one tuple of 54 `WTEpisode` objects.
The only repeated public grouping variable is the four-level control, so using
it as the environment would reproduce the registered control-level bank rather
than test invariant module discovery.

The three-family tensor contract preserves every training example as a
separate anonymous `World`. At K=`4/6/9`, a shared fit receives respectively
12, 18 or 27 candidate-visible world boundaries. These boundaries are enough
to compare a conditional residual law across at least three environments.
`World.slot` is unnecessary and is now explicitly forbidden. Tensor masks may
only remove padded cells under one scalar relation extractor; their signature,
valid width or shape may not route, key or select a module because that would
perfectly classify the evaluator-private family.

This discriminator is not supplied by WT control-level banks, change-point
RLS, CSSR or spectral PSR. In three-family it is also distinct from predictive
indexing and recurrent residual fitting: the proposed factor is selection of a
shared relation module by cross-environment residual stability. Pooled ridge,
RLS, empirical joint, contextual Chow--Liu, autoregressive, raw-window and
random-hash models remain mandatory alternatives capable of falsifying any
claimed advantage.

## Protected role-only successor

`heldout_three_family_continuous_transfer_v8` re-exports the unchanged v1
evaluator and preserves every world, split, tensor, normalization, metric,
threshold, K value, cost, seed rule, Pareto axis and baseline. It adds four
standard assignment roles for one future invariant-module core plus two pooled
source-identical nulls: environment-stability selection disabled and partition
frozen before data. No candidate module exists in this cycle. The exact
residual statistic, stability test, partition, thresholds, ties, update and
operation accounting must be preregistered before implementation.

The previous continuous-local manifest is archived at
`research/manifests/continuous_local_cellular_v4-protocol-v2-d676522b30ad.json`.
V8 evaluator digest is
`a5845f6dcb25024a344d94df203e784d8bc9ae86463bce8638a169d38a822811`,
candidate bundle digest is
`ddb8648fb4cb375ac00d9cda5213712741f640e5a6ed0e596d534682d05245b3`,
and preflight certificate is
`2100cb157a7fd14acab37d2ad8ea0f518eb689a60ae4211ffa9571cd2fa92577`.
All 518 tests, integrity over 692 files and doctor passed.

## Interpretation and uncertainty

V8 makes the proposed causal factor observationally testable without adding a
new dataset or hand ontology. It does not show that stable transferable modules
exist. Confidence is `0.99` that the anonymous world-count gate is satisfied
and that WT lacks the required public grouping. Confidence is `0.80` that the
registered nulls separate environment stability from ordinary pooling; a
future implementation could still collapse functionally to a mixture of
linear experts, which the classical controls and nulls must expose.

## Decision and exact next experiment

Decision: `activate_v8_roles_for_one_invariant_residual_module_quick`.

Cycle 191 must create low-confidence `HYP-0049` and immutable expected
`EXP-20260901-0038` before implementing anything, then execute exactly one
runner-random quick on v8 at K=`4/6/9`, D=`1`. The plan must freeze one
source-identical scalar relation extractor, residual-law stability statistic,
module partition, tie rule, thresholds, initialization, update and complete
operation/state accounting. It must include all six v8 roles and all nine
frozen controls. Reject unless shared beats independent, cross-family-only,
support-only, pooled-without-invariance, frozen-partition and the strongest
complete classical control by a development-frozen meaningful NRMSE margin in
every family, remains stable, avoids worst-family collapse and is universally
Pareto non-dominated. Any use of slot, family/mask routing or post-score tuning
invalidates. A positive one-seed result can only authorize unchanged
three-seed adversarial replication; a valid null or negative ends the exact
mechanism without tuning. No second service wake is permitted before this
quick.
